Al terminar, tendrás:

* El CLI de Databricks `1.0.0+` instalado y disponible en el `PATH`
* Un perfil de CLI autenticado (`databricks auth profiles` muestra `Valid: YES`)
* Una prueba de humo correcta (`databricks current-user me` devuelve tu identidad)