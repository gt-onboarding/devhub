Une fois terminé, vous disposerez de :

* La CLI Databricks `1.0.0+` installée et accessible depuis le `PATH`
* Un profil CLI authentifié (`databricks auth profiles` affiche `Valid: YES`)
* Un test de validation réussi (`databricks current-user me` renvoie votre identité)