// fallow-ignore-file unused-file
// Source for the generated public asset at public/js/home-hero-player.js.
const DEFAULT_COPY = {
  cliInstalled: "Databricks CLI successfully installed.",
  cliInstalledNext:
    "You can now begin building and deploying apps on Databricks.",
  prompt:
    "Build me an app our sales team can use to track revenue and quota across\nregions. Add AI to spot problems and suggest next steps.",
  buildTitle: "Starting build and deployment",
  statusInProgress: "In progress",
  statusOk: "OK",
  finalDone: "Done - your app is live",
};

function createDatabricksHeroExportPlayer(root, options = {}) {
  const copy = { ...DEFAULT_COPY, ...(options.copy ?? {}) };
  window.DATABRICKS_EXPORT_MODE = true;
  window.DATABRICKS_USED_FEATURES = {
    powerOn: true,
    logoReveal: true,
    logoTubeWarmup: false,
    terminalTyping: true,
    terminalScroll: true,
    buildStatus: true,
    finalStatus: true,
    windowBoot: true,
    appReveal: true,
    parallax: false,
    rgbSplit: true,
    shadeMap: false,
    rgbGrid: true,
    scanlines: true,
    vignette: false,
    perspective: true,
    dragEcho: true,
    logoClick: true,
    guide: false,
    perfMonitor: false,
    editorControls: false,
  };
  window.DATABRICKS_EXPORT_ASSETS = {
    logoSvg:
      '<svg width="1280" height="176" viewBox="0 0 1280 176" fill="none" xmlns="http://www.w3.org/2000/svg">\n<mask id="mask0_7186_508" style="mask-type:alpha" maskUnits="userSpaceOnUse" x="0" y="5" width="1251" height="167">\n<path d="M954.706 16.3207H983.949V45.564H907.401V140.175H983.949V169.418H885.898V151.356" stroke="#2E3038" style="stroke:#2E3038;stroke:color(display-p3 0.1804 0.1882 0.2196);stroke-opacity:1;" stroke-width="5.16058"/>\n<rect x="873.857" y="5.99976" width="98.051" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="873.857" y="129.853" width="98.051" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="866.117" y="36.963" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="866.117" y="67.9265" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="866.117" y="98.89" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<path d="M1155.11 16.3208H1248V45.5641H1171.45V78.2477H1230.8V107.491H1248V140.175H1235.1V169.418H1142.21V140.175" stroke="#2E3038" style="stroke:#2E3038;stroke:color(display-p3 0.1804 0.1882 0.2196);stroke-opacity:1;" stroke-width="5.16058"/>\n<path d="M1218.76 107.491H1155.11V92.0093" stroke="#2E3038" style="stroke:#2E3038;stroke:color(display-p3 0.1804 0.1882 0.2196);stroke-opacity:1;" stroke-width="5.16058"/>\n<rect x="1143.06" y="6" width="92.8904" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="1143.06" y="67.9268" width="75.6885" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="1130.16" y="129.854" width="92.8904" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="1130.16" y="36.9634" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="1206.71" y="98.8901" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<path d="M1026.95 16.3209H1040.71V78.2479H1068.24" stroke="#2E3038" style="stroke:#2E3038;stroke:color(display-p3 0.1804 0.1882 0.2196);stroke-opacity:1;" stroke-width="5.16058"/>\n<path d="M1085.44 16.3208H1114.68V45.5641H1097.48V78.2477H1082.86V107.491H1097.48V140.175H1114.68V169.418H1085.44V140.175M1011.47 156.517V169.418H1040.71V107.491H1068.24V123.833" stroke="#2E3038" style="stroke:#2E3038;stroke:color(display-p3 0.1804 0.1882 0.2196);stroke-opacity:1;" stroke-width="5.16058"/>\n<rect x="999.428" y="6" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="1073.4" y="6" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="999.428" y="98.8901" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="1056.2" y="98.8901" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="999.428" y="36.9634" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="1056.2" y="36.9634" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="999.428" y="129.854" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="1073.4" y="129.854" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="999.428" y="67.9268" width="71.388" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="809.352" y="6" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="809.352" y="36.9634" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="809.352" y="67.9268" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="809.352" y="98.8901" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="809.352" y="129.854" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<path d="M821.393 16.3208H850.636V169.418H821.393V149.206" stroke="#2E3038" style="stroke:#2E3038;stroke:color(display-p3 0.1804 0.1882 0.2196);stroke-opacity:1;" stroke-width="5.16058"/>\n<path d="M540.998 16.3208H639.049V45.5641H653.671V78.2477H631.308V107.491H653.671V140.175H639.049V169.418H540.998V152.216" stroke="#2E3038" style="stroke:#2E3038;stroke:color(display-p3 0.1804 0.1882 0.2196);stroke-opacity:1;" stroke-width="5.16058"/>\n<path d="M624.428 61.906V45.5642H570.242V78.2479H597.335" stroke="#2E3038" style="stroke:#2E3038;stroke:color(display-p3 0.1804 0.1882 0.2196);stroke-opacity:1;" stroke-width="5.16058"/>\n<path d="M624.428 123.833V107.491H570.242V140.175H597.335" stroke="#2E3038" style="stroke:#2E3038;stroke:color(display-p3 0.1804 0.1882 0.2196);stroke-opacity:1;" stroke-width="5.16058"/>\n<rect x="528.955" y="6" width="98.051" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="528.955" y="67.9268" width="90.3101" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="528.955" y="129.854" width="98.051" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="528.955" y="36.9634" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="528.955" y="98.8901" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="612.385" y="36.9634" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="612.385" y="98.8901" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<path d="M681.197 16.3208H779.248V45.5641H793.87V78.2477H771.507V107.491H793.87V169.418H764.627V154.366" stroke="#2E3038" style="stroke:#2E3038;stroke:color(display-p3 0.1804 0.1882 0.2196);stroke-opacity:1;" stroke-width="5.16058"/>\n<path d="M764.626 61.9063V45.5645H710.439V78.2481H737.532" stroke="#2E3038" style="stroke:#2E3038;stroke:color(display-p3 0.1804 0.1882 0.2196);stroke-opacity:1;" stroke-width="5.16058"/>\n<path d="M764.627 107.491H710.441V169.418H681.197V159.957" stroke="#2E3038" style="stroke:#2E3038;stroke:color(display-p3 0.1804 0.1882 0.2196);stroke-opacity:1;" stroke-width="5.16058"/>\n<rect x="669.15" y="6" width="98.051" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="669.15" y="67.9268" width="90.3101" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="669.15" y="36.9634" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="669.15" y="98.8901" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="669.15" y="129.854" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="752.58" y="36.9634" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="752.58" y="98.8901" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="752.58" y="129.854" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<path d="M318.236 156.947V169.418H347.479V45.5641H393.064V16.3208H272.65V45.5641H318.236" stroke="#2E3038" style="stroke:#2E3038;stroke:color(display-p3 0.1804 0.1882 0.2196);stroke-opacity:1;" stroke-width="5.16058"/>\n<rect x="260.609" y="5.99963" width="120.414" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="306.195" y="36.9631" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="306.195" y="67.9265" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="306.195" y="98.8899" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="306.195" y="129.853" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<path d="M166.857 16.3208H244.266V45.5641H258.888V169.418H229.644V152.216" stroke="#2E3038" style="stroke:#2E3038;stroke:color(display-p3 0.1804 0.1882 0.2196);stroke-opacity:1;" stroke-width="5.16058"/>\n<path d="M229.644 45.5645H181.479V78.2481H229.644" stroke="#2E3038" style="stroke:#2E3038;stroke:color(display-p3 0.1804 0.1882 0.2196);stroke-opacity:1;" stroke-width="5.16058"/>\n<path d="M229.643 107.491H181.478V169.418H152.234V149.636" stroke="#2E3038" style="stroke:#2E3038;stroke:color(display-p3 0.1804 0.1882 0.2196);stroke-opacity:1;" stroke-width="5.16058"/>\n<rect x="154.816" y="6" width="77.4087" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="140.195" y="67.9268" width="106.652" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="140.195" y="36.9634" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="140.195" y="98.8901" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="140.195" y="129.854" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="217.604" y="36.9634" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="217.604" y="98.8901" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="217.604" y="129.854" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<path d="M421.449 16.3208H498.858V45.5641H513.48V169.418H484.236V152.216" stroke="#2E3038" style="stroke:#2E3038;stroke:color(display-p3 0.1804 0.1882 0.2196);stroke-opacity:1;" stroke-width="5.16058"/>\n<path d="M484.236 45.5645H436.07V78.2481H484.236" stroke="#2E3038" style="stroke:#2E3038;stroke:color(display-p3 0.1804 0.1882 0.2196);stroke-opacity:1;" stroke-width="5.16058"/>\n<path d="M484.237 107.491H436.071V169.418H406.828V149.636" stroke="#2E3038" style="stroke:#2E3038;stroke:color(display-p3 0.1804 0.1882 0.2196);stroke-opacity:1;" stroke-width="5.16058"/>\n<rect x="409.406" y="6" width="77.4087" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="394.785" y="67.9268" width="106.652" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="394.785" y="36.9634" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="394.785" y="98.8901" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="394.785" y="129.854" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="472.193" y="36.9634" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="472.193" y="98.8901" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="472.193" y="129.854" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<path d="M12.0391 16.3208H110.09V45.5641H124.712V140.175H110.09V169.418H12.0391V156.947" stroke="#2E3038" style="stroke:#2E3038;stroke:color(display-p3 0.1804 0.1882 0.2196);stroke-opacity:1;" stroke-width="5.16058"/>\n<path d="M95.4693 45.5645H41.2832V140.175H95.4693" stroke="#2E3038" style="stroke:#2E3038;stroke:color(display-p3 0.1804 0.1882 0.2196);stroke-opacity:1;" stroke-width="5.16058"/>\n<rect x="7.74023" y="6" width="90.3101" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect y="129.854" width="98.051" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect y="36.9633" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="83.4297" y="36.9634" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect y="67.9268" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="83.4297" y="67.9268" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect y="98.8903" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n<rect x="83.4297" y="98.8901" width="29.2433" height="29.2433" fill="#2E3038" style="fill:#2E3038;fill:color(display-p3 0.1804 0.1882 0.2196);fill-opacity:1;"/>\n</mask>\n<g mask="url(#mask0_7186_508)">\n<rect x="-30.1034" y="-25.8237" width="1303.91" height="212.444" fill="#32323C" style="fill:#32323C;fill:color(display-p3 0.1955 0.1955 0.2357);fill-opacity:1;"/>\n</g>\n</svg>\n',
  };
  window.DATABRICKS_EXPORT_CONFIG = {
    version: 1,
    typographyVersion: 2,
    scrollTuningVersion: 1,
    colorRevealVersion: 2,
    currentState: 1,
    settings: {
      autoStep: false,
      loopState: false,
      loopRestartDelay: 20000,
      timelineTrimStart: 1,
      timelineTrimEnd: 6,
      timelineLoop: false,
    },
    stateAnimationSettings: {
      1: {
        speed: 1,
        delayBefore: 0,
        delayAfter: 500,
        powerOnEnabled: true,
        powerOnDuration: 1460,
        powerOnLineDuration: 1800,
        powerOnLineExpandDuration: 235,
        powerOnYScaleDuration: 2200,
        powerOnLineHold: 140,
        powerOnBloom: 0,
        powerOnBrightness: 0.69,
        powerOnStretchBrightness: 0.2,
        powerOnColor: "#dedede",
        powerOnY: 36,
        powerOnYStretch: 420,
        powerOnScale: 0.03,
        powerOnSceneStartY: -184,
        powerOnSceneStartSquash: 0.41,
        powerOnSceneYScale: 1.03,
        powerOnJitter: 11,
        powerOnWarmup: 1,
        logoTubeStartY: -67,
        logoTubeStartSquash: 0.17,
        logoTubeStartWidth: 1.07,
        logoTubeStartOpacity: 0.44,
        installTextDelay: 1800,
        installType: 0,
        installFramePause: 600,
      },
      2: {
        speed: 1,
        delayBefore: 300,
        delayAfter: 0,
        promptType: 2,
        promptTypingContrast: 3,
        promptCursorBlink: 1500,
        promptStartDelay: 0,
        promptTypingMode: "letters",
        promptLinePause: 240,
        promptEnterMoment: true,
        promptEnterColor: "#cfcfd2",
        promptEnterFlashColor: "#808080",
      },
      3: {
        speed: 1,
        delayBefore: 0,
        delayAfter: 600,
        buildLineReveal: 860,
        statusOkSteps: 4,
        statusAsciiWindow: 5,
        statusAsciiDelay: 120,
        statusAsciiSpeed: 280,
        terminalScroll: true,
        statusLeaderGap: 35,
        statusLeaderX: -11.5,
        statusLeaderY: 3,
        statusDotBurstDuration: 160,
      },
      4: {
        speed: 1,
        delayBefore: 600,
        delayAfter: 1880,
        terminalScroll: true,
        terminalScrollDuration: 400,
        terminalScrollDistance: 12,
        statusDotBurstDuration: 200,
        statusLeaderGap: 44,
        statusLeaderX: 0,
        statusLeaderY: -7.5,
        terminalScrollMotionJitter: 0,
        terminalScrollInstall: 0.8,
        terminalScrollPrompt: 0.8,
        terminalScrollBuild: 0.8,
        terminalScrollEndOpacity: 0,
        terminalScrollOpacityJitter: 0.5,
        terminalScrollJitterX: 0,
        terminalScrollJitterY: 0,
      },
      5: {
        speed: 1,
        delayBefore: 0,
        delayAfter: 0,
        windowBootEnabled: true,
        windowBootClip: false,
        windowBootDuration: 1800,
        windowBootOrigin: "custom",
        windowBootX: -160,
        windowBootY: 171,
        windowBootSize: 10,
        windowBootSteps: 8,
        windowBootTraceIterations: 10,
        windowBootTrailCount: 3,
        windowBootWindowHold: 1600,
        windowBootControlSize: 10,
        windowBootControlGap: 8.5,
        windowBootControlX: 0,
        windowBootControlY: 1.5,
        windowBootControlStroke: 3,
        windowBootGrow1: 1.9,
        windowBootGrow2: 3.4,
        windowBootEchoCount: 0,
        windowBootEchoSpread: 35,
        windowBootEchoOpacity: 0,
        windowBootAsciiDuration: 0,
        windowBootAsciiStagger: 0,
        windowBootAsciiNoise: 0,
      },
      6: {
        speed: 1.25,
        delayBefore: 80,
        delayAfter: 5000,
      },
    },
    sharedTextLayers: {
      install: {
        x: 195,
        y: 353,
        width: 710,
        size: 17,
        letterSpacing: 0.2,
        opacity: 1,
      },
      prompt: {
        x: 190,
        y: 439,
        width: 980,
        size: 17,
        letterSpacing: 0.3,
        opacity: 1,
      },
      build: {
        x: 190,
        y: 536,
        width: 749,
        size: 17,
        letterSpacing: 0.3,
        opacity: 1,
        listIndent: 21,
        listY: -9,
        rowGap: 1,
        starSize: 0.9,
        bulletSize: 1.05,
        bulletX: 8,
        bulletY: -2,
      },
      final: {
        x: 171,
        y: 360,
        width: 760,
        size: 17,
        letterSpacing: 0.1,
        opacity: 1,
        blockX: 0,
        blockY: -8,
        checkSparkX: 0.6,
        checkSparkY: 0.8,
        linkIndent: 52,
        checkSize: 24,
        checkX: 12,
        checkY: 0,
        linkY: 0,
        linkSize: 17,
        linkLetterSpacing: 0.2,
        promptX: 20,
        promptY: -10,
        promptSize: 18,
        linkLineHeight: 1.5,
        linkAsciiDuration: 700,
        linkAsciiNoise: 2,
        linkAsciiHold: 60,
        linkRevealDelay: 560,
        linkRevealDuration: 160,
      },
    },
    sharedAppSettings: {
      left: 576,
      top: 242,
      scale: 1,
    },
    staticAppSettings: {
      windowWidth: 768,
      windowHeight: 514,
      sidebarWidth: 64,
      contentPadTop: 16,
      contentPadRight: 18,
      contentPadBottom: 14,
      contentPadLeft: 18,
      headerMarginBottom: 39,
      chartPlotY: 34,
      appTitleSize: 20,
      appSubtitleSize: 12,
      sharedTitleSize: 14,
      sharedSubtitleSize: 10,
    },
    preAppFocusSettings: {
      opacity: 0.6,
      scale: 1,
      duration: 760,
      steps: 4,
    },
    dragEchoSettings: {
      color: "#27272a",
      opacity: 1,
      lifetime: 1000,
      maxFrames: 5,
      width: 2,
      minDistance: 160,
      minInterval: 20,
    },
    stateAppRevealSettings: {
      5: {
        preset: "colorWave",
        intensity: 3.2,
        duration: 980,
        lineColor: "#ffffff",
        lineDirection: "horizontal",
        lineOpacity: 0.3,
        lineGlow: 0,
        lineHold: 380,
        lineFade: 460,
        grid: 5,
        corner: "tl",
        originX: 0,
        originY: 0,
        layers: 1,
      },
      6: {
        preset: "colorWave",
        intensity: 3.2,
        duration: 980,
        lineColor: "#ffffff",
        lineDirection: "horizontal",
        lineOpacity: 0.3,
        lineGlow: 0,
        lineHold: 380,
        lineFade: 460,
        grid: 5,
        corner: "tl",
        originX: 0,
        originY: 0,
        layers: 1,
      },
    },
    stateLogoSettings: {
      1: {
        x: 190,
        y: 123,
        width: 1285,
        baseOpacity: 0.15,
        mainColor: "#32323c",
        opacityDuration: 420,
        revealDuration: 1500,
        revealDelay: 0,
        stutter: 0,
      },
    },
    groupGlitchSettings: {
      preset: "perspective",
      activePresets: ["perspective", "scanlines", "rgbsplit", "rgbgrid"],
      presets: {
        perspective: {
          intensity: 0.8,
          duration: 900,
          loop: false,
          detail: 5,
          speed: 0,
          tiltX: 0,
          tiltY: 4,
          depth: 2200,
          shiftX: 0,
          shiftY: 20,
          windowLift: 0,
          returnToCenter: false,
        },
        scanlines: {
          intensity: 5,
          duration: 1200,
          loop: false,
          detail: 0,
          speed: 0.4,
          thickness: 2,
          spacing: 2,
          barHeight: 110,
          sweepOpacity: 0.16,
          sweepGlow: 0.4,
          sweepShadow: 0.12,
          sweepSoftness: 80,
          reverse: true,
        },
        rgbsplit: {
          intensity: 2,
          logoIntensity: 2.4,
          textIntensity: 1.6,
          windowIntensity: 0,
          duration: 1000,
          loop: true,
          detail: 5.6,
          speed: 0,
          splitX: 2.3,
          splitY: 0,
          colorA: "#ff5542",
          colorB: "#0e855d",
          colorC: "#465fff",
        },
        rgbgrid: {
          intensity: 10,
          duration: 900,
          loop: true,
          detail: 3.4,
          speed: 0,
          stripeHeight: 1,
          stripeGap: 1,
          opacity: 1,
          softness: 0,
          redColor: "#0d0d0d",
          greenColor: "#121212",
          blueColor: "#000000",
          blendMode: "screen",
          redOpacity: 1,
          greenOpacity: 1,
          blueOpacity: 0.83,
        },
      },
    },
    logoClickSettings: {
      waveSpread: 2.25,
      waveCount: 1,
      waveGap: 0,
      waveHold: 139,
      waveOpacity: 1,
      tailHold: 0,
      tailOpacity: 1,
      pressX: 5,
      pressY: 5,
      glow: 0,
      hoverGlowOpacity: 0,
      hoverGlowBlur: 0,
      hoverGlowCore: 3.5,
      hoverGlowColor: "#000000",
      waveColor: "#3a3a45",
      tailColor: "#1a1b23",
      splitAmount: 2,
      splitOpacity: 0,
      splitRed: "#000000",
      splitBlue: "#616161",
    },
    exportSettings: {
      includeStaticPng: false,
    },
  };

  const eventController = new AbortController();
  let destroyed = false;
  const stage = root.querySelector("#stage");
  const stagePerspectiveShell = root.querySelector("#stagePerspectiveShell");
  const toolWorkspace = root.querySelector("#toolWorkspace");
  const stageViewport = root.querySelector("#stageViewport");
  const preAppScene = root.querySelector("#preAppScene");
  const installText = root.querySelector("#installText");
  const installLayer = root.querySelector(".install-copy");
  const promptLayer = root.querySelector(".prompt-copy");
  const promptText = root.querySelector("#promptText");
  const buildLines = [...root.querySelectorAll("[data-build-line]")];
  const buildStatus = root.querySelector(".build-status");
  const buildTitle = root.querySelector(".build-title");
  const buildTitleCopy = root.querySelector(".build-title-copy");
  const statusRows = [...root.querySelectorAll(".status-row")];
  const statusValues = [...root.querySelectorAll(".status-value")];
  const finalStatus = root.querySelector(".final-status");
  const finalDone = root.querySelector(".final-done");
  const finalCheck = root.querySelector(".final-check");
  const finalLink = root.querySelector(".final-link");
  const finalAscii = root.querySelector("#finalAscii");
  const finalPrompt = root.querySelector(".final-prompt");
  const appPreview = root.querySelector("#appPreview");
  const appPrototypeImage = root.querySelector(".app-prototype-image");
  const appDoneImage = root.querySelector(".app-done-image");
  const appChartOverlay = root.querySelector(".app-chart-overlay");
  const appRevealOverlay = root.querySelector("#appRevealOverlay");
  const powerOnOverlay = root.querySelector("#powerOnOverlay");
  const heroLogoReveal = root.querySelector("#heroLogoReveal");
  const logoSvgHosts = [...root.querySelectorAll("[data-logo-svg]")];
  const gridCanvas = root.querySelector("#gridCanvas");
  const gridCtx = gridCanvas?.getContext("2d") || null;
  const shadeMapOverlay = root.querySelector("#shadeMapOverlay");

  if (!stage || !appPreview) {
    return () => undefined;
  }

  const rootStyleValueCache = new Map();
  const RENDER_FPS_CAP = 30;
  const RENDER_FRAME_MS = 1000 / RENDER_FPS_CAP;
  const GRID_PIXEL_RATIO_CAP = 1;
  const APP_DRAG_LIMIT = 42;
  const APP_DRAG_HARD_LIMIT = 76;
  let appWindowDragOffset = { x: 0, y: 0 };
  let appWindowDragState = null;
  let appWindowEchoLayer = null;
  let shadeMapEraseFrame = 0;
  let shadeMapErasePointer = null;
  let shadeMapEraseTarget = null;
  let shadeMapEraseCurrent = null;
  const isExportPlayer = Boolean(window.DATABRICKS_EXPORT_MODE);
  let logoWaveTimers = [];
  const logoHoverSuppressionTimers = new Map();

  const nativeSetRootStyleProperty = root.style.setProperty.bind(root.style);

  function setRootVar(property, value = "", priority = "") {
    const nextValue = String(value);
    const nextPriority = String(priority || "");
    const cacheKey = `${property}\n${nextPriority}`;
    if (rootStyleValueCache.get(cacheKey) === nextValue) {
      return;
    }

    rootStyleValueCache.set(cacheKey, nextValue);
    nativeSetRootStyleProperty(property, nextValue, nextPriority);
  }

  const defaultTextLayers = {
    install: {
      x: 193,
      y: 354.5,
      width: 710,
      size: 18,
      letterSpacing: -0.36,
      opacity: 1,
    },
    prompt: {
      x: 205,
      y: 468,
      width: 980,
      size: 18,
      letterSpacing: -0.36,
      opacity: 1,
    },
    build: {
      x: 205,
      y: 571,
      width: 800,
      size: 18,
      letterSpacing: -0.36,
      opacity: 1,
      listIndent: 0,
      listY: 0,
      rowGap: 8,
      starSize: 1,
      bulletSize: 1,
      bulletX: 0,
      bulletY: 0,
    },
    final: {
      x: 205,
      y: 376,
      width: 760,
      size: 18,
      letterSpacing: -0.36,
      opacity: 1,
      blockX: 0,
      blockY: 0,
      checkSparkX: 1,
      checkSparkY: 1,
      linkIndent: 0,
      checkSize: 18,
      checkX: 0,
      checkY: 0,
      linkY: 1,
      linkSize: 22,
      linkLetterSpacing: -0.36,
      promptX: 0,
      promptY: 0,
      promptSize: 18,
      linkLineHeight: 1.5,
      linkAsciiDuration: 900,
      linkAsciiNoise: 8,
      linkAsciiHold: 180,
      linkRevealDelay: 620,
      linkRevealDuration: 160,
    },
  };

  const visibleTextLayersByState = {
    1: ["install"],
    2: ["install", "prompt"],
    3: ["install", "prompt", "build"],
    4: ["final"],
    5: ["final"],
    6: ["final"],
  };

  const sharedTextLayers = cloneData(defaultTextLayers);

  const sharedAppSettings = { left: 614, top: 254, scale: 1 };

  const staticAppSettings = {
    windowWidth: 768,
    windowHeight: 514,
    sidebarWidth: 64,
    contentPadTop: 16,
    contentPadRight: 16,
    contentPadBottom: 14,
    contentPadLeft: 18,
    headerMarginBottom: 39,
    chartPlotY: 22,
    appTitleSize: 20,
    appSubtitleSize: 12,
    sharedTitleSize: 14,
    sharedSubtitleSize: 10,
  };

  const deprecatedStaticAppSettingKeys = [
    "metricCardWidth",
    "metricCardHeight",
    "metricCardGap",
    "metricCardPaddingTop",
    "metricCardPaddingX",
    "metricCardPaddingBottom",
    "metricGridMarginBottom",
    "metricIconSize",
    "metricIconLeft",
    "metricIconTop",
    "metricChangeTop",
    "metricChangeRight",
    "metricLabelSize",
    "metricValueSize",
    "dashboardChartWidth",
    "dashboardRegionWidth",
    "dashboardGap",
    "dashboardPanelHeight",
    "dashboardPanelPadding",
    "dashboardTitleMarginBottom",
  ];

  const preAppFocusSettings = {
    opacity: 0.34,
    scale: 0.9,
    duration: 560,
    steps: 6,
  };

  const dragEchoSettings = {
    color: "#ffffff",
    opacity: 0.74,
    lifetime: 760,
    maxFrames: 18,
    width: 1,
    minDistance: 7,
    minInterval: 46,
  };

  const stateLogoSettings = {
    1: {
      x: 205,
      y: 129,
      width: 1335,
      baseOpacity: 0.28,
      mainColor: "#32323c",
      opacityDuration: 420,
      revealDuration: 1365,
      revealDelay: 0,
      stutter: 6,
      reflexOpacity: 0,
      reflexBlur: 12,
      reflexX: 0,
      reflexY: 0,
      reflexLeft: 0,
      reflexTop: 0,
      reflexScaleX: 1,
      reflexScaleY: 1,
      reflexShadeOpacity: 0.58,
      reflexShadeX: 0,
      reflexShadeY: -4,
      reflexShadeWidth: 1600,
      reflexShadeHeight: 92,
      reflexShadeBlur: 0,
      reflexShadeColor: "#000000",
    },
  };

  const defaultStateAnimationSettings = {
    1: {
      speed: 1,
      delayBefore: 0,
      delayAfter: 500,
      powerOnEnabled: true,
      powerOnDuration: 1460,
      powerOnLineDuration: 1800,
      powerOnLineExpandDuration: 235,
      powerOnYScaleDuration: 2200,
      powerOnLineHold: 140,
      powerOnBloom: 0,
      powerOnBrightness: 0.69,
      powerOnStretchBrightness: 0.2,
      powerOnColor: "#dedede",
      powerOnY: 36,
      powerOnYStretch: 420,
      powerOnScale: 0.03,
      powerOnSceneStartY: -184,
      powerOnSceneStartSquash: 0.41,
      powerOnSceneYScale: 1.03,
      powerOnJitter: 11,
      powerOnWarmup: 1,
      logoTubeStartY: -67,
      logoTubeStartSquash: 0.17,
      logoTubeStartWidth: 1.07,
      logoTubeStartOpacity: 0.44,
      installTextDelay: 1800,
      installType: 0,
      installFramePause: 600,
    },
    2: {
      speed: 1,
      delayBefore: 300,
      delayAfter: 0,
      promptType: 2,
      promptTypingContrast: 3,
      promptCursorBlink: 1500,
      promptStartDelay: 0,
      promptTypingMode: "letters",
      promptLinePause: 240,
      promptEnterMoment: true,
      promptEnterColor: "#cfcfd2",
      promptEnterFlashColor: "#808080",
    },
    3: {
      speed: 1,
      delayBefore: 0,
      delayAfter: 600,
      buildLineReveal: 860,
      statusOkSteps: 4,
      statusAsciiWindow: 5,
      statusAsciiDelay: 120,
      statusAsciiSpeed: 280,
      terminalScroll: true,
      statusLeaderGap: 35,
      statusLeaderX: -11.5,
      statusLeaderY: 3,
      statusDotBurstDuration: 160,
    },
    4: {
      speed: 1,
      delayBefore: 600,
      delayAfter: 1880,
      terminalScroll: true,
      terminalScrollDuration: 400,
      terminalScrollDistance: 12,
      statusDotBurstDuration: 200,
      statusLeaderGap: 44,
      statusLeaderX: 0,
      statusLeaderY: -7.5,
      terminalScrollMotionJitter: 0,
      terminalScrollInstall: 0.8,
      terminalScrollPrompt: 0.8,
      terminalScrollBuild: 0.8,
      terminalScrollEndOpacity: 0,
      terminalScrollOpacityJitter: 0.5,
      terminalScrollJitterX: 0,
      terminalScrollJitterY: 0,
    },
    5: {
      speed: 1,
      delayBefore: 0,
      delayAfter: 0,
      windowBootEnabled: true,
      windowBootClip: false,
      windowBootDuration: 1800,
      windowBootOrigin: "custom",
      windowBootX: -160,
      windowBootY: 171,
      windowBootSize: 10,
      windowBootSteps: 8,
      windowBootTraceIterations: 10,
      windowBootTrailCount: 3,
      windowBootWindowHold: 1600,
      windowBootControlSize: 10,
      windowBootControlGap: 8.5,
      windowBootControlX: 0,
      windowBootControlY: 1.5,
      windowBootControlStroke: 3,
      windowBootGrow1: 1.9,
      windowBootGrow2: 3.4,
      windowBootEchoCount: 0,
      windowBootEchoSpread: 35,
      windowBootEchoOpacity: 0,
      windowBootAsciiDuration: 0,
      windowBootAsciiStagger: 0,
      windowBootAsciiNoise: 0,
    },
    6: {
      speed: 1.25,
      delayBefore: 80,
      delayAfter: 5000,
    },
  };

  const stateAnimationSettings = Object.fromEntries(
    Object.entries(defaultStateAnimationSettings).map(
      ([state, stateSettings]) => [state, { ...stateSettings }],
    ),
  );

  const defaultGridSettings = {};

  const stateGridSettings = Object.fromEntries(
    Object.keys(visibleTextLayersByState).map((state) => [
      state,
      { ...defaultGridSettings },
    ]),
  );

  const stateAppRevealSettings = {
    5: {
      preset: "colorWave",
      intensity: 3.2,
      duration: 980,
      lineColor: "#ffffff",
      lineDirection: "horizontal",
      lineOpacity: 0.3,
      lineGlow: 0,
      lineHold: 380,
      lineFade: 460,
      grid: 5,
      corner: "tl",
      originX: 0,
      originY: 0,
      layers: 1,
    },
    6: {
      preset: "colorWave",
      intensity: 3.2,
      duration: 980,
      lineColor: "#ffffff",
      lineDirection: "horizontal",
      lineOpacity: 0.3,
      lineGlow: 0,
      lineHold: 380,
      lineFade: 460,
      grid: 5,
      corner: "tl",
      originX: 0,
      originY: 0,
      layers: 1,
    },
  };

  function setAppRevealOriginTopLeft(revealSettings) {
    if (!revealSettings || revealSettings.preset !== "colorWave") {
      return;
    }

    revealSettings.corner = "tl";
    revealSettings.originX = 0;
    revealSettings.originY = 0;
  }

  function normalizeWindowBootSettings() {
    const stateSettings = stateAnimationSettings[5];
    if (!stateSettings) {
      return;
    }

    if (
      stateSettings.windowBootEnabled &&
      Number(stateSettings.windowBootDuration || 0) <= 0
    ) {
      stateSettings.windowBootDuration = 1180;
    }
    stateSettings.windowBootClip = Boolean(stateSettings.windowBootClip);
    if (
      !Number.isFinite(Number(stateSettings.windowBootSteps)) ||
      Number(stateSettings.windowBootSteps) <= 0
    ) {
      stateSettings.windowBootSteps = 4;
    }
    if (
      !Number.isFinite(Number(stateSettings.windowBootTraceIterations)) ||
      Number(stateSettings.windowBootTraceIterations) <= 0
    ) {
      stateSettings.windowBootTraceIterations = Math.max(
        4,
        Math.round(Number(stateSettings.windowBootSteps || 4)) + 3,
      );
    }
    if (
      !Number.isFinite(Number(stateSettings.windowBootTrailCount)) ||
      Number(stateSettings.windowBootTrailCount) <= 0
    ) {
      stateSettings.windowBootTrailCount = 3;
    }
    if (
      !Number.isFinite(Number(stateSettings.windowBootWindowHold)) ||
      Number(stateSettings.windowBootWindowHold) < 0
    ) {
      stateSettings.windowBootWindowHold = 420;
    }
    if (
      !Number.isFinite(Number(stateSettings.windowBootSize)) ||
      Number(stateSettings.windowBootSize) <= 0
    ) {
      stateSettings.windowBootSize = 10;
    }
    if (
      !Number.isFinite(Number(stateSettings.windowBootControlSize)) ||
      Number(stateSettings.windowBootControlSize) <= 0
    ) {
      stateSettings.windowBootControlSize = 8;
    }
    if (
      !Number.isFinite(Number(stateSettings.windowBootControlGap)) ||
      Number(stateSettings.windowBootControlGap) < 0
    ) {
      stateSettings.windowBootControlGap = 8;
    }
    if (!Number.isFinite(Number(stateSettings.windowBootControlX))) {
      stateSettings.windowBootControlX = 0;
    }
    if (!Number.isFinite(Number(stateSettings.windowBootControlY))) {
      stateSettings.windowBootControlY = 0;
    }
    if (
      !Number.isFinite(Number(stateSettings.windowBootControlStroke)) ||
      Number(stateSettings.windowBootControlStroke) <= 0
    ) {
      stateSettings.windowBootControlStroke = 1.5;
    }
  }

  function normalizeBuildStatusSettings() {
    const stateSettings = stateAnimationSettings[3];
    if (!stateSettings) {
      return;
    }

    if (
      !Number.isFinite(Number(stateSettings.statusAsciiSpeed)) ||
      Number(stateSettings.statusAsciiSpeed) <= 0
    ) {
      stateSettings.statusAsciiSpeed = 160;
    }

    if (
      !Number.isFinite(Number(stateSettings.statusAsciiDelay)) ||
      Number(stateSettings.statusAsciiDelay) < 0
    ) {
      stateSettings.statusAsciiDelay = 0;
    }
  }

  function normalizeTerminalScrollSettings() {
    const stateSettings = stateAnimationSettings[4];
    const legacyBuildSettings = stateAnimationSettings[3] || {};
    if (!stateSettings) {
      return;
    }

    if (
      !Number.isFinite(Number(stateSettings.statusDotBurstDuration)) ||
      Number(stateSettings.statusDotBurstDuration) <= 0
    ) {
      stateSettings.statusDotBurstDuration =
        Number(stateAnimationSettings[3]?.statusAsciiSpeed) || 160;
    }
    if (!Number.isFinite(Number(stateSettings.statusLeaderGap))) {
      stateSettings.statusLeaderGap = Number.isFinite(
        Number(legacyBuildSettings.statusLeaderGap),
      )
        ? Number(legacyBuildSettings.statusLeaderGap)
        : 44;
    }
    if (!Number.isFinite(Number(stateSettings.statusLeaderX))) {
      stateSettings.statusLeaderX = Number.isFinite(
        Number(legacyBuildSettings.statusLeaderX),
      )
        ? Number(legacyBuildSettings.statusLeaderX)
        : 0;
    }
    if (!Number.isFinite(Number(stateSettings.statusLeaderY))) {
      stateSettings.statusLeaderY = Number.isFinite(
        Number(legacyBuildSettings.statusLeaderY),
      )
        ? Number(legacyBuildSettings.statusLeaderY)
        : 3;
    }
  }

  const legacyGroupFxPresetDefaults = {
    perspective: {
      intensity: 0.8,
      duration: 900,
      loop: false,
      detail: 5,
      speed: 0,
      tiltX: 0,
      tiltY: 4,
      depth: 2200,
      shiftX: 0,
      shiftY: 20,
      windowLift: 0,
      returnToCenter: false,
    },
    scanlines: {
      intensity: 5,
      duration: 1200,
      loop: false,
      detail: 0,
      speed: 0.4,
      thickness: 2,
      spacing: 2,
      barHeight: 110,
      sweepOpacity: 0.16,
      sweepGlow: 0.4,
      sweepShadow: 0.12,
      sweepSoftness: 80,
      reverse: true,
    },
    rgbsplit: {
      intensity: 2,
      logoIntensity: 2.4,
      textIntensity: 1.6,
      windowIntensity: 0,
      duration: 1000,
      loop: true,
      detail: 5.6,
      speed: 0,
      splitX: 2.3,
      splitY: 0,
      colorA: "#ff5542",
      colorB: "#0e855d",
      colorC: "#465fff",
    },
    rgbgrid: {
      intensity: 10,
      duration: 900,
      loop: true,
      detail: 3.4,
      speed: 0,
      stripeHeight: 1,
      stripeGap: 1,
      opacity: 1,
      softness: 0,
      redColor: "#0d0d0d",
      greenColor: "#121212",
      blueColor: "#000000",
      blendMode: "screen",
      redOpacity: 1,
      greenOpacity: 1,
      blueOpacity: 0.83,
    },
  };

  const groupFxPresetDefaults = legacyGroupFxPresetDefaults;

  const fxPresetOrder = window.DatabricksHeroEffectRegistry
    ? window.DatabricksHeroEffectRegistry.order
    : [
        "rgbsplit",
        "shademap",
        "rgbgrid",
        "scanlines",
        "vignette",
        "parallax",
        "perspective",
      ];

  const fxSettingsPanelOrder = [
    "vignette",
    "rgbsplit",
    "shademap",
    "rgbgrid",
    "scanlines",
    "parallax",
    "perspective",
  ];

  const groupGlitchSettings = {
    preset: "off",
    activePresets: [],
    presets: cloneData(groupFxPresetDefaults),
  };

  const logoClickSettings = {
    waveSpread: 1.35,
    waveCount: 1,
    waveGap: 110,
    waveHold: 58,
    waveOpacity: 1,
    tailHold: 90,
    tailOpacity: 1,
    pressX: 4,
    pressY: 4,
    glow: 0.58,
    hoverGlowOpacity: 0.55,
    hoverGlowBlur: 14,
    hoverGlowCore: 3,
    hoverGlowColor: "#62636a",
    waveColor: "#62636a",
    tailColor: "#62636a",
    splitAmount: 1.2,
    splitOpacity: 0.7,
    splitRed: "#ff281e",
    splitBlue: "#00dcff",
  };

  const installFrames = [
    copy.cliInstalled,
    `${copy.cliInstalled}\n${copy.cliInstalledNext}`,
  ];

  const promptCopy = copy.prompt;

  const finalState = {
    done: `✓ ${copy.finalDone}`,
    url: "https://sales-overview.databricksapps.com",
  };

  const FINAL_CHECK_SEQUENCE_DURATION = 760;
  const TIMELINE_STATE_COUNT = 6;
  const STAGE_BASE_WIDTH = 2300;
  const STAGE_BASE_HEIGHT = 1144;

  const settings = {
    loopRestartDelay: 0,
    timelineTrimStart: 1,
    timelineTrimEnd: 6,
    timelineLoop: false,
    paused: false,
  };

  let runId = 0;
  let currentState = 1;
  let timelineRunning = false;
  let logoRunId = 0;
  let windowBootRunId = 0;
  let windowBootTimers = [];
  let finalAsciiRunId = 0;
  let logoWaveInstanceId = 0;
  const logoWaveRects = new Set();
  const logoRectWaveStates = new WeakMap();
  let parallaxTargetX = 0;
  let parallaxTargetY = 0;
  let parallaxCurrentX = 0;
  let parallaxCurrentY = 0;
  let parallaxWasActive = false;
  let parallaxLastPointerAt = 0;
  let perspectiveTargetX = 0;
  let perspectiveTargetY = 0;
  let perspectiveCurrentX = 0;
  let perspectiveCurrentY = 0;
  let perspectiveWasActive = false;
  let perspectiveLastPointerAt = 0;
  let perspectiveFrameRequest = 0;
  let perspectiveHasPointerTarget = false;
  let draggingFxPreset = "";
  let lastRenderTickAt = 0;
  let renderFrameTimer = 0;
  let viewportPaused = false;
  let intersectionPaused = false;
  let documentVisibilityPaused = false;
  let renderIdleSettled = false;
  let quietLastInteractionAt = performance.now();
  const IDLE_SETTLE_HOLD_MS = 1800;
  let lastGridRenderKey = "";
  let lastShadeMapRenderKey = "";
  let gridWasVisible = false;
  let gridCanvasDirty = true;
  const rawWait = (duration) =>
    new Promise((resolve) => window.setTimeout(resolve, duration));

  function hasActiveAnimationWork() {
    return (
      timelineRunning ||
      !settings.paused ||
      stage.classList.contains("power-on-active") ||
      stage.classList.contains("terminal-scroll") ||
      appPreview.classList.contains("app-boot-running") ||
      appPreview.classList.contains("app-reveal-running")
    );
  }

  function shouldSettleIdlePreview(now = performance.now()) {
    if (document.hidden || hasActiveAnimationWork()) {
      return false;
    }

    return now - quietLastInteractionAt >= IDLE_SETTLE_HOLD_MS;
  }

  function setRenderIdleSettled(settled) {
    if (renderIdleSettled === settled) {
      return;
    }

    renderIdleSettled = settled;
    stage.classList.toggle("is-idle-settled", settled);
    stage.classList.toggle(
      "is-render-paused",
      !isExportPlayer && (document.hidden || settled),
    );

    if (settled) {
      lastRenderTickAt = 0;
      parallaxLastPointerAt = performance.now();
      perspectiveLastPointerAt = performance.now();
    }
  }

  const STAGE_ZOOM_MIN = 0.2;
  const STAGE_ZOOM_MAX = 1.25;

  function clampStageZoom(value) {
    return Math.min(
      STAGE_ZOOM_MAX,
      Math.max(STAGE_ZOOM_MIN, Number(value) || 1),
    );
  }

  function getFitStageZoom() {
    const rect = toolWorkspace?.getBoundingClientRect();
    const width = Math.max(260, rect?.width || window.innerWidth);
    const height = Math.max(220, rect?.height || window.innerHeight);
    return clampStageZoom(
      Math.min(1, width / STAGE_BASE_WIDTH, height / STAGE_BASE_HEIGHT),
    );
  }

  function applyStageFit() {
    const zoom = getFitStageZoom();
    root.style.setProperty("--stage-zoom", zoom.toFixed(4));
    root.style.setProperty("--stage-zoom-inverse", (1 / zoom).toFixed(4));

    if (stageViewport) {
      stageViewport.style.width = `${Math.ceil(STAGE_BASE_WIDTH * zoom)}px`;
      stageViewport.style.height = `${Math.ceil(STAGE_BASE_HEIGHT * zoom)}px`;
    }
  }

  function cloneData(value) {
    return JSON.parse(JSON.stringify(value));
  }

  function syncAppRevealSettingsPair(sourceState = 5) {
    const source =
      stateAppRevealSettings[sourceState] ||
      stateAppRevealSettings[5] ||
      stateAppRevealSettings[6];
    if (!source) {
      return;
    }

    const targetState = Number(sourceState) === 6 ? 5 : 6;
    stateAppRevealSettings[targetState] = cloneData(source);
  }

  function normalizeGroupFxSettings() {
    groupGlitchSettings.presets = groupGlitchSettings.presets || {};
    groupGlitchSettings.activePresets = Array.isArray(
      groupGlitchSettings.activePresets,
    )
      ? groupGlitchSettings.activePresets.filter(
          (preset) => preset !== "off" && groupFxPresetDefaults[preset],
        )
      : [];
    Object.entries(groupFxPresetDefaults).forEach(([preset, defaults]) => {
      groupGlitchSettings.presets[preset] = {
        ...defaults,
        ...(groupGlitchSettings.presets[preset] || {}),
      };
    });

    if (!groupFxPresetDefaults[groupGlitchSettings.preset]) {
      groupGlitchSettings.preset =
        groupGlitchSettings.activePresets[0] || "off";
    }
  }

  function getGroupFxConfig(preset = groupGlitchSettings.preset) {
    normalizeGroupFxSettings();
    return groupGlitchSettings.presets[preset] || {};
  }

  function isGroupFxActive(preset) {
    normalizeGroupFxSettings();
    return groupGlitchSettings.activePresets.includes(preset);
  }

  function getRgbSplitTargetIntensity(config, key) {
    if (Object.prototype.hasOwnProperty.call(config || {}, key)) {
      return Math.max(0, Number(config[key] || 0));
    }

    return key === "windowIntensity"
      ? 0
      : Math.max(0, Number(config?.intensity || 0));
  }

  function getRgbSplitMaxIntensity(config) {
    return Math.max(
      getRgbSplitTargetIntensity(config, "logoIntensity"),
      getRgbSplitTargetIntensity(config, "textIntensity"),
      getRgbSplitTargetIntensity(config, "windowIntensity"),
    );
  }

  function getPrimaryVisualFxPreset() {
    normalizeGroupFxSettings();
    const visualPresets = groupGlitchSettings.activePresets.filter(
      (preset) =>
        ![
          "rgbgrid",
          "scanlines",
          "parallax",
          "vignette",
          "perspective",
        ].includes(preset),
    );
    return visualPresets[0] || "off";
  }

  function applyConfig(config) {
    if (!config || config.version !== 1) {
      return;
    }

    Object.assign(settings, config.settings || {});
    settings.loopRestartDelay = getLoopRestartDelay();
    Object.assign(logoClickSettings, config.logoClickSettings || {});
    sanitizeLogoClickSettings();

    if (config.stateAnimationSettings) {
      Object.entries(config.stateAnimationSettings).forEach(
        ([state, animationSettings]) => {
          if (stateAnimationSettings[state]) {
            Object.assign(stateAnimationSettings[state], animationSettings);
            if (
              Number(state) === 4 &&
              animationSettings.terminalScroll === false
            ) {
              stateAnimationSettings[state].terminalScroll = true;
            }
            if (
              Number(state) === 1 &&
              !Object.prototype.hasOwnProperty.call(
                animationSettings,
                "powerOnLineDuration",
              ) &&
              Number.isFinite(Number(animationSettings.powerOnDuration))
            ) {
              stateAnimationSettings[state].powerOnLineDuration = Math.max(
                40,
                Number(animationSettings.powerOnDuration || 0) -
                  Number(animationSettings.powerOnLineHold || 0),
              );
            }
            if (
              Number(state) === 1 &&
              !Object.prototype.hasOwnProperty.call(
                animationSettings,
                "powerOnLineExpandDuration",
              )
            ) {
              stateAnimationSettings[state].powerOnLineExpandDuration =
                Math.max(
                  40,
                  Math.round(
                    Number(
                      stateAnimationSettings[state].powerOnLineDuration || 920,
                    ) * 0.44,
                  ),
                );
            }
            if (
              Number(state) === 1 &&
              !Object.prototype.hasOwnProperty.call(
                animationSettings,
                "powerOnYScaleDuration",
              )
            ) {
              stateAnimationSettings[state].powerOnYScaleDuration = Math.max(
                40,
                Math.round(
                  Number(
                    stateAnimationSettings[state].powerOnLineDuration || 920,
                  ) * 0.3,
                ),
              );
            }
          }
        },
      );
    } else {
      migrateLegacyAnimationSettings(config.settings || {});
    }

    if (stateAnimationSettings[2].delayAfter === 620) {
      stateAnimationSettings[2].delayAfter =
        defaultStateAnimationSettings[2].delayAfter;
    }

    if (
      config.scrollTuningVersion !== 1 &&
      Number(stateAnimationSettings[4]?.terminalScrollDistance) === 392
    ) {
      stateAnimationSettings[4].terminalScrollDistance =
        defaultStateAnimationSettings[4].terminalScrollDistance;
    }

    if (stateLogoSettings[1].revealDelay === 260) {
      stateLogoSettings[1].revealDelay = 0;
    }

    Object.entries(config.stateGridSettings || {}).forEach(
      ([state, gridSettings]) => {
        if (stateGridSettings[state]) {
          Object.assign(stateGridSettings[state], gridSettings);
        }
      },
    );

    Object.entries(config.stateAppRevealSettings || {}).forEach(
      ([state, revealSettings]) => {
        if (stateAppRevealSettings[state]) {
          Object.assign(stateAppRevealSettings[state], revealSettings);
        }
      },
    );
    if (
      config.stateAppRevealSettings?.[6] &&
      !config.stateAppRevealSettings?.[5]
    ) {
      Object.assign(
        stateAppRevealSettings[5],
        config.stateAppRevealSettings[6],
      );
      Object.assign(
        stateAppRevealSettings[6],
        config.stateAppRevealSettings[6],
      );
    }
    if (config.colorRevealVersion !== 2) {
      const revealSettings = stateAppRevealSettings[5];
      if (
        revealSettings?.preset === "none" &&
        Number(revealSettings.intensity || 0) === 0 &&
        Number(revealSettings.duration || 0) === 0
      ) {
        Object.assign(revealSettings, {
          preset: "colorWave",
          intensity: 6,
          duration: 980,
          grid: 14,
          corner: "tl",
          originX: 0,
          originY: 0,
          layers: 3,
        });
      }
    }
    setAppRevealOriginTopLeft(stateAppRevealSettings[5]);
    setAppRevealOriginTopLeft(stateAppRevealSettings[6]);
    syncAppRevealSettingsPair(5);
    normalizeWindowBootSettings();
    normalizeBuildStatusSettings();
    normalizeTerminalScrollSettings();

    if (config.sharedTextLayers) {
      Object.entries(config.sharedTextLayers).forEach(([layerKey, layer]) => {
        if (sharedTextLayers[layerKey]) {
          Object.assign(sharedTextLayers[layerKey], layer);
        }
      });
    } else if (config.stateTextLayers) {
      const preferredState = String(config.currentState || currentState);
      Object.keys(sharedTextLayers).forEach((layerKey) => {
        const preferredLayer =
          config.stateTextLayers[preferredState]?.[layerKey];
        const fallbackLayer = Object.values(config.stateTextLayers).find(
          (layers) => layers?.[layerKey],
        )?.[layerKey];
        if (preferredLayer || fallbackLayer) {
          Object.assign(
            sharedTextLayers[layerKey],
            preferredLayer || fallbackLayer,
          );
        }
      });
    }

    if (config.typographyVersion !== 2) {
      Object.entries(defaultTextLayers).forEach(([layerKey, defaults]) => {
        if (sharedTextLayers[layerKey]) {
          sharedTextLayers[layerKey].size = defaults.size;
          sharedTextLayers[layerKey].letterSpacing = defaults.letterSpacing;
        }
      });
    }

    if (config.sharedAppSettings) {
      Object.assign(sharedAppSettings, config.sharedAppSettings);
    } else if (config.stateAppSettings) {
      const preferredAppSettings =
        config.stateAppSettings[String(config.currentState || currentState)] ||
        config.stateAppSettings[6] ||
        config.stateAppSettings[5];
      if (preferredAppSettings) {
        Object.assign(sharedAppSettings, preferredAppSettings);
      }
    }

    if (config.staticAppSettings) {
      Object.assign(staticAppSettings, config.staticAppSettings);
      deprecatedStaticAppSettingKeys.forEach((key) => {
        delete staticAppSettings[key];
      });
    }

    if (config.preAppFocusSettings) {
      Object.assign(preAppFocusSettings, config.preAppFocusSettings);
    }
    if (config.dragEchoSettings) {
      Object.assign(dragEchoSettings, config.dragEchoSettings);
    }

    Object.entries(config.stateLogoSettings || {}).forEach(
      ([state, logoSettings]) => {
        if (stateLogoSettings[state]) {
          Object.assign(stateLogoSettings[state], logoSettings);
        }
      },
    );
    if (Number(stateLogoSettings[1]?.revealDelay) === 260) {
      stateLogoSettings[1].revealDelay = 0;
    }

    if (config.groupGlitchSettings) {
      if (config.groupGlitchSettings.presets) {
        groupGlitchSettings.preset =
          config.groupGlitchSettings.preset || groupGlitchSettings.preset;
        groupGlitchSettings.activePresets =
          config.groupGlitchSettings.activePresets ||
          groupGlitchSettings.activePresets;
        Object.entries(config.groupGlitchSettings.presets).forEach(
          ([preset, presetSettings]) => {
            if (groupFxPresetDefaults[preset]) {
              Object.assign(
                groupGlitchSettings.presets[preset],
                presetSettings,
              );
            }
          },
        );
      } else {
        groupGlitchSettings.preset =
          config.groupGlitchSettings.preset || groupGlitchSettings.preset;
        if (groupGlitchSettings.preset !== "off") {
          groupGlitchSettings.activePresets = [groupGlitchSettings.preset];
        }
        Object.assign(getGroupFxConfig(groupGlitchSettings.preset), {
          intensity: config.groupGlitchSettings.intensity,
          duration: config.groupGlitchSettings.duration,
          loop: config.groupGlitchSettings.loop,
        });
      }
    } else if (config.stateGlitchSettings) {
      const legacyState =
        config.stateGlitchSettings[config.currentState] ||
        config.stateGlitchSettings[1];
      if (legacyState) {
        groupGlitchSettings.preset =
          legacyState.preset || groupGlitchSettings.preset;
        if (groupGlitchSettings.preset !== "off") {
          groupGlitchSettings.activePresets = [groupGlitchSettings.preset];
        }
        Object.assign(getGroupFxConfig(groupGlitchSettings.preset), {
          intensity: legacyState.intensity,
          duration: legacyState.duration,
          loop: legacyState.loop,
        });
      }
    }
    normalizeGroupFxSettings();
  }

  function getLoopRestartDelay() {
    return clamp(Number(settings.loopRestartDelay ?? 0), 0, 60000);
  }

  function getAnimationSettings(state = currentState) {
    return stateAnimationSettings[state] || stateAnimationSettings[1];
  }

  function getAppRevealSettings(state = currentState) {
    if (Number(state) === 5 || Number(state) === 6) {
      return stateAppRevealSettings[5];
    }

    return stateAppRevealSettings[state] || stateAppRevealSettings[5];
  }

  function getTypingTokens(text, mode = "letters") {
    if (mode !== "words") {
      return [...text];
    }

    return text.match(/\n|[^\s\n]+[\t ]*/g) || [];
  }

  function seededUnit(index, salt = 0) {
    const raw = Math.sin((index + 1) * 12.9898 + salt * 78.233) * 43758.5453;
    return raw - Math.floor(raw);
  }

  function getHumanTypingDelay(
    text,
    tokenText,
    tokenIndex,
    textOffset,
    charDelay,
    lineDelay,
    mode = "letters",
    options = {},
  ) {
    const contrast = Math.max(0, Number(options.contrast ?? 1));
    if (tokenText === "\n") {
      const lineBase = Math.max(0, Number(lineDelay) || 0);
      const linePause = lineBase * (0.72 + seededUnit(tokenIndex, 31) * 0.58);
      return lineBase + (linePause - lineBase) * contrast;
    }

    const baseDelay = Math.max(0, Number(charDelay) || 0);
    if (baseDelay <= 0) {
      return 0;
    }

    const tokenLength = [...tokenText].length;
    const textBefore = text.slice(0, textOffset);
    const previousCharacter = textBefore.at(-1) || "";
    const nextCharacter = text[textOffset + tokenText.length] || "";
    const finalCharacter = tokenText.trimEnd().at(-1) || tokenText.at(-1) || "";
    const isWhitespace = /^\s+$/.test(tokenText);
    const lengthFactor = mode === "words" ? Math.max(1, tokenLength * 0.5) : 1;
    const isWordCharacter = /[\p{L}\p{N}]/u.test(finalCharacter);
    const inWordRun =
      isWordCharacter &&
      /[\p{L}\p{N}]/u.test(previousCharacter) &&
      /[\p{L}\p{N}]/u.test(nextCharacter);
    const burstPosition = tokenIndex % 7;
    let delay =
      baseDelay * lengthFactor * (0.62 + seededUnit(tokenIndex, 1) * 0.82);

    if (
      mode === "letters" &&
      inWordRun &&
      burstPosition > 0 &&
      burstPosition < 5
    ) {
      delay *= 0.48 + seededUnit(tokenIndex, 8) * 0.24;
    }

    if (
      mode === "letters" &&
      isWordCharacter &&
      (burstPosition === 0 || burstPosition === 5)
    ) {
      delay += baseDelay * (0.36 + seededUnit(tokenIndex, 9) * 0.72);
    }

    if (isWhitespace || /\s$/.test(tokenText)) {
      delay += baseDelay * (1.1 + seededUnit(tokenIndex, 2) * 2.4);
    }

    if (/[,.!?;:]/.test(finalCharacter)) {
      delay += baseDelay * (4.2 + seededUnit(tokenIndex, 3) * 5.8);
    }

    if (/[,.!?;:]/.test(previousCharacter)) {
      delay += baseDelay * (1.1 + seededUnit(tokenIndex, 4) * 2.2);
    }

    if (tokenIndex > 0 && tokenIndex % 19 === 0) {
      delay += baseDelay * (2.1 + seededUnit(tokenIndex, 5) * 3.4);
    }

    if (seededUnit(tokenIndex, 6) > 0.92) {
      delay += baseDelay * (2.8 + seededUnit(tokenIndex, 7) * 6.8);
    }

    const contrastedDelay = baseDelay + (delay - baseDelay) * contrast;
    return Math.max(
      6,
      Math.min(contrastedDelay, baseDelay * (9 + contrast * 9) + 420),
    );
  }

  function getTypingDelay(
    text,
    tokenText,
    tokenIndex,
    textOffset,
    charDelay,
    lineDelay,
    mode = "letters",
    options = {},
  ) {
    if (options.human) {
      return getHumanTypingDelay(
        text,
        tokenText,
        tokenIndex,
        textOffset,
        charDelay,
        lineDelay,
        mode,
        options,
      );
    }

    return tokenText === "\n" ? lineDelay : charDelay;
  }

  function getTypingDuration(
    text,
    charDelay,
    lineDelay,
    mode = "letters",
    startDelay = 0,
    options = {},
  ) {
    let textOffset = 0;
    return getTypingTokens(text, mode).reduce((duration, token, index) => {
      const nextDuration =
        duration +
        getTypingDelay(
          text,
          token,
          index,
          textOffset,
          charDelay,
          lineDelay,
          mode,
          options,
        );
      textOffset += token.length;
      return nextDuration;
    }, startDelay);
  }

  function getInstallTypingDuration() {
    const stateSettings = getAnimationSettings(1);
    let previousText = "";
    let duration = stateSettings.installTextDelay || 0;

    installFrames.forEach((frame) => {
      const nextText = frame.startsWith(previousText)
        ? frame.slice(previousText.length)
        : frame;
      duration += getTypingDuration(
        nextText,
        stateSettings.installType,
        stateSettings.installFramePause,
      );
      duration += stateSettings.installFramePause;
      previousText = frame;
    });

    return duration;
  }

  function getPowerOnDuration(stateSettings = getAnimationSettings(1)) {
    if (!stateSettings.powerOnEnabled) {
      return 0;
    }

    const powerDuration = Math.max(
      0,
      Number(stateSettings.powerOnDuration || 0),
    );
    if (powerDuration <= 0) {
      return 0;
    }

    const lineHold = getPowerOnLineHold(stateSettings, powerDuration);
    const activeDuration = getPowerOnActiveDuration(
      stateSettings,
      powerDuration,
      lineHold,
    );
    const lineDuration = getPowerOnLineDuration(stateSettings, activeDuration);
    return lineHold + Math.max(activeDuration, lineDuration);
  }

  function getPowerOnLineHold(
    stateSettings = getAnimationSettings(1),
    powerDuration = Math.max(0, Number(stateSettings.powerOnDuration || 0)),
  ) {
    return Math.min(
      Math.max(0, Number(stateSettings.powerOnLineHold || 0)),
      Math.max(0, powerDuration - 40),
    );
  }

  function getPowerOnActiveDuration(
    stateSettings = getAnimationSettings(1),
    powerDuration = Math.max(0, Number(stateSettings.powerOnDuration || 0)),
    lineHold = getPowerOnLineHold(stateSettings, powerDuration),
  ) {
    return Math.max(40, powerDuration - lineHold);
  }

  function getPowerOnLineDuration(
    stateSettings = getAnimationSettings(1),
    activeDuration = getPowerOnActiveDuration(stateSettings),
  ) {
    const duration = Number(stateSettings.powerOnLineDuration);
    return Number.isFinite(duration) && duration > 0
      ? Math.max(0, duration)
      : activeDuration;
  }

  function getPowerOnLineExpandDuration(
    stateSettings = getAnimationSettings(1),
    lineDuration = getPowerOnLineDuration(stateSettings),
  ) {
    const duration = Number(stateSettings.powerOnLineExpandDuration);
    return Number.isFinite(duration) && duration > 0
      ? Math.max(40, duration)
      : Math.max(40, lineDuration * 0.44);
  }

  function getPowerOnYScaleDuration(
    stateSettings = getAnimationSettings(1),
    lineDuration = getPowerOnLineDuration(stateSettings),
  ) {
    const duration = Number(stateSettings.powerOnYScaleDuration);
    return Number.isFinite(duration) && duration > 0
      ? Math.max(40, duration)
      : Math.max(40, lineDuration * 0.3);
  }

  function setPowerOnVariables(
    stateSettings,
    activeDuration,
    lineDuration = activeDuration,
  ) {
    const playbackSpeed = Math.max(0.1, stateSettings.speed || 1);
    const bloom = Math.max(0, Number(stateSettings.powerOnBloom || 0));
    const warmup = Math.max(0, Number(stateSettings.powerOnWarmup || 0));
    const brightness = Math.max(
      0,
      Number(stateSettings.powerOnBrightness ?? 0.85),
    );
    const stretchBrightness = Math.max(
      0,
      Number(stateSettings.powerOnStretchBrightness ?? brightness),
    );
    const lineExpandDuration = getPowerOnLineExpandDuration(
      stateSettings,
      lineDuration,
    );
    const yScaleDuration = getPowerOnYScaleDuration(
      stateSettings,
      lineDuration,
    );
    const powerY = Math.max(
      0,
      Math.min(100, Number(stateSettings.powerOnY ?? 42)),
    );
    const yStretch = Math.max(1, Number(stateSettings.powerOnYStretch ?? 520));
    const legacySceneYScale = 1 + Math.min(0.32, yStretch / 2800);
    const sceneStartYValue = Number(stateSettings.powerOnSceneStartY ?? 0);
    const sceneStartSquashValue = Number(
      stateSettings.powerOnSceneStartSquash ?? 0.04,
    );
    const sceneYScaleValue = Number(
      stateSettings.powerOnSceneYScale ?? legacySceneYScale,
    );
    const sceneStartY = Number.isFinite(sceneStartYValue)
      ? sceneStartYValue
      : 0;
    const sceneStartSquash = Number.isFinite(sceneStartSquashValue)
      ? Math.max(0.01, sceneStartSquashValue)
      : 0.04;
    const sceneYScale = Number.isFinite(sceneYScaleValue)
      ? Math.max(0.1, sceneYScaleValue)
      : legacySceneYScale;
    const chroma = colorSaturation(stateSettings.powerOnColor, "#8a8fa3");
    setRootVar(
      "--power-on-duration",
      `${Math.max(40, activeDuration) / playbackSpeed}ms`,
    );
    setRootVar(
      "--power-on-line-duration",
      `${Math.max(40, lineDuration) / playbackSpeed}ms`,
    );
    setRootVar(
      "--power-on-line-expand-duration",
      `${Math.max(40, lineExpandDuration) / playbackSpeed}ms`,
    );
    setRootVar(
      "--power-on-y-scale-duration",
      `${Math.max(40, yScaleDuration) / playbackSpeed}ms`,
    );
    setRootVar("--power-on-bloom", bloom.toFixed(3));
    setRootVar("--power-on-bloom-radius", "18px");
    setRootVar("--power-on-brightness", brightness.toFixed(3));
    setRootVar("--power-on-stretch-brightness", stretchBrightness.toFixed(3));
    setRootVar(
      "--power-on-rgb",
      rgbTupleFromColor(stateSettings.powerOnColor, "#8a8fa3"),
    );
    setRootVar("--power-on-y", `${powerY.toFixed(1)}%`);
    setRootVar("--power-on-y-stretch", yStretch.toFixed(0));
    setRootVar("--power-on-scene-y-scale", sceneYScale.toFixed(3));
    setRootVar("--power-on-scene-start-y", `${sceneStartY.toFixed(1)}px`);
    setRootVar(
      "--power-on-scene-mid-y",
      `${(sceneStartY * 0.48).toFixed(1)}px`,
    );
    setRootVar(
      "--power-on-scene-overshoot-y",
      `${(sceneStartY * -0.16).toFixed(1)}px`,
    );
    setRootVar(
      "--power-on-scene-settle-y",
      `${(sceneStartY * 0.08).toFixed(1)}px`,
    );
    setRootVar("--power-on-scene-start-squash", sceneStartSquash.toFixed(3));
    setRootVar(
      "--power-on-core-opacity",
      Math.min(1, 0.92 * brightness).toFixed(3),
    );
    setRootVar(
      "--power-on-accent-opacity",
      Math.min(0.48, (0.12 + chroma * 0.3) * stretchBrightness).toFixed(3),
    );
    setRootVar(
      "--power-on-edge-opacity",
      Math.min(0.32, (0.08 + chroma * 0.16) * stretchBrightness).toFixed(3),
    );
    setRootVar(
      "--power-on-haze-core-opacity",
      Math.min(0.34, 0.16 * stretchBrightness).toFixed(3),
    );
    setRootVar(
      "--power-on-haze-soft-opacity",
      Math.min(0.16, 0.06 * stretchBrightness).toFixed(3),
    );
    setRootVar(
      "--power-on-haze-edge-opacity",
      Math.min(0.08, 0.03 * stretchBrightness).toFixed(3),
    );
    setRootVar(
      "--power-on-bloom-opacity",
      Math.min(1, (0.16 + bloom * 0.76) * stretchBrightness).toFixed(3),
    );
    setRootVar(
      "--power-on-haze-opacity",
      Math.min(
        0.6,
        (0.08 + bloom * 0.3) * Math.max(0.25, stretchBrightness),
      ).toFixed(3),
    );
    setRootVar(
      "--power-on-line-stretch-opacity",
      Math.min(0.72, 0.38 * stretchBrightness).toFixed(3),
    );
    setRootVar(
      "--power-on-line-end-opacity",
      Math.min(0.34, 0.14 * stretchBrightness).toFixed(3),
    );
    setRootVar(
      "--power-on-scale",
      Number(stateSettings.powerOnScale || 0).toFixed(3),
    );
    setRootVar(
      "--power-on-jitter",
      `${Number(stateSettings.powerOnJitter || 0).toFixed(2)}px`,
    );
    setRootVar("--power-on-warmup", warmup.toFixed(3));
    setRootVar("--power-on-warmup-blur", "0px");
    setRootVar("--power-on-warmup-blur-mid", "0px");
    setRootVar(
      "--power-on-warmup-brightness",
      ((0.72 + warmup * 0.52) * Math.max(0.22, brightness)).toFixed(3),
    );
    setRootVar(
      "--logo-tube-warmup-duration",
      `${Math.max(0, Number(stateSettings.logoTubeWarmupDuration ?? 1400)) / playbackSpeed}ms`,
    );
    setRootVar(
      "--logo-tube-start-y",
      `${Number(stateSettings.logoTubeStartY ?? 32).toFixed(1)}px`,
    );
    setRootVar(
      "--logo-tube-start-squash",
      Math.max(0.01, Number(stateSettings.logoTubeStartSquash ?? 0.04)).toFixed(
        3,
      ),
    );
    setRootVar(
      "--logo-tube-start-width",
      Math.max(0.01, Number(stateSettings.logoTubeStartWidth ?? 0.62)).toFixed(
        3,
      ),
    );
    setRootVar(
      "--logo-tube-start-opacity",
      Math.max(
        0,
        Math.min(1, Number(stateSettings.logoTubeStartOpacity ?? 0)),
      ).toFixed(3),
    );
    setRootVar(
      "--logo-tube-depth",
      `${Math.max(0, Number(stateSettings.logoTubeWarmupDepth ?? 320)).toFixed(1)}px`,
    );
    setRootVar(
      "--logo-tube-jump",
      `${Math.max(0, Number(stateSettings.logoTubeWarmupJump ?? 58)).toFixed(1)}px`,
    );
    setRootVar(
      "--logo-tube-blur",
      `${Math.max(0, Number(stateSettings.logoTubeWarmupBlur ?? 11)).toFixed(1)}px`,
    );
  }

  function syncPowerOnVariables(stateSettings = getAnimationSettings(1)) {
    const powerDuration = Math.max(
      0,
      Number(stateSettings.powerOnDuration || 0),
    );
    const lineHold = getPowerOnLineHold(stateSettings, powerDuration);
    const activeDuration = getPowerOnActiveDuration(
      stateSettings,
      powerDuration,
      lineHold,
    );
    const lineDuration = getPowerOnLineDuration(stateSettings, activeDuration);
    setPowerOnVariables(stateSettings, activeDuration, lineDuration);
  }

  function getPromptEnterDuration(stateSettings) {
    return stateSettings.promptEnterMoment ? 280 : 0;
  }

  function getPromptStartDelay(stateSettings = getAnimationSettings(2)) {
    return Math.max(0, Number(stateSettings.promptStartDelay || 0));
  }

  function syncPromptCursorTiming(stateSettings = getAnimationSettings(2)) {
    const blinkDuration = Math.max(
      0,
      Number(stateSettings.promptCursorBlink || 0),
    );
    const cursorColor = stateSettings.promptEnterColor || "#8a8fa3";
    const flashColor = stateSettings.promptEnterFlashColor || "#ff6049";
    promptLayer.style.setProperty(
      "--prompt-cursor-blink-duration",
      `${Math.max(80, blinkDuration || 0)}ms`,
    );
    promptLayer.style.setProperty("--prompt-cursor-color", cursorColor);
    promptLayer.style.setProperty(
      "--prompt-cursor-rgb",
      rgbTupleFromColor(cursorColor, "#8a8fa3"),
    );
    promptLayer.style.setProperty("--prompt-enter-flash-color", flashColor);
    promptLayer.style.setProperty(
      "--prompt-enter-flash-rgb",
      rgbTupleFromColor(flashColor, "#ff6049"),
    );
    setRootVar("--text-glitch-red-color", flashColor);
    promptLayer.classList.toggle("prompt-cursor-static", blinkDuration <= 0);
  }

  function syncBuildStatusTuning(stateSettings = getAnimationSettings(4)) {
    const leaderGap = Number(stateSettings.statusLeaderGap ?? 44);
    const leaderX = Number(stateSettings.statusLeaderX ?? 0);
    const leaderY = Number(stateSettings.statusLeaderY ?? 3);
    const safeLeaderX = Number.isFinite(leaderX) ? leaderX : 0;
    const safeLeaderY = Number.isFinite(leaderY) ? leaderY : 3;
    const target = buildStatus || root;
    target.style.setProperty(
      "--status-leader-status-gap",
      `${Number.isFinite(leaderGap) ? leaderGap : 44}px`,
    );
    target.style.setProperty("--status-leader-x", `${safeLeaderX}px`);
    target.style.setProperty("--status-leader-y", `${safeLeaderY}px`);
    setRootVar("--final-check-burst-x", `${safeLeaderX}px`);
    setRootVar("--final-check-burst-y", `${safeLeaderY}px`);
  }

  const prototypeBuildSelectors = [];

  function getWindowBootDuration(stateSettings = getAnimationSettings(5)) {
    if (!stateSettings.windowBootEnabled) {
      return 0;
    }

    return Math.max(0, Number(stateSettings.windowBootDuration || 0));
  }

  function getWindowBootExpansionDuration(
    stateSettings = getAnimationSettings(5),
  ) {
    if (!stateSettings.windowBootEnabled) {
      return 0;
    }

    const playbackSpeed = Math.max(0.1, stateSettings.speed || 1);
    const duration =
      Math.max(0, Number(stateSettings.windowBootDuration || 0)) /
      playbackSpeed;
    const holdDuration = Math.min(
      duration * 0.45,
      Math.max(0, Number(stateSettings.windowBootWindowHold || 0)) /
        playbackSpeed,
    );
    return Math.max(
      140,
      Math.min(duration * 0.48, Math.max(140, duration - holdDuration - 180)),
    );
  }

  function getWindowBootFrameReadyDelay(
    stateSettings = getAnimationSettings(5),
  ) {
    const expansionDuration = getWindowBootExpansionDuration(stateSettings);
    if (expansionDuration <= 0) {
      return 0;
    }

    const iterations = Math.max(
      2,
      Math.round(Number(stateSettings.windowBootTraceIterations || 7)),
    );
    const frameGap = expansionDuration / Math.max(1, iterations);
    return expansionDuration + Math.max(40, frameGap * 0.9);
  }

  const WINDOW_BOOT_TEMP_NODE_SELECTOR =
    ".app-boot-square, .app-boot-trace, .app-boot-echo, .app-boot-block, .app-boot-scaffold";
  const WINDOW_BOOT_NODE_SELECTOR = `${WINDOW_BOOT_TEMP_NODE_SELECTOR}, .app-boot-full-frame`;

  function clearWindowBoot({
    cancelTimers = true,
    preserveFrame = false,
  } = {}) {
    if (cancelTimers) {
      windowBootRunId += 1;
    }

    windowBootTimers.forEach((timer) => window.clearTimeout(timer));
    windowBootTimers = [];
    appPreview.classList.remove(
      "app-boot-running",
      "app-boot-finalizing",
      "app-boot-scaffolding",
      "app-boot-final-frame-ready",
    );
    if (!preserveFrame) {
      appPreview.classList.remove("app-boot-clip-enabled");
    }
    [
      "--app-boot-duration",
      "--app-boot-x",
      "--app-boot-y",
      "--app-boot-size",
      "--app-boot-steps",
      "--app-boot-grow-1-size",
      "--app-boot-grow-2-size",
      "--app-boot-frame-x",
      "--app-boot-frame-y",
      "--app-boot-frame-width",
      "--app-boot-frame-height",
      "--app-boot-frame-mid-x",
      "--app-boot-frame-mid-y",
      "--app-boot-frame-mid-width",
      "--app-boot-frame-mid-height",
      "--app-boot-frame-near-x",
      "--app-boot-frame-near-y",
      "--app-boot-frame-near-width",
      "--app-boot-frame-near-height",
      "--app-boot-echo-spread",
      "--app-boot-echo-opacity",
      "--app-boot-echo-delay",
      "--app-boot-ascii-duration",
    ].forEach((property) => appPreview.style.removeProperty(property));
    appRevealOverlay
      .querySelectorAll(
        preserveFrame
          ? WINDOW_BOOT_TEMP_NODE_SELECTOR
          : WINDOW_BOOT_NODE_SELECTOR,
      )
      .forEach((node) => node.remove());
  }

  function getWindowBootTargetRect() {
    const previewWidth =
      appPreview.offsetWidth ||
      Number.parseFloat(getComputedStyle(appPreview).width) ||
      1;
    const previewHeight =
      appPreview.offsetHeight ||
      Number.parseFloat(getComputedStyle(appPreview).height) ||
      1;
    const configuredWidth = Math.max(
      1,
      Number(staticAppSettings.windowWidth || previewWidth),
    );
    const configuredHeight = Math.max(
      1,
      Number(staticAppSettings.windowHeight || previewHeight),
    );
    const sourceImage = appDoneImage?.naturalWidth
      ? appDoneImage
      : appPrototypeImage;
    const imageCssWidth = sourceImage?.naturalWidth
      ? sourceImage.naturalWidth / 2
      : configuredWidth;
    const imageCssHeight = sourceImage?.naturalHeight
      ? sourceImage.naturalHeight / 2
      : configuredHeight;
    const width = Math.min(previewWidth, configuredWidth, imageCssWidth);
    const height = Math.min(previewHeight, configuredHeight, imageCssHeight);
    const left = 0;
    const top = 0;

    return { left, top, width, height };
  }

  function getWindowBootStartRect(
    stateSettings,
    startSize,
    targetRect = getWindowBootTargetRect(),
  ) {
    const centerX =
      stateSettings.windowBootOrigin === "center"
        ? targetRect.left + targetRect.width / 2
        : Number(stateSettings.windowBootX || 0);
    const centerY =
      stateSettings.windowBootOrigin === "center"
        ? targetRect.top + targetRect.height / 2
        : Number(stateSettings.windowBootY || 0);

    return {
      left: centerX - startSize / 2,
      top: centerY - startSize / 2,
      centerX,
      centerY,
    };
  }

  function getWindowBootFrameRect(stateSettings, frame, frameCount) {
    const targetRect = getWindowBootTargetRect();
    const finalWidth = targetRect.width;
    const finalHeight = targetRect.height;
    const startSize = Math.max(1, Number(stateSettings.windowBootSize || 10));
    const startRect = getWindowBootStartRect(
      stateSettings,
      startSize,
      targetRect,
    );
    const startX = startRect.left;
    const startY = startRect.top;
    const rawProgress =
      frameCount <= 0 ? 1 : Math.max(0, Math.min(1, frame / frameCount));
    const geometrySteps = Math.max(
      1,
      Math.round(Number(stateSettings.windowBootSteps || 4)),
    );
    const steppedFrame =
      frame <= 0 ? 0 : Math.ceil(rawProgress * geometrySteps);
    const maxVisibleStep =
      frame >= frameCount ? geometrySteps : Math.max(0, geometrySteps - 1);
    const steppedProgress = Math.min(
      1,
      Math.min(steppedFrame, maxVisibleStep) / geometrySteps,
    );
    const progress = steppedProgress >= 1 ? 1 : Math.pow(steppedProgress, 1.18);
    const width = startSize + (finalWidth - startSize) * progress;
    const height = startSize + (finalHeight - startSize) * progress;
    const left =
      startX * (1 - progress) +
      targetRect.left * progress +
      Math.sin(frame * 1.7) * (1 - progress) * 4;
    const top =
      startY * (1 - progress) +
      targetRect.top * progress +
      Math.cos(frame * 1.15) * (1 - progress) * 3;

    return { left, top, width, height };
  }

  function setWindowBootSquareFrame(square, stateSettings, frame, frameCount) {
    const { left, top, width, height } = getWindowBootFrameRect(
      stateSettings,
      frame,
      frameCount,
    );
    square.style.left = `${left.toFixed(1)}px`;
    square.style.top = `${top.toFixed(1)}px`;
    square.style.width = `${width.toFixed(1)}px`;
    square.style.height = `${height.toFixed(1)}px`;
    square.style.opacity = "1";

    return { left, top, width, height };
  }

  function addWindowBootTrace(rect, index, total) {
    const trace = document.createElement("span");
    trace.className = "app-boot-trace";
    trace.style.left = `${rect.left.toFixed(1)}px`;
    trace.style.top = `${rect.top.toFixed(1)}px`;
    trace.style.width = `${rect.width.toFixed(1)}px`;
    trace.style.height = `${rect.height.toFixed(1)}px`;
    trace.style.setProperty("--trace-index", index);
    trace.style.setProperty("--trace-count", total);
    trace.style.setProperty("--trace-alpha", "0.94");
    appRevealOverlay.append(trace);
  }

  function setWindowBootFullFrame(
    rect,
    stateSettings = getAnimationSettings(5),
  ) {
    appRevealOverlay
      .querySelectorAll(".app-boot-full-frame")
      .forEach((node) => node.remove());

    const frame = document.createElement("span");
    frame.className = "app-boot-full-frame";
    frame.style.left = `${rect.left.toFixed(1)}px`;
    frame.style.top = `${rect.top.toFixed(1)}px`;
    frame.style.width = `${rect.width.toFixed(1)}px`;
    frame.style.height = `${rect.height.toFixed(1)}px`;

    ["top", "right", "bottom", "left", "header"].forEach((side) => {
      const line = document.createElement("span");
      line.className = `app-boot-frame-side is-${side}`;
      frame.append(line);
    });

    const controls = document.createElement("span");
    controls.className = "app-boot-window-controls";
    syncWindowBootControlStyles(controls, stateSettings);
    for (let index = 0; index < 3; index += 1) {
      controls.append(document.createElement("span"));
    }
    frame.append(controls);

    appRevealOverlay.append(frame);
    return frame;
  }

  function syncWindowBootControlStyles(
    target = appRevealOverlay,
    stateSettings = getAnimationSettings(5),
  ) {
    const nodes = target?.classList?.contains("app-boot-window-controls")
      ? [target]
      : [...(target?.querySelectorAll?.(".app-boot-window-controls") || [])];

    nodes.forEach((node) => {
      node.style.setProperty(
        "--app-boot-control-size",
        `${Math.max(1, Number(stateSettings.windowBootControlSize ?? 8))}px`,
      );
      node.style.setProperty(
        "--app-boot-control-gap",
        `${Math.max(0, Number(stateSettings.windowBootControlGap ?? 8))}px`,
      );
      node.style.setProperty(
        "--app-boot-control-x",
        `${Number(stateSettings.windowBootControlX ?? 0)}px`,
      );
      node.style.setProperty(
        "--app-boot-control-y",
        `${Number(stateSettings.windowBootControlY ?? 0)}px`,
      );
      node.style.setProperty(
        "--app-boot-control-stroke",
        `${Math.max(0.1, Number(stateSettings.windowBootControlStroke ?? 1.5))}px`,
      );
    });
  }

  function pruneWindowBootTraces(maxVisible) {
    const traces = [...appRevealOverlay.querySelectorAll(".app-boot-trace")];
    const overflow = traces.length - Math.max(0, Math.round(maxVisible));
    if (overflow <= 0) {
      return;
    }

    traces.slice(0, overflow).forEach((trace) => trace.remove());
  }

  function scheduleWindowBootSquareSteps(square, stateSettings, duration) {
    const iterations = Math.max(
      2,
      Math.round(Number(stateSettings.windowBootTraceIterations || 7)),
    );
    const visibleTrail = Math.max(
      1,
      Math.round(Number(stateSettings.windowBootTrailCount || 3)),
    );
    const frameGap = duration / Math.max(1, iterations);
    const finalFrameDelay = Math.max(40, frameGap * 0.9);

    addWindowBootTrace(
      setWindowBootSquareFrame(square, stateSettings, 0, iterations),
      0,
      iterations + 1,
    );
    pruneWindowBootTraces(visibleTrail);

    for (let frame = 1; frame <= iterations; frame += 1) {
      const timer = window.setTimeout(() => {
        if (appPreview.classList.contains("app-boot-running")) {
          const rect = setWindowBootSquareFrame(
            square,
            stateSettings,
            frame,
            iterations,
          );
          if (frame === iterations) {
            pruneWindowBootTraces(0);
            const finalTimer = window.setTimeout(() => {
              if (appPreview.classList.contains("app-boot-running")) {
                setWindowBootSquareFrame(
                  square,
                  stateSettings,
                  iterations,
                  iterations,
                );
                square.style.opacity = "0";
                pruneWindowBootTraces(0);
                appPreview.classList.add("app-boot-final-frame-ready");
                setWindowBootFullFrame(rect, stateSettings);
                revealGatedAppPreviewSection();
              }
            }, finalFrameDelay);
            windowBootTimers.push(finalTimer);
            return;
          }
          addWindowBootTrace(rect, frame, iterations + 1);
          pruneWindowBootTraces(visibleTrail);
        }
      }, frame * frameGap);
      windowBootTimers.push(timer);
    }
  }

  function finishWindowBoot(token) {
    if (token !== windowBootRunId) {
      return;
    }

    appRevealOverlay
      .querySelectorAll(WINDOW_BOOT_NODE_SELECTOR)
      .forEach((node) => node.remove());
    appPreview.classList.remove(
      "app-boot-running",
      "app-boot-scaffolding",
      "app-boot-final-frame-ready",
    );
    appPreview.classList.remove("app-boot-clip-enabled");
    appPreview.classList.add("app-boot-finalizing");
    const timer = window.setTimeout(() => {
      if (token === windowBootRunId) {
        appPreview.classList.remove("app-boot-finalizing");
      }
    }, 240);
    windowBootTimers.push(timer);
  }

  function triggerWindowBoot({ autoFinish = true } = {}) {
    const stateSettings = getAnimationSettings(5);
    clearWindowBoot({ cancelTimers: false });
    clearPrototypeBuild();
    clearAppReveal();
    appPreview.classList.remove("app-boot-final-frame-ready");

    if (!stateSettings.windowBootEnabled) {
      revealGatedAppPreviewSection();
      return 0;
    }

    const rawDuration = Math.max(
      0,
      Number(stateSettings.windowBootDuration || 0),
    );
    if (rawDuration <= 0) {
      revealGatedAppPreviewSection();
      return 0;
    }

    const playbackSpeed = Math.max(0.1, stateSettings.speed || 1);
    const duration = rawDuration / playbackSpeed;
    const holdDuration = Math.min(
      duration * 0.45,
      Math.max(0, Number(stateSettings.windowBootWindowHold || 0)) /
        playbackSpeed,
    );
    const expansionDuration = getWindowBootExpansionDuration(stateSettings);
    const scaffoldStart = expansionDuration + holdDuration;
    const scaffoldDuration = Math.max(180, duration - scaffoldStart);
    const squareSize = Math.max(1, Number(stateSettings.windowBootSize || 8));
    const steps = Math.max(
      1,
      Math.round(Number(stateSettings.windowBootSteps || 4)),
    );
    const targetRect = getWindowBootTargetRect();
    const grow1Size = Math.min(
      Math.max(
        squareSize,
        squareSize * Math.max(1, Number(stateSettings.windowBootGrow1 || 1.9)),
      ),
      targetRect.width,
      targetRect.height,
    );
    const grow2Size = Math.min(
      Math.max(
        grow1Size,
        squareSize * Math.max(1, Number(stateSettings.windowBootGrow2 || 3.4)),
      ),
      targetRect.width,
      targetRect.height,
    );
    const midWidth = Math.max(grow2Size, targetRect.width * 0.48);
    const midHeight = Math.max(grow2Size, targetRect.height * 0.38);
    const nearWidth = Math.max(midWidth, targetRect.width * 0.84);
    const nearHeight = Math.max(midHeight, targetRect.height * 0.74);
    const bootRect = getWindowBootStartRect(
      stateSettings,
      squareSize,
      targetRect,
    );
    const bootX = bootRect.left;
    const bootY = bootRect.top;

    appPreview.classList.remove("is-polished");
    appPreview.classList.add("is-visible", "is-wire", "is-image-backed");
    gateAppPreviewSection();
    appPreview.style.setProperty(
      "--app-boot-duration",
      `${Math.max(120, duration)}ms`,
    );
    appPreview.style.setProperty("--app-boot-x", `${bootX.toFixed(1)}px`);
    appPreview.style.setProperty("--app-boot-y", `${bootY.toFixed(1)}px`);
    appPreview.style.setProperty(
      "--app-boot-size",
      `${squareSize.toFixed(1)}px`,
    );
    appPreview.style.setProperty("--app-boot-steps", `${steps}`);
    appPreview.style.setProperty(
      "--app-boot-grow-1-size",
      `${grow1Size.toFixed(1)}px`,
    );
    appPreview.style.setProperty(
      "--app-boot-grow-2-size",
      `${grow2Size.toFixed(1)}px`,
    );
    appPreview.style.setProperty(
      "--app-boot-frame-x",
      `${targetRect.left.toFixed(1)}px`,
    );
    appPreview.style.setProperty(
      "--app-boot-frame-y",
      `${targetRect.top.toFixed(1)}px`,
    );
    appPreview.style.setProperty(
      "--app-boot-frame-width",
      `${targetRect.width.toFixed(1)}px`,
    );
    appPreview.style.setProperty(
      "--app-boot-frame-height",
      `${targetRect.height.toFixed(1)}px`,
    );
    appPreview.style.setProperty(
      "--app-boot-frame-mid-width",
      `${midWidth.toFixed(1)}px`,
    );
    appPreview.style.setProperty(
      "--app-boot-frame-mid-height",
      `${midHeight.toFixed(1)}px`,
    );
    appPreview.style.setProperty(
      "--app-boot-frame-mid-x",
      `${(targetRect.left + (targetRect.width - midWidth) / 2).toFixed(1)}px`,
    );
    appPreview.style.setProperty(
      "--app-boot-frame-mid-y",
      `${(targetRect.top + (targetRect.height - midHeight) / 2).toFixed(1)}px`,
    );
    appPreview.style.setProperty(
      "--app-boot-frame-near-width",
      `${nearWidth.toFixed(1)}px`,
    );
    appPreview.style.setProperty(
      "--app-boot-frame-near-height",
      `${nearHeight.toFixed(1)}px`,
    );
    appPreview.style.setProperty(
      "--app-boot-frame-near-x",
      `${(targetRect.left + (targetRect.width - nearWidth) / 2).toFixed(1)}px`,
    );
    appPreview.style.setProperty(
      "--app-boot-frame-near-y",
      `${(targetRect.top + (targetRect.height - nearHeight) / 2).toFixed(1)}px`,
    );
    appRevealOverlay.replaceChildren();

    const square = document.createElement("span");
    square.className = "app-boot-square";
    appRevealOverlay.append(square);

    const token = ++windowBootRunId;
    appPreview.classList.add("app-boot-running", "app-boot-scaffolding");
    appPreview.classList.toggle(
      "app-boot-clip-enabled",
      Boolean(stateSettings.windowBootClip),
    );
    scheduleWindowBootSquareSteps(square, stateSettings, expansionDuration);
    const total = getWindowBootDuration(stateSettings);
    if (autoFinish) {
      const finishTimer = window.setTimeout(
        () => finishWindowBoot(token),
        total / playbackSpeed,
      );
      windowBootTimers.push(finishTimer);
    }
    return total;
  }

  function getTerminalScrollDuration(stateSettings = getAnimationSettings(4)) {
    return stateSettings.terminalScroll
      ? Math.max(0, Number(stateSettings.terminalScrollDuration ?? 620))
      : 0;
  }

  function getTerminalScrollLayerConfigs(
    stateSettings = getAnimationSettings(4),
  ) {
    return [
      {
        layer: installLayer || installText,
        amount: Number(stateSettings.terminalScrollInstall ?? 1),
      },
      {
        layer: promptLayer,
        amount: Number(stateSettings.terminalScrollPrompt ?? 1),
      },
      {
        layer: buildStatus,
        amount: Number(stateSettings.terminalScrollBuild ?? 1),
      },
    ].filter(({ layer }) => Boolean(layer));
  }

  function getTerminalScrollLayers() {
    return getTerminalScrollLayerConfigs().map(({ layer }) => layer);
  }

  function resetTerminalScrollStyles() {
    for (const layer of getTerminalScrollLayers()) {
      layer.style.transform = "";
      layer.style.opacity = "";
      layer.style.filter = "";
    }
  }

  function resetFinalStatusScrollStyles() {
    finalStatus.style.transform = "";
    finalStatus.style.opacity = "";
    finalStatus.style.filter = "";
  }

  function getTerminalScrollFrames() {
    return [
      { at: 0, progress: 0, opacity: 1, jitter: 0, filter: "" },
      { at: 0.1, progress: 0.046, opacity: 1, jitter: 0.18, filter: "" },
      {
        at: 0.22,
        progress: 0.189,
        opacity: 1,
        jitter: 0.68,
        filter: "brightness(1.1) contrast(1.08)",
      },
      { at: 0.34, progress: 0.143, opacity: 1, jitter: 0.22, filter: "" },
      {
        at: 0.5,
        progress: 0.378,
        opacity: 0.98,
        jitter: 0.84,
        filter: "brightness(1.24) contrast(1.18)",
      },
      { at: 0.64, progress: 0.577, opacity: 0.9, jitter: 0.34, filter: "" },
      {
        at: 0.76,
        progress: 0.505,
        opacity: 0.82,
        jitter: 0.74,
        filter: "brightness(0.9) contrast(1.24)",
      },
      { at: 0.88, progress: 0.811, opacity: 0.54, jitter: 0.26, filter: "" },
      {
        at: 1,
        progress: 1,
        opacity: 0,
        jitter: 0,
        filter: "brightness(0.72) contrast(1.3)",
      },
    ];
  }

  function getTerminalScrollProgress(
    frame,
    stateSettings = getAnimationSettings(4),
  ) {
    const motionJitter = Math.max(
      0,
      Math.min(2, Number(stateSettings.terminalScrollMotionJitter ?? 1)),
    );
    const smoothProgress = Math.max(0, Math.min(1, Number(frame.at ?? 0)));
    const rawProgress = Math.max(
      0,
      Math.min(1, Number(frame.progress ?? smoothProgress)),
    );
    return smoothProgress + (rawProgress - smoothProgress) * motionJitter;
  }

  function applyTerminalScrollFrame(
    frame,
    stateSettings = getAnimationSettings(4),
  ) {
    const distance = Math.max(
      0,
      Number(stateSettings.terminalScrollDistance ?? 260),
    );
    const endOpacity = Math.max(
      0,
      Math.min(1, Number(stateSettings.terminalScrollEndOpacity ?? 0)),
    );
    const opacityJitter = Math.max(
      0,
      Math.min(0.5, Number(stateSettings.terminalScrollOpacityJitter ?? 0.18)),
    );
    const jitterX = Math.max(
      0,
      Number(stateSettings.terminalScrollJitterX ?? 0),
    );
    const jitterY = Math.max(
      0,
      Number(stateSettings.terminalScrollJitterY ?? 0),
    );
    const progress = getTerminalScrollProgress(frame, stateSettings);
    const jitter = Math.max(0, Number(frame.jitter || 0)) * opacityJitter;
    const jitterSeed = Math.round(Number(frame.at || 0) * 100);
    const shakeX =
      ((jitterSeed % 2) * 2 - 1) *
      Math.max(0, Number(frame.jitter || 0)) *
      jitterX;
    const shakeY =
      ((jitterSeed % 3) - 1) * Math.max(0, Number(frame.jitter || 0)) * jitterY;
    const opacity = Math.max(
      endOpacity,
      Math.min(
        1,
        endOpacity + Math.max(0, frame.opacity - jitter) * (1 - endOpacity),
      ),
    );

    for (const { layer, amount } of getTerminalScrollLayerConfigs(
      stateSettings,
    )) {
      const safeAmount = Math.max(0, Number.isFinite(amount) ? amount : 1);
      const scrollY = distance * progress * safeAmount;
      layer.style.setProperty(
        "transform",
        `translate3d(${shakeX.toFixed(2)}px, ${(scrollY + shakeY).toFixed(2)}px, 0)`,
        "important",
      );
      layer.style.setProperty("opacity", String(opacity), "important");
      layer.style.setProperty("filter", frame.filter || "", "important");
    }
  }

  function applyFinalStatusScrollFrame(
    frame,
    stateSettings = getAnimationSettings(4),
  ) {
    const distance = Math.max(
      0,
      Number(stateSettings.terminalScrollDistance ?? 260),
    );
    const amount = Math.max(0, Number(stateSettings.terminalScrollFinal ?? 1));
    const endOpacity = Math.max(
      0,
      Math.min(1, Number(stateSettings.terminalScrollEndOpacity ?? 0)),
    );
    const opacityJitter = Math.max(
      0,
      Math.min(0.5, Number(stateSettings.terminalScrollOpacityJitter ?? 0.18)),
    );
    const jitterX = Math.max(
      0,
      Number(stateSettings.terminalScrollJitterX ?? 0),
    );
    const jitterY = Math.max(
      0,
      Number(stateSettings.terminalScrollJitterY ?? 0),
    );
    const progress = getTerminalScrollProgress(frame, stateSettings);
    const jitter = Math.max(0, Number(frame.jitter || 0)) * opacityJitter;
    const jitterSeed = Math.round(Number(frame.at || 0) * 100);
    const shakeX =
      ((jitterSeed % 2) * 2 - 1) *
      Math.max(0, Number(frame.jitter || 0)) *
      jitterX;
    const shakeY =
      ((jitterSeed % 3) - 1) * Math.max(0, Number(frame.jitter || 0)) * jitterY;
    const opacity = Math.max(
      endOpacity,
      Math.min(
        1,
        endOpacity + Math.max(0, frame.opacity - jitter) * (1 - endOpacity),
      ),
    );

    finalStatus.style.setProperty(
      "transform",
      `translate3d(${shakeX.toFixed(2)}px, ${(distance * progress * amount + shakeY).toFixed(2)}px, 0)`,
      "important",
    );
    finalStatus.style.setProperty("opacity", String(opacity), "important");
    finalStatus.style.setProperty("filter", frame.filter || "", "important");
  }

  async function playFinalStatusScrollDown(
    token,
    duration = getTerminalScrollDuration(),
  ) {
    const frames = getTerminalScrollFrames();
    const stateSettings = getAnimationSettings(4);
    const safeDuration = Math.max(0, Number(duration || 0));

    resetFinalStatusScrollStyles();
    finalStatus.classList.add("is-visible");

    if (safeDuration <= 0) {
      applyFinalStatusScrollFrame(frames[frames.length - 1], stateSettings);
      return;
    }

    applyFinalStatusScrollFrame(frames[0], stateSettings);
    for (let index = 1; index < frames.length; index += 1) {
      const previous = frames[index - 1];
      const frame = frames[index];
      await wait((frame.at - previous.at) * safeDuration, token);
      applyFinalStatusScrollFrame(frame, stateSettings);
    }
  }

  async function playTerminalOutputScroll(
    token,
    duration = getTerminalScrollDuration(),
  ) {
    const frames = getTerminalScrollFrames();
    const stateSettings = getAnimationSettings(4);
    const safeDuration = Math.max(0, Number(duration || 0));

    resetTerminalScrollStyles();
    stage.classList.remove("terminal-scroll");
    void stage.offsetWidth;
    stage.classList.add("terminal-scroll");

    if (safeDuration <= 0) {
      applyTerminalScrollFrame(frames[frames.length - 1], stateSettings);
      return;
    }

    applyTerminalScrollFrame(frames[0], stateSettings);
    for (let index = 1; index < frames.length; index += 1) {
      const previous = frames[index - 1];
      const frame = frames[index];
      await wait((frame.at - previous.at) * safeDuration, token);
      applyTerminalScrollFrame(frame, stateSettings);
    }
  }

  async function playAppRevealOverWindowBoot(
    token,
    mode = "prototype",
    state = 5,
  ) {
    const duration = triggerAppReveal(mode, state, { preserveBootFrame: true });
    clearWindowBoot({ preserveFrame: true });
    if (duration > 0) {
      await wait(duration + 260, token);
    }
    setAppRevealCompleteTarget(mode === "polished");
  }

  function getStateActionDuration(state) {
    const stateSettings = getAnimationSettings(state);

    if (state === 1) {
      const powerOnDuration = getPowerOnDuration(stateSettings);
      const logoSettings = stateLogoSettings[1];
      const logoDelay = Math.max(0, Number(logoSettings.revealDelay || 0));
      const logoOpacityDuration = Math.max(
        0,
        Number(logoSettings.opacityDuration ?? 420),
      );
      const logoMaskDuration = Math.max(
        0,
        Number(logoSettings.revealDuration || 0),
      );
      const logoWarmupDuration = stateSettings.logoTubeWarmupEnabled
        ? Math.max(0, Number(stateSettings.logoTubeWarmupDuration || 0))
        : 0;
      const logoTrackDuration =
        logoDelay +
        Math.max(logoOpacityDuration, logoMaskDuration, logoWarmupDuration);
      const startupDuration = Math.max(powerOnDuration, logoTrackDuration);
      const installDurationAfterDelayBefore = Math.max(
        0,
        getInstallTypingDuration() -
          Math.max(0, Number(stateSettings.delayBefore || 0)),
      );
      return Math.max(startupDuration, installDurationAfterDelayBefore);
    }

    if (state === 2) {
      return (
        getTypingDuration(
          promptCopy,
          stateSettings.promptType,
          stateSettings.promptLinePause,
          stateSettings.promptTypingMode,
          getPromptStartDelay(stateSettings),
          { human: true, contrast: stateSettings.promptTypingContrast },
        ) + getPromptEnterDuration(stateSettings)
      );
    }

    if (state === 3) {
      return (
        stateSettings.buildLineReveal * (1 + statusRows.length + 0.9) + 520
      );
    }

    if (state === 4) {
      return Math.max(280, getTerminalScrollDuration(stateSettings));
    }

    if (state === 5) {
      const bootDuration = getWindowBootFrameReadyDelay(
        getAnimationSettings(5),
      );
      const prototypeRevealDuration = getAppRevealTotalDuration(
        getAppRevealSettings(5),
      );
      return (
        Math.max(0, Number(preAppFocusSettings.duration || 0), bootDuration) +
        prototypeRevealDuration
      );
    }

    if (state === 6) {
      return getAppRevealTotalDuration(getAppRevealSettings(6));
    }

    return 500;
  }

  function getStateDuration(state) {
    const stateSettings = getAnimationSettings(state);
    const rawDuration =
      stateSettings.delayBefore +
      getStateActionDuration(state) +
      stateSettings.delayAfter;
    return Math.max(120, rawDuration / Math.max(0.1, stateSettings.speed));
  }

  function resizeGridCanvas() {
    const pixelRatio = Math.min(
      window.devicePixelRatio || 1,
      GRID_PIXEL_RATIO_CAP,
    );
    const width = stage.clientWidth;
    const height = stage.clientHeight;

    if (
      !gridCanvasDirty &&
      gridCanvas.width === width * pixelRatio &&
      gridCanvas.height === height * pixelRatio
    ) {
      return { width, height, resized: false };
    }

    gridCanvas.width = width * pixelRatio;
    gridCanvas.height = height * pixelRatio;
    gridCanvas.style.width = `${width}px`;
    gridCanvas.style.height = `${height}px`;
    gridCtx.setTransform(pixelRatio, 0, 0, pixelRatio, 0, 0);
    gridCanvasDirty = false;
    lastGridRenderKey = "";
    return { width, height, resized: true };
  }

  function syncGridLayer() {
    setRootVar("--grid-opacity", 0);
  }

  function clamp(value, min, max) {
    return Math.min(max, Math.max(min, value));
  }

  function setAppDragVars(x = 0, y = 0) {
    setRootVar("--app-drag-x", `${x.toFixed(2)}px`);
    setRootVar("--app-drag-y", `${y.toFixed(2)}px`);
  }

  function getAppWindowEchoLayer() {
    if (!appWindowEchoLayer) {
      appWindowEchoLayer = document.createElement("div");
      appWindowEchoLayer.className = "app-drag-echo-layer";
      appWindowEchoLayer.setAttribute("aria-hidden", "true");
      (stagePerspectiveShell || stage).append(appWindowEchoLayer);
    }

    return appWindowEchoLayer;
  }

  function clearAppWindowEchoes() {
    appWindowEchoLayer?.replaceChildren();
  }

  function addAppWindowDragEcho(force = false) {
    if (!appWindowDragState || !appPreview.classList.contains("is-visible")) {
      return;
    }

    const now = performance.now();
    const minDistance = Math.max(0, Number(dragEchoSettings.minDistance ?? 7));
    const minInterval = Math.max(0, Number(dragEchoSettings.minInterval ?? 46));
    const distance = Math.hypot(
      appWindowDragState.x - appWindowDragState.lastEchoX,
      appWindowDragState.y - appWindowDragState.lastEchoY,
    );
    if (
      !force &&
      distance < minDistance &&
      now - appWindowDragState.lastEchoAt < minInterval
    ) {
      return;
    }

    const echoRoot = stagePerspectiveShell || stage;
    const stageRect = echoRoot.getBoundingClientRect();
    const appRect = appPreview.getBoundingClientRect();
    const scaleX = stageRect.width / Math.max(1, echoRoot.offsetWidth);
    const scaleY = stageRect.height / Math.max(1, echoRoot.offsetHeight);
    const echo = document.createElement("span");
    echo.className = "app-drag-echo-frame";
    echo.style.left = `${((appRect.left - stageRect.left) / Math.max(0.001, scaleX)).toFixed(1)}px`;
    echo.style.top = `${((appRect.top - stageRect.top) / Math.max(0.001, scaleY)).toFixed(1)}px`;
    echo.style.width = `${(appRect.width / Math.max(0.001, scaleX)).toFixed(1)}px`;
    echo.style.height = `${(appRect.height / Math.max(0.001, scaleY)).toFixed(1)}px`;

    const layer = getAppWindowEchoLayer();
    layer.append(echo);
    const maxFrames = Math.max(
      1,
      Math.round(Number(dragEchoSettings.maxFrames ?? 18)),
    );
    while (layer.children.length > maxFrames) {
      layer.firstElementChild?.remove();
    }

    appWindowDragState.lastEchoX = appWindowDragState.x;
    appWindowDragState.lastEchoY = appWindowDragState.y;
    appWindowDragState.lastEchoAt = now;

    const lifetime = Math.max(80, Number(dragEchoSettings.lifetime ?? 760));
    window.setTimeout(() => echo.remove(), lifetime);
  }

  function softenAppDragDelta(value) {
    const sign = Math.sign(value);
    const abs = Math.abs(value);
    if (abs <= APP_DRAG_LIMIT) {
      return value;
    }

    const overflow = abs - APP_DRAG_LIMIT;
    return (
      sign * Math.min(APP_DRAG_HARD_LIMIT, APP_DRAG_LIMIT + overflow * 0.28)
    );
  }

  function releaseAppWindowDrag(event) {
    if (!appWindowDragState) {
      return;
    }

    const { dragTarget, pointerId, x, y } = appWindowDragState;
    addAppWindowDragEcho(true);
    appWindowDragOffset = { x, y };
    setAppDragVars(appWindowDragOffset.x, appWindowDragOffset.y);
    appWindowDragState = null;
    dragTarget?.releasePointerCapture?.(pointerId);
    window.removeEventListener("pointermove", moveAppWindowDrag);
    window.removeEventListener("pointerup", releaseAppWindowDrag);
    window.removeEventListener("pointercancel", releaseAppWindowDrag);
    appPreview.classList.remove("is-window-dragging");
    event?.preventDefault?.();
  }

  function startAppWindowDrag(event) {
    if (
      !appPreview.classList.contains("is-visible") ||
      (event.button ?? 0) !== 0
    ) {
      return;
    }

    event.preventDefault();
    event.stopPropagation();
    event.currentTarget.setPointerCapture?.(event.pointerId);
    appPreview.classList.remove("is-window-returning");
    appPreview.classList.add("is-window-dragging");
    appWindowDragState = {
      pointerId: event.pointerId,
      dragTarget: event.currentTarget,
      startX: event.clientX,
      startY: event.clientY,
      startOffsetX: appWindowDragOffset.x,
      startOffsetY: appWindowDragOffset.y,
      x: appWindowDragOffset.x,
      y: appWindowDragOffset.y,
      lastEchoX: Number.POSITIVE_INFINITY,
      lastEchoY: Number.POSITIVE_INFINITY,
      lastEchoAt: 0,
    };
    setAppDragVars(appWindowDragOffset.x, appWindowDragOffset.y);
    clearAppWindowEchoes();
    addAppWindowDragEcho(true);
    window.addEventListener("pointermove", moveAppWindowDrag, {
      signal: eventController.signal,
    });
    window.addEventListener("pointerup", releaseAppWindowDrag, {
      signal: eventController.signal,
    });
    window.addEventListener("pointercancel", releaseAppWindowDrag, {
      signal: eventController.signal,
    });
  }

  function moveAppWindowDrag(event) {
    if (
      !appWindowDragState ||
      appWindowDragState.pointerId !== event.pointerId
    ) {
      return;
    }

    event.preventDefault();
    const x =
      appWindowDragState.startOffsetX +
      softenAppDragDelta(event.clientX - appWindowDragState.startX);
    const y =
      appWindowDragState.startOffsetY +
      softenAppDragDelta(event.clientY - appWindowDragState.startY);
    appWindowDragState.x = x;
    appWindowDragState.y = y;
    setAppDragVars(x, y);
    addAppWindowDragEcho();
  }

  function bindRuntimeAppWindowDrag() {
    const appWindowBar = appPreview.querySelector(".app-window-bar");
    if (!appWindowBar || appWindowBar.dataset.dragBound === "true") {
      return;
    }

    appWindowBar.dataset.dragBound = "true";
    appWindowBar.addEventListener("pointerdown", startAppWindowDrag, {
      signal: eventController.signal,
    });
  }

  function setParallaxVars(bgX = 0, bgY = 0, appX = 0, appY = 0) {
    setRootVar("--parallax-bg-x", `${bgX.toFixed(2)}px`);
    setRootVar("--parallax-bg-y", `${bgY.toFixed(2)}px`);
    setRootVar("--parallax-app-x", `${appX.toFixed(2)}px`);
    setRootVar("--parallax-app-y", `${appY.toFixed(2)}px`);
  }

  function setPerspectiveVars(
    rotateX = 0,
    rotateY = 0,
    shiftX = 0,
    shiftY = 0,
    depth = 1000,
    glareX = 50,
    glareY = 50,
    glareOpacity = 0,
    windowLift = 0,
  ) {
    const perspectiveDepth = Math.max(320, depth);
    const lift = Math.max(0, Number(windowLift || 0));
    const zScale =
      1 + Math.min(0.08, lift / Math.max(1200, perspectiveDepth * 2.4));

    setRootVar("--perspective-rotate-x", `${rotateX.toFixed(3)}deg`);
    setRootVar("--perspective-rotate-y", `${rotateY.toFixed(3)}deg`);
    setRootVar("--perspective-shift-x", `${shiftX.toFixed(2)}px`);
    setRootVar("--perspective-shift-y", `${shiftY.toFixed(2)}px`);
    setRootVar("--perspective-depth", `${perspectiveDepth.toFixed(0)}px`);
    setRootVar("--perspective-window-lift", `${lift.toFixed(2)}px`);
    setRootVar("--perspective-window-z-scale", zScale.toFixed(4));
    setRootVar("--perspective-logo-lift", `${(lift * 0.28).toFixed(2)}px`);
    setRootVar("--perspective-glare-x", `${glareX.toFixed(1)}%`);
    setRootVar("--perspective-glare-y", `${glareY.toFixed(1)}%`);
    setRootVar("--perspective-glare-opacity", glareOpacity.toFixed(3));
  }

  function updateParallaxFromPointer(event) {
    const rect = stage.getBoundingClientRect();
    const now = performance.now();
    quietLastInteractionAt = now;
    if (renderIdleSettled) {
      setRenderIdleSettled(false);
    }

    parallaxTargetX = clamp(
      ((event.clientX - rect.left) / rect.width) * 2 - 1,
      -1,
      1,
    );
    parallaxTargetY = clamp(
      ((event.clientY - rect.top) / rect.height) * 2 - 1,
      -1,
      1,
    );
    parallaxLastPointerAt = now;

    perspectiveTargetX = clamp(
      ((event.clientX - rect.left) / rect.width) * 2 - 1,
      -1,
      1,
    );
    perspectiveTargetY = clamp(
      ((event.clientY - rect.top) / rect.height) * 2 - 1,
      -1,
      1,
    );
    perspectiveLastPointerAt = now;
    perspectiveHasPointerTarget = true;
    schedulePerspectiveFrame();
  }

  function updateParallaxFrame() {
    const config = getGroupFxConfig("parallax");
    const active = isGroupFxActive("parallax") && (config.intensity ?? 0) > 0;
    const idleStopsParallax = renderIdleSettled || shouldSettleIdlePreview();

    if (!active || idleStopsParallax) {
      parallaxTargetX = 0;
      parallaxTargetY = 0;
    } else if (config.loop && performance.now() - parallaxLastPointerAt > 700) {
      const driftTime =
        (performance.now() / 1000) * Math.max(0, config.speed ?? 1);
      parallaxTargetX = Math.sin(driftTime * 1.15) * 0.38;
      parallaxTargetY = Math.cos(driftTime * 0.9) * 0.26;
    }

    const delay = Math.max(0, config.duration ?? 180);
    const ease = delay <= 0 ? 1 : clamp(16 / (delay + 16), 0.018, 0.34);
    parallaxCurrentX += (parallaxTargetX - parallaxCurrentX) * ease;
    parallaxCurrentY += (parallaxTargetY - parallaxCurrentY) * ease;

    if (
      !active &&
      !parallaxWasActive &&
      Math.abs(parallaxCurrentX) < 0.001 &&
      Math.abs(parallaxCurrentY) < 0.001
    ) {
      return;
    }

    if (
      !active &&
      Math.abs(parallaxCurrentX) < 0.001 &&
      Math.abs(parallaxCurrentY) < 0.001
    ) {
      parallaxCurrentX = 0;
      parallaxCurrentY = 0;
    }

    const strength = clamp(config.intensity ?? 0, 0, 10) / 5;
    const xStrength = Math.max(0, config.xStrength ?? 48) * strength;
    const yStrength = Math.max(0, config.yStrength ?? 22) * strength;
    const balance = clamp(config.balance ?? 0, -1, 1);
    const bgFactor = (balance - 1) / 2;
    const appFactor = (balance + 1) / 2;

    setParallaxVars(
      parallaxCurrentX * xStrength * bgFactor,
      parallaxCurrentY * yStrength * bgFactor,
      parallaxCurrentX * xStrength * appFactor,
      parallaxCurrentY * yStrength * appFactor,
    );
    parallaxWasActive = active;
  }

  function updatePerspectiveFrame() {
    const config = getGroupFxConfig("perspective");
    const active =
      isGroupFxActive("perspective") && (config.intensity ?? 0) > 0;
    const idleStopsPerspective = renderIdleSettled || shouldSettleIdlePreview();

    if (!active || idleStopsPerspective) {
      perspectiveTargetX = 0;
      perspectiveTargetY = 0;
      perspectiveHasPointerTarget = false;
    } else if (
      config.loop &&
      performance.now() - perspectiveLastPointerAt > 700
    ) {
      const driftTime =
        (performance.now() / 1000) * Math.max(0, config.speed ?? 1);
      perspectiveTargetX = Math.sin(driftTime * 0.84) * 0.22;
      perspectiveTargetY = Math.cos(driftTime * 0.72) * 0.16;
      perspectiveHasPointerTarget = false;
    }

    const delay = Math.max(0, config.duration ?? 220);
    const ease = delay <= 0 ? 1 : clamp(16 / (delay + 16), 0.015, 0.28);
    perspectiveCurrentX += (perspectiveTargetX - perspectiveCurrentX) * ease;
    perspectiveCurrentY += (perspectiveTargetY - perspectiveCurrentY) * ease;

    if (
      !active &&
      !perspectiveWasActive &&
      Math.abs(perspectiveCurrentX) < 0.001 &&
      Math.abs(perspectiveCurrentY) < 0.001
    ) {
      return;
    }

    if (
      !active &&
      Math.abs(perspectiveCurrentX) < 0.001 &&
      Math.abs(perspectiveCurrentY) < 0.001
    ) {
      perspectiveCurrentX = 0;
      perspectiveCurrentY = 0;
    }

    const strength = clamp(config.intensity ?? 0, 0, 10) / 5;
    const tiltX =
      Math.max(0, Number(config.tiltX ?? config.tilt ?? 7)) * strength;
    const tiltY =
      Math.max(0, Number(config.tiltY ?? config.tilt ?? 7)) * strength;
    const shiftX =
      Math.max(0, Number(config.shiftX ?? config.shift ?? 12)) * strength;
    const shiftY =
      Math.max(0, Number(config.shiftY ?? (config.shift ?? 12) * 0.72)) *
      strength;
    const depth = Math.max(320, Number(config.depth ?? 980));
    const windowLift = active
      ? Math.max(0, Number(config.windowLift ?? 34))
      : 0;
    const glareX = 50 + perspectiveCurrentX * 28;
    const glareY = 50 + perspectiveCurrentY * 22;

    setPerspectiveVars(
      -perspectiveCurrentY * tiltY,
      perspectiveCurrentX * tiltX,
      perspectiveCurrentX * shiftX,
      perspectiveCurrentY * shiftY,
      depth,
      glareX,
      glareY,
      0,
      windowLift,
    );
    perspectiveWasActive = active;
  }

  function isPerspectiveLoopActive() {
    const config = getGroupFxConfig("perspective");
    return isGroupFxActive("perspective") && (config.intensity ?? 0) > 0;
  }

  function hasPerspectiveFrameWork() {
    return (
      isPerspectiveLoopActive() ||
      perspectiveHasPointerTarget ||
      Math.abs(perspectiveCurrentX) > 0.001 ||
      Math.abs(perspectiveCurrentY) > 0.001 ||
      Math.abs(perspectiveTargetX) > 0.001 ||
      Math.abs(perspectiveTargetY) > 0.001
    );
  }

  function runPerspectiveFrame() {
    perspectiveFrameRequest = 0;

    if (document.hidden || renderIdleSettled) {
      return;
    }

    updatePerspectiveFrame();

    if (hasPerspectiveFrameWork()) {
      schedulePerspectiveFrame();
    }
  }

  function schedulePerspectiveFrame() {
    if (
      destroyed ||
      perspectiveFrameRequest ||
      document.hidden ||
      renderIdleSettled
    ) {
      return;
    }

    perspectiveFrameRequest = window.requestAnimationFrame(runPerspectiveFrame);
  }

  function hexToRgb(hex, fallback = { r: 215, g: 221, b: 232 }) {
    const normalized = String(hex || "")
      .trim()
      .replace(/^#/, "");
    const expanded =
      normalized.length === 3
        ? normalized
            .split("")
            .map((char) => char + char)
            .join("")
        : normalized;

    if (!/^[0-9a-f]{6}$/i.test(expanded)) {
      return fallback;
    }

    const value = Number.parseInt(expanded, 16);
    return {
      r: (value >> 16) & 255,
      g: (value >> 8) & 255,
      b: value & 255,
    };
  }

  function rgbTupleFromColor(hex, fallbackHex = "#d7dde8") {
    const fallback = hexToRgb(fallbackHex);
    const { r, g, b } = hexToRgb(hex, fallback);
    return `${r} ${g} ${b}`;
  }

  function colorSaturation(hex, fallbackHex = "#d7dde8") {
    const fallback = hexToRgb(fallbackHex);
    const { r, g, b } = hexToRgb(hex, fallback);
    const max = Math.max(r, g, b);
    const min = Math.min(r, g, b);
    return max <= 0 ? 0 : (max - min) / max;
  }

  function isYellowishColor(hex) {
    const { r, g, b } = hexToRgb(hex, { r: 0, g: 0, b: 0 });
    return r > 145 && g > 120 && b < 95 && Math.abs(r - g) < 115;
  }

  function sanitizeLogoClickSettings() {
    if (isYellowishColor(logoClickSettings.waveColor)) {
      logoClickSettings.waveColor = "#62636a";
    }
  }

  function queueLogoWaveTimer(callback, delay) {
    const timer = window.setTimeout(
      () => {
        logoWaveTimers = logoWaveTimers.filter(
          (activeTimer) => activeTimer !== timer,
        );
        callback();
      },
      Math.max(0, delay),
    );

    logoWaveTimers.push(timer);
  }

  function releaseLogoRectHoverSuppression(rect) {
    window.clearTimeout(logoHoverSuppressionTimers.get(rect));
    logoHoverSuppressionTimers.delete(rect);
    rect.classList.remove("is-logo-hover-suppressed");
  }

  function suppressLogoRectHover(rect, duration) {
    rect.classList.add("is-logo-hover-suppressed");
    window.clearTimeout(logoHoverSuppressionTimers.get(rect));
    const timer = window.setTimeout(
      () => releaseLogoRectHoverSuppression(rect),
      Math.max(0, duration),
    );
    logoHoverSuppressionTimers.set(rect, timer);
    rect.addEventListener(
      "pointerleave",
      () => releaseLogoRectHoverSuppression(rect),
      { once: true, signal: eventController.signal },
    );
  }

  function getLogoRectWaveState(rect) {
    let state = logoRectWaveStates.get(rect);

    if (!state) {
      state = {
        wave: new Set(),
        tail: new Set(),
        pressed: new Set(),
      };
      logoRectWaveStates.set(rect, state);
      logoWaveRects.add(rect);
    }

    return state;
  }

  function syncLogoRectWaveClasses(rect) {
    const state = getLogoRectWaveState(rect);
    rect.classList.toggle("is-logo-wave", state.wave.size > 0);
    rect.classList.toggle(
      "is-logo-wave-tail",
      state.tail.size > 0 && state.wave.size === 0,
    );
    rect.classList.toggle("is-logo-pressed", state.pressed.size > 0);
  }

  function setLogoRectWaveState(rect, type, waveId, active) {
    const state = getLogoRectWaveState(rect);

    if (active) {
      state[type].add(waveId);
    } else {
      state[type].delete(waveId);
    }

    syncLogoRectWaveClasses(rect);
  }

  function getSvgRectCenter(rect) {
    const x = Number(rect.getAttribute("x") || 0);
    const y = Number(rect.getAttribute("y") || 0);
    const width = Number(rect.getAttribute("width") || 0);
    const height = Number(rect.getAttribute("height") || 0);

    return {
      x: x + width / 2,
      y: y + height / 2,
    };
  }

  function runLogoRectWave(svg, sourceRect) {
    const rects = [...svg.querySelectorAll(".hero-logo-shapes rect")];
    const sourceCenter = getSvgRectCenter(sourceRect);
    const waveCount = Math.max(1, Math.round(logoClickSettings.waveCount || 1));
    const waveGap = Math.max(0, logoClickSettings.waveGap || 0);
    let waveDuration = 0;

    for (let waveIndex = 0; waveIndex < waveCount; waveIndex += 1) {
      const waveId = ++logoWaveInstanceId;
      const waveOffset = waveIndex * waveGap;

      rects.forEach((rect) => {
        const center = getSvgRectCenter(rect);
        const distance = Math.hypot(
          center.x - sourceCenter.x,
          center.y - sourceCenter.y,
        );
        const delay =
          waveOffset + Math.round(distance * logoClickSettings.waveSpread);
        const hold = logoClickSettings.waveHold;
        const tailHold = logoClickSettings.tailHold;
        waveDuration = Math.max(waveDuration, delay + hold + tailHold);

        queueLogoWaveTimer(() => {
          setLogoRectWaveState(rect, "tail", waveId, false);
          setLogoRectWaveState(rect, "wave", waveId, true);

          if (rect === sourceRect) {
            setLogoRectWaveState(rect, "pressed", waveId, true);
          }
        }, delay);

        queueLogoWaveTimer(() => {
          setLogoRectWaveState(rect, "wave", waveId, false);
          setLogoRectWaveState(rect, "pressed", waveId, false);
          setLogoRectWaveState(rect, "tail", waveId, true);
        }, delay + hold);

        queueLogoWaveTimer(
          () => {
            setLogoRectWaveState(rect, "tail", waveId, false);
          },
          delay + hold + tailHold,
        );
      });
    }

    suppressLogoRectHover(sourceRect, waveDuration + 50);
  }

  function setupLogoRectInteraction(svg) {
    [...svg.querySelectorAll(".hero-logo-shapes rect")].forEach(
      (rect, index) => {
        rect.dataset.logoRect = String(index);
        rect.addEventListener(
          "click",
          (event) => {
            event.stopPropagation();
            runLogoRectWave(svg, rect);
          },
          { signal: eventController.signal },
        );
      },
    );
  }

  function normalizeLogoSvg(svg) {
    svg.setAttribute("preserveAspectRatio", "none");
    svg.classList.add("hero-logo-inline-svg");

    const mask = svg.querySelector("mask");
    const maskedFill = svg.querySelector("g[mask]");

    if (!mask) {
      return;
    }

    const shapes = document.createElementNS("http://www.w3.org/2000/svg", "g");
    shapes.classList.add("hero-logo-shapes");
    const pathsLayer = document.createElementNS(
      "http://www.w3.org/2000/svg",
      "g",
    );
    pathsLayer.classList.add("hero-logo-line-layer");
    const rectsLayer = document.createElementNS(
      "http://www.w3.org/2000/svg",
      "g",
    );
    rectsLayer.classList.add("hero-logo-rect-layer");

    [...mask.children].forEach((shape) => {
      const clone = shape.cloneNode(true);
      clone.removeAttribute("style");
      clone.removeAttribute("mask");

      if (clone.tagName.toLowerCase() === "rect") {
        clone.setAttribute("fill", "#32323C");
      }

      if (clone.tagName.toLowerCase() === "path") {
        clone.setAttribute("fill", "none");
        clone.setAttribute("stroke", "#32323C");
        clone.setAttribute("stroke-linejoin", "miter");
      }

      if (clone.tagName.toLowerCase() === "path") {
        pathsLayer.appendChild(clone);
      } else {
        rectsLayer.appendChild(clone);
      }
    });

    shapes.append(pathsLayer, rectsLayer);
    maskedFill?.remove();
    mask.remove();
    svg.appendChild(shapes);
    if (window.DATABRICKS_USED_FEATURES?.logoClick) {
      setupLogoRectInteraction(svg);
    }
  }

  async function loadLogoSvg() {
    if (!logoSvgHosts.length) {
      return;
    }

    try {
      const svgText = window.DATABRICKS_EXPORT_ASSETS?.logoSvg;

      if (!svgText) {
        return;
      }

      logoSvgHosts.forEach((host) => {
        host.innerHTML = svgText;
        const svg = host.querySelector("svg");
        if (svg) {
          normalizeLogoSvg(svg);
        }
      });
    } catch (error) {
      console.warn("Could not load inline logo SVG", error);
    }
  }

  function setLogoReveal(percent, shift = 0) {
    setRootVar("--logo-reveal", `${percent}%`);
    setRootVar("--logo-shift", `${shift}px`);
  }

  function setLogoBlockOpacity(value) {
    setRootVar(
      "--logo-block-opacity",
      Math.max(0, Math.min(1, Number(value) || 0)).toFixed(3),
    );
  }

  async function waitLogoRevealHold(duration, token, respectPause) {
    if (respectPause) {
      await wait(duration, token);
      return;
    }

    let remaining = duration;

    while (remaining > 0) {
      if (token !== runId) {
        throw new Error("Animation cancelled");
      }

      const chunk = Math.min(remaining, 50);
      const startedAt = performance.now();
      await rawWait(chunk);
      remaining -= performance.now() - startedAt;
    }
  }

  async function animateLogoBlockOpacity(
    token,
    { respectPause = false, revealRunId = ++logoRunId } = {},
  ) {
    const localLogoRunId = revealRunId;
    const logoSettings = stateLogoSettings[1];
    const opacityDuration = Math.max(
      0,
      Number(logoSettings.opacityDuration ?? 420),
    );

    if (opacityDuration <= 0) {
      setLogoBlockOpacity(1);
      return;
    }

    const steps = Math.max(1, Math.round(opacityDuration / 34));
    const stepHold = opacityDuration / steps;

    for (let step = 0; step <= steps; step += 1) {
      if (token !== runId || localLogoRunId !== logoRunId) {
        return;
      }

      const progress = step / steps;
      setLogoBlockOpacity(progress);

      if (step < steps) {
        await waitLogoRevealHold(stepHold, token, respectPause);
      }
    }

    setLogoBlockOpacity(1);
  }

  async function animateLogoReveal(
    token,
    { respectPause = false, revealRunId = ++logoRunId } = {},
  ) {
    const localLogoRunId = revealRunId;
    const logoSettings = stateLogoSettings[1];
    const revealDuration = Math.max(
      0,
      Number(logoSettings.revealDuration || 0),
    );
    const stutter = Math.max(0, Number(logoSettings.stutter || 0));
    const revealSteps = [
      [0, 0, 80],
      [4, 0, 120],
      [8, -1, 55],
      [7, 1, 45],
      [13, 0, 90],
      [19, 0, 70],
      [24, -1, 120],
      [31, 1, 55],
      [30, 0, 45],
      [38, 0, 105],
      [47, -1, 70],
      [55, 0, 130],
      [63, 1, 50],
      [62, 0, 45],
      [72, 0, 90],
      [81, -1, 70],
      [90, 0, 110],
      [96, 1, 55],
      [100, 0, 0],
    ];
    const totalHold = revealSteps.reduce((sum, step) => sum + step[2], 0);
    const durationScale = revealDuration / totalHold;
    const stutterMix = Math.min(1.8, stutter / 6);
    let highestReveal = 0;

    heroLogoReveal.classList.add("is-revealing");

    try {
      if (revealDuration <= 0) {
        setLogoReveal(100, 0);
        return;
      }

      for (const [percent, shift, hold] of revealSteps) {
        if (token !== runId || localLogoRunId !== logoRunId) {
          return;
        }

        highestReveal = Math.max(highestReveal, percent);
        const mixedPercent =
          highestReveal + (percent - highestReveal) * stutterMix;
        const mixedShift = shift * stutterMix * 2;
        setLogoReveal(mixedPercent, mixedShift);
        await waitLogoRevealHold(hold * durationScale, token, respectPause);
      }
    } catch (error) {
      if (error.message !== "Animation cancelled") {
        throw error;
      }
    } finally {
      if (localLogoRunId === logoRunId) {
        heroLogoReveal.classList.remove("is-revealing");
        setLogoReveal(100, 0);
      }
    }
  }

  async function playLogoReveal(options = {}, token = runId) {
    const logoSettings = stateLogoSettings[1];
    const delay = Math.max(
      0,
      Number(options.delay ?? logoSettings.revealDelay ?? 0),
    );
    const revealLogoRunId = ++logoRunId;

    syncLogoSettings();
    syncPowerOnVariables(getAnimationSettings(1));
    heroLogoReveal.classList.remove("is-revealing", "is-tube-warming");
    setLogoReveal(0, 0);
    setLogoBlockOpacity(0);

    if (delay > 0) {
      await waitLogoRevealHold(delay, token, options.respectPause);
    }

    if (token !== runId || revealLogoRunId !== logoRunId) {
      return;
    }

    heroLogoReveal.classList.add("is-visible");
    void heroLogoReveal.offsetWidth;

    const stateOneSettings = getAnimationSettings(1);
    let warmupTask = Promise.resolve();
    if (
      stateOneSettings.logoTubeWarmupEnabled &&
      Number(stateOneSettings.logoTubeWarmupDuration || 0) > 0
    ) {
      const warmupLogoRunId = revealLogoRunId;
      const playbackSpeed = Math.max(0.1, stateOneSettings.speed || 1);
      const warmupDuration =
        Math.max(0, Number(stateOneSettings.logoTubeWarmupDuration || 0)) /
        playbackSpeed;
      heroLogoReveal.classList.add("is-tube-warming");
      window.setTimeout(() => {
        if (warmupLogoRunId === logoRunId) {
          heroLogoReveal.classList.remove("is-tube-warming");
        }
      }, warmupDuration);
      warmupTask = waitLogoRevealHold(
        Number(stateOneSettings.logoTubeWarmupDuration || 0),
        token,
        options.respectPause,
      );
    }

    await Promise.all([
      animateLogoBlockOpacity(token, {
        ...options,
        revealRunId: revealLogoRunId,
      }),
      animateLogoReveal(token, { ...options, revealRunId: revealLogoRunId }),
      warmupTask,
    ]);
  }

  function showLogoReveal(percent = 100) {
    syncLogoSettings();
    heroLogoReveal.classList.add("is-visible");
    heroLogoReveal.classList.remove("is-revealing", "is-tube-warming");
    setLogoBlockOpacity(1);
    setLogoReveal(percent, 0);
  }

  function hideLogoReveal() {
    heroLogoReveal.classList.remove(
      "is-visible",
      "is-revealing",
      "is-tube-warming",
    );
    setLogoBlockOpacity(0);
    setLogoReveal(0, 0);
  }

  function setScaledProperty(name, value) {
    setRootVar(name, `${value}px`);
  }

  function syncTextLayers() {
    Object.entries(sharedTextLayers).forEach(([key, layer]) => {
      setScaledProperty(`--${key}-left`, layer.x);
      setScaledProperty(`--${key}-top`, layer.y);
      setScaledProperty(`--${key}-width`, layer.width);
      setScaledProperty(`--${key}-font-size`, layer.size);
      setRootVar(`--${key}-letter-spacing`, `${layer.letterSpacing ?? 0}px`);
      setRootVar(`--${key}-opacity`, layer.opacity);
    });

    setRootVar(
      "--build-list-indent",
      `${sharedTextLayers.build.listIndent ?? 0}px`,
    );
    setRootVar(
      "--build-list-offset-y",
      `${sharedTextLayers.build.listY ?? 0}px`,
    );
    setRootVar("--build-row-gap", `${sharedTextLayers.build.rowGap ?? 8}px`);
    setRootVar("--build-star-scale", sharedTextLayers.build.starSize ?? 1);
    setRootVar("--build-bullet-scale", sharedTextLayers.build.bulletSize ?? 1);
    setRootVar("--build-bullet-x", `${sharedTextLayers.build.bulletX ?? 0}px`);
    setRootVar("--build-bullet-y", `${sharedTextLayers.build.bulletY ?? 0}px`);
    setRootVar("--final-block-x", `${sharedTextLayers.final.blockX ?? 0}px`);
    setRootVar("--final-block-y", `${sharedTextLayers.final.blockY ?? 0}px`);
    setRootVar(
      "--final-check-spark-x",
      sharedTextLayers.final.checkSparkX ?? 1,
    );
    setRootVar(
      "--final-check-spark-y",
      sharedTextLayers.final.checkSparkY ?? 1,
    );
    setRootVar(
      "--final-link-indent",
      `${sharedTextLayers.final.linkIndent ?? 0}px`,
    );
    setRootVar(
      "--final-check-size",
      `${sharedTextLayers.final.checkSize ?? sharedTextLayers.final.size ?? 18}px`,
    );
    setRootVar("--final-check-x", `${sharedTextLayers.final.checkX ?? 0}px`);
    setRootVar("--final-check-y", `${sharedTextLayers.final.checkY ?? 0}px`);
    setRootVar(
      "--final-link-offset-y",
      `${sharedTextLayers.final.linkY ?? 1}px`,
    );
    setRootVar(
      "--final-link-size",
      `${sharedTextLayers.final.linkSize ?? 22}px`,
    );
    setRootVar(
      "--final-link-line-height",
      sharedTextLayers.final.linkLineHeight ?? 1.5,
    );
    setRootVar(
      "--final-link-letter-spacing",
      `${sharedTextLayers.final.linkLetterSpacing ?? -0.36}px`,
    );
    setRootVar("--final-prompt-x", `${sharedTextLayers.final.promptX ?? 0}px`);
    setRootVar("--final-prompt-y", `${sharedTextLayers.final.promptY ?? 0}px`);
    setRootVar(
      "--final-prompt-size",
      `${sharedTextLayers.final.promptSize ?? sharedTextLayers.final.size ?? 18}px`,
    );
    setRootVar(
      "--final-link-reveal-duration",
      `${sharedTextLayers.final.linkRevealDuration ?? 160}ms`,
    );
  }

  function resetFinalAscii() {
    finalAsciiRunId += 1;
    finalLink?.classList.remove("is-ascii-revealing");
    finalStatus?.classList.remove("is-final-sequencing");
    finalDone?.classList.remove("is-final-done-visible");
    finalCheck?.classList.remove("is-hellspark");
    finalPrompt?.classList.remove("is-prompt-visible");
    if (finalLink) {
      finalLink.classList.remove("is-link-visible");
      finalLink.textContent = finalState.url;
    }
    if (finalAscii) {
      finalAscii.textContent = "";
    }
  }

  async function playFinalAscii(token = runId) {
    if (!finalLink) {
      return;
    }

    const localAsciiRunId = ++finalAsciiRunId;
    const target = finalState.url;
    const layer = sharedTextLayers.final;
    const duration = Math.max(0, Number(layer.linkAsciiDuration ?? 900));
    const noise = Math.max(0, Math.round(Number(layer.linkAsciiNoise ?? 8)));
    const hold = Math.max(0, Number(layer.linkAsciiHold ?? 180));
    const glyphs = "01#@$%&*+=<>[]{}:/\\|-_~^";
    const fillGlyphs = ["░", "▒", "▓", "█"];

    if (duration <= 0 || noise <= 0) {
      finalLink.textContent = target;
      finalPrompt?.classList.add("is-prompt-visible");
      return;
    }

    finalLink.classList.add("is-ascii-revealing");
    const frameCount = Math.max(18, Math.round(duration / 34));

    for (let frame = 0; frame <= frameCount; frame += 1) {
      if (token !== runId || localAsciiRunId !== finalAsciiRunId) {
        return;
      }

      const progress = frame / frameCount;
      const resolved = Math.floor(progress * target.length);
      const activeWindow = Math.max(
        2,
        Math.min(noise, target.length - resolved),
      );
      finalLink.textContent = [...target]
        .map((character, index) => {
          if (index < resolved) {
            return character;
          }

          if (index < resolved + activeWindow) {
            return fillGlyphs[(frame + index) % fillGlyphs.length];
          }

          if (
            character === ":" ||
            character === "/" ||
            character === "." ||
            character === "-"
          ) {
            return character;
          }

          return glyphs[(frame * 7 + index * 3) % glyphs.length];
        })
        .join("");
      await rawWait(Math.max(18, duration / frameCount));
    }

    if (token === runId && localAsciiRunId === finalAsciiRunId) {
      finalLink.textContent = target;
      finalLink.classList.add("is-link-visible");
      await rawWait(hold);
      finalLink.classList.remove("is-ascii-revealing");
      finalPrompt?.classList.add("is-prompt-visible");
    }
  }

  function showFinalLinkResolved() {
    finalStatus?.classList.remove("is-final-sequencing");
    finalDone?.classList.add("is-final-done-visible");
    finalCheck?.classList.remove("is-hellspark");
    if (finalLink) {
      finalLink.classList.add("is-link-visible");
      finalLink.textContent = finalState.url;
    }
    finalPrompt?.classList.add("is-prompt-visible");
  }

  async function playFinalReveal(token = runId) {
    const layer = sharedTextLayers.final;
    const linkRevealDelay = Math.max(0, Number(layer.linkRevealDelay ?? 620));

    resetFinalAscii();
    finalStatus.classList.add("is-visible", "is-final-sequencing");
    finalDone?.classList.add("is-final-done-visible");
    if (finalCheck) {
      finalCheck.classList.remove("is-hellspark");
      void finalCheck.offsetWidth;
      finalCheck.classList.add("is-hellspark");
    }

    await rawWait(FINAL_CHECK_SEQUENCE_DURATION + linkRevealDelay);

    if (token !== runId) {
      throw new Error("Animation cancelled");
    }

    finalLink?.classList.add("is-link-visible");
    await playFinalAscii(token);
    finalStatus?.classList.remove("is-final-sequencing");
    finalCheck?.classList.remove("is-hellspark");
  }

  function setBuildTitleDots(count = 3) {
    buildTitleCopy.textContent = `${copy.buildTitle}${".".repeat(count)}`;
  }

  function shuffledIndexes(length) {
    const indexes = Array.from({ length }, (_, index) => index);
    for (let index = indexes.length - 1; index > 0; index -= 1) {
      const swapIndex = Math.floor(Math.random() * (index + 1));
      [indexes[index], indexes[swapIndex]] = [
        indexes[swapIndex],
        indexes[index],
      ];
    }
    return indexes;
  }

  function pulseTerminalBuildElement(element, duration = 240) {
    if (!element) {
      return;
    }

    element.classList.remove("is-terminal-tick");
    void element.offsetWidth;
    element.classList.add("is-terminal-tick");
    window.setTimeout(
      () => element.classList.remove("is-terminal-tick"),
      duration,
    );
  }

  function syncStatusLeaderDots(row) {
    const leader = row.querySelector(".status-leader");
    if (!leader) {
      return [];
    }

    const width =
      leader.offsetWidth || leader.getBoundingClientRect().width || 320;
    const leaderStyles = window.getComputedStyle(leader);
    const reservedStatusGap =
      Number.parseFloat(leaderStyles.paddingRight || "0") || 0;
    const drawableWidth = Math.max(24, width - reservedStatusGap);
    const dotCount = Math.max(16, Math.floor(drawableWidth / 8));
    if (Number(leader.dataset.dotCount) !== dotCount) {
      leader.dataset.dotCount = String(dotCount);
      leader.replaceChildren(
        ...Array.from({ length: dotCount }, () => {
          const dot = document.createElement("span");
          dot.className = "status-leader-dot";
          return dot;
        }),
      );
    }

    return [...leader.querySelectorAll(".status-leader-dot")];
  }

  function resetStatusLeaderDots(row) {
    syncStatusLeaderDots(row).forEach((dot) => dot.classList.remove("is-on"));
  }

  function fillStatusLeaderDotsImmediately(row) {
    syncStatusLeaderDots(row).forEach((dot) => dot.classList.add("is-on"));
  }

  function setStatusLeaderProgress(row, progress) {
    const dots = syncStatusLeaderDots(row);
    const fillTo = Math.min(
      dots.length,
      Math.ceil(dots.length * Math.max(0, Math.min(1, progress))),
    );
    dots.forEach((dot, index) => dot.classList.toggle("is-on", index < fillTo));
  }

  async function fillStatusLeaderDots(
    row,
    token,
    duration = 320,
    options = {},
  ) {
    const dots = syncStatusLeaderDots(row);
    if (!dots.length) {
      return;
    }

    dots.forEach((dot) => dot.classList.remove("is-on"));
    const order = dots.map((_, index) => index);
    const stepCount = Math.max(1, Math.ceil(dots.length / 4));
    const baseStep = Math.max(18, duration / stepCount);
    const cadence = [0.55, 0.95, 0.72, 1.2, 0.68];

    for (let cursor = 0; cursor < order.length;) {
      if (token !== runId) {
        throw new Error("Animation cancelled");
      }

      const burstSize = cursor % 12 === 0 ? 5 : 4;
      order
        .slice(cursor, cursor + burstSize)
        .forEach((index) => dots[index].classList.add("is-on"));
      cursor += burstSize;

      const delay = Math.min(74, baseStep * cadence[cursor % cadence.length]);
      await wait(delay, token);
    }

    dots.forEach((dot) => dot.classList.add("is-on"));
  }

  function getBuildCompletionOrder() {
    return shuffledIndexes(statusRows.length);
  }

  function resetBuildStatus({ final = false } = {}) {
    buildStatus?.classList.remove("is-terminal-active");
    setBuildTitleDots(3);
    statusRows.forEach((row, index) => {
      row.classList.remove("is-muted", "is-complete", "is-terminal-tick");
      resetStatusLeaderDots(row);
      if (final) {
        row.classList.add("is-complete");
        fillStatusLeaderDotsImmediately(row);
      }
    });
    statusValues.forEach((value, index) => {
      const isOk = final;
      value.textContent = isOk ? copy.statusOk : copy.statusInProgress;
      value.classList.toggle("status-ok", isOk);
      value.classList.remove("is-switching", "is-terminal-tick");
      value.dataset.trail = "";
    });
  }

  function startBuildDots(token) {
    let dotCount = 0;
    setBuildTitleDots(1);
    const interval = window.setInterval(() => {
      if (token !== runId) {
        window.clearInterval(interval);
        return;
      }

      dotCount = (dotCount % 3) + 1;
      setBuildTitleDots(dotCount);
    }, 180);

    return () => window.clearInterval(interval);
  }

  function makeStatusAsciiFrame(step, steps, width) {
    const size = Math.max(3, Math.min(18, Math.round(width || 7)));
    const safeSteps = Math.max(1, steps - 1);
    const head = Math.round((step / safeSteps) * (size - 1));
    const glyphs = ["░", "▒", "▓", "█"];
    const cells = Array.from({ length: size }, (_, index) => {
      const distance = Math.abs(index - head);
      if (distance === 0) {
        return glyphs[3];
      }
      if (distance === 1) {
        return glyphs[2];
      }
      if (distance === 2) {
        return glyphs[1];
      }
      return glyphs[0];
    });
    return `[${cells.join("")}]`;
  }

  async function switchStatusToOk(value, token) {
    const stateThreeSettings = getAnimationSettings(3);
    const stateFourSettings = getAnimationSettings(4);
    const asciiDelay = Math.max(
      0,
      Number(stateThreeSettings.statusAsciiDelay ?? 0),
    );
    const revealDuration = Math.max(
      40,
      Number(stateThreeSettings.statusAsciiSpeed ?? 160),
    );
    const dotBurstDuration = Math.max(
      40,
      Number(stateFourSettings.statusDotBurstDuration ?? revealDuration),
    );
    const row = value.closest(".status-row");
    if (!row) {
      return;
    }

    setStatusLeaderProgress(row, 1);
    row?.classList.add("is-complete");
    if (asciiDelay > 0) {
      await wait(asciiDelay, token);
    }
    value.classList.add("status-ok", "is-switching");
    value.style.setProperty(
      "--status-switch-duration",
      `${Math.round(revealDuration)}ms`,
    );
    const steps = Math.max(
      2,
      Math.min(6, Math.round(stateThreeSettings.statusOkSteps || 4)),
    );
    const asciiWindow = Math.max(
      3,
      Math.min(18, Math.round(stateThreeSettings.statusAsciiWindow || 7)),
    );
    value.style.setProperty("--status-ok-steps", steps);
    const dots = syncStatusLeaderDots(row);
    const totalDuration = Math.max(revealDuration, dotBurstDuration);
    const frameCount = Math.max(steps, Math.ceil(totalDuration / 28));
    const frameDuration = Math.max(20, totalDuration / frameCount);

    for (let frame = 0; frame < frameCount; frame += 1) {
      if (token !== runId) {
        throw new Error("Animation cancelled");
      }

      const elapsed =
        frameCount <= 1
          ? totalDuration
          : (totalDuration * frame) / (frameCount - 1);
      const asciiProgress = Math.max(0, Math.min(1, elapsed / revealDuration));
      const dotProgress = Math.max(0, Math.min(1, elapsed / dotBurstDuration));
      const asciiStep = Math.min(steps - 1, Math.floor(asciiProgress * steps));
      const dotStep = Math.min(steps - 1, Math.floor(dotProgress * steps));

      value.textContent =
        asciiProgress >= 1
          ? copy.statusOk
          : makeStatusAsciiFrame(asciiStep, steps, asciiWindow);
      value.dataset.trail = "";
      setStatusLeaderProgress(row, 1);
      dots.forEach((dot, index) => {
        const gradientIndex = (index + dotStep) % 3;
        dot.style.background =
          gradientIndex === 0
            ? "rgba(0, 189, 134, 0.82)"
            : gradientIndex === 1
              ? "rgba(244, 189, 23, 0.66)"
              : "rgba(174, 176, 188, 0.72)";
      });
      await wait(frameDuration, token);
    }

    dots.forEach((dot) => {
      dot.classList.add("is-on");
      dot.style.removeProperty("background");
    });
    value.textContent = copy.statusOk;
    value.dataset.trail = "";
    value.classList.remove("is-switching");
    value.style.removeProperty("--status-switch-duration");
    value.style.removeProperty("--status-ok-steps");
  }

  function hideAppPreviewShell() {
    appPreview.classList.remove(
      "is-visible",
      "is-wire",
      "is-polished",
      "is-image-backed",
      "is-prototype-visible",
      "is-section-reveal-gated",
      "terminal-window-enter",
      "app-reveal-running",
      "reveal-target-prototype",
      "reveal-target-polished",
      "reveal-colorWave",
      "reveal-pixelTiles",
      "reveal-pixelate",
      "reveal-scanSweep",
      "reveal-crtBloom",
    );
    appPreview.classList.remove(
      "app-boot-running",
      "app-boot-finalizing",
      "app-boot-scaffolding",
      "app-boot-final-frame-ready",
      "app-boot-clip-enabled",
    );
    appRevealOverlay.replaceChildren();
  }

  function resetStage() {
    logoRunId += 1;
    stage.classList.remove(
      "terminal-scroll",
      "app-focus-active",
      "logo-inspect-active",
      "power-on-active",
      "power-on-primed",
    );
    resetTerminalScrollStyles();
    resetFinalStatusScrollStyles();
    hideLogoReveal();
    clearAppWindowEchoes();
    clearPrototypeBuild();
    clearWindowBoot();
    hideAppPreviewShell();
    installText.textContent = "";
    installText.classList.add("is-complete");
    promptLayer.classList.remove(
      "is-visible",
      "is-submitted",
      "is-entering",
      "is-typing",
    );
    promptText.textContent = "";
    promptText.classList.remove("is-waiting");
    buildLines.forEach((line) => line.classList.remove("is-visible"));
    resetBuildStatus();
    resetFinalAscii();
    finalStatus.classList.remove("is-visible");
  }

  async function playPowerOn(stateSettings, token, options = {}) {
    const hideLogoOnComplete = options.hideLogoOnComplete ?? true;
    const resetLogoAtStart = options.resetLogoAtStart ?? true;
    const powerDuration = Math.max(
      0,
      Number(stateSettings.powerOnDuration || 0),
    );
    if (!stateSettings.powerOnEnabled || powerDuration <= 0) {
      if (hideLogoOnComplete && resetLogoAtStart) {
        hideLogoReveal();
      }
      return;
    }

    const lineHold = getPowerOnLineHold(stateSettings, powerDuration);
    const activeDuration = getPowerOnActiveDuration(
      stateSettings,
      powerDuration,
      lineHold,
    );
    const lineDuration = getPowerOnLineDuration(stateSettings, activeDuration);
    const lineExpandDuration = getPowerOnLineExpandDuration(
      stateSettings,
      lineDuration,
    );
    const yScaleDuration = getPowerOnYScaleDuration(
      stateSettings,
      lineDuration,
    );
    const activeWait = Math.max(
      activeDuration,
      lineDuration,
      lineExpandDuration + yScaleDuration,
    );
    setPowerOnVariables(stateSettings, activeDuration, lineDuration);
    if (resetLogoAtStart) {
      hideLogoReveal();
    }
    installText.textContent = "";
    promptLayer.classList.remove(
      "is-visible",
      "is-submitted",
      "is-entering",
      "is-typing",
    );
    promptText.textContent = "";
    buildLines.forEach((line) => line.classList.remove("is-visible"));
    finalStatus.classList.remove("is-visible");

    try {
      if (lineHold > 0) {
        stage.classList.add("power-on-primed");
        await wait(lineHold, token);
        stage.classList.remove("power-on-primed");
      }

      stage.classList.remove("power-on-active");
      void powerOnOverlay.offsetWidth;
      stage.classList.add("power-on-active");
      await wait(activeWait, token);
    } finally {
      stage.classList.remove("power-on-active", "power-on-primed");
      if (token === runId && hideLogoOnComplete) {
        hideLogoReveal();
      }
    }
  }

  async function wait(duration, token) {
    let remaining = duration / Math.max(0.1, getAnimationSettings().speed);

    while (remaining > 0) {
      if (token !== runId) {
        throw new Error("Animation cancelled");
      }

      if (settings.paused) {
        await rawWait(60);
        continue;
      }

      const chunk = Math.min(remaining, 50);
      const startedAt = performance.now();
      await rawWait(chunk);
      remaining -= performance.now() - startedAt;
    }
  }

  async function typeInto(
    target,
    text,
    token,
    charDelay,
    lineDelay = charDelay,
    options = {},
  ) {
    target.textContent = "";
    const startDelay = Math.max(0, Number(options.startDelay || 0));
    target.classList.remove("is-waiting");
    if (startDelay > 0) {
      target.classList.add("is-waiting");
      try {
        await wait(startDelay, token);
      } finally {
        target.classList.remove("is-waiting");
      }
    }

    let textOffset = 0;
    const typingTokens = getTypingTokens(text, options.mode);

    for (const [index, tokenText] of typingTokens.entries()) {
      if (token !== runId) {
        throw new Error("Animation cancelled");
      }

      target.textContent += tokenText;
      await wait(
        getTypingDelay(
          text,
          tokenText,
          index,
          textOffset,
          charDelay,
          lineDelay,
          options.mode,
          {
            human: options.human,
            contrast: options.contrast,
          },
        ),
        token,
      );
      textOffset += tokenText.length;
    }
  }

  function prepareStateForAnimation(state) {
    resetStage();
    stage.dataset.currentState = String(state);

    if (state >= 2) {
      installText.textContent = installFrames[installFrames.length - 1];
      installText.classList.add("is-complete");
      showLogoReveal(100);
    }

    if (state >= 3) {
      promptLayer.classList.add("is-visible", "is-submitted");
      promptLayer.classList.remove("is-typing");
      promptText.textContent = promptCopy;
    }

    if (state >= 4) {
      resetBuildStatus({ final: true });
      buildLines.forEach((line) => line.classList.add("is-visible"));
      statusRows.forEach(fillStatusLeaderDotsImmediately);
    }

    if (state >= 5) {
      setPreAppFocus(true, false);
      installText.textContent = "";
      promptLayer.classList.remove(
        "is-visible",
        "is-submitted",
        "is-entering",
        "is-typing",
      );
      promptText.textContent = "";
      buildLines.forEach((line) => line.classList.remove("is-visible"));
      finalStatus.classList.add("is-visible");
      showFinalLinkResolved();
    }

    if (state >= 6) {
      appPreview.classList.add(
        "is-visible",
        "is-wire",
        "is-image-backed",
        "is-prototype-visible",
      );
    }
  }

  async function playPromptEnter(token) {
    promptLayer.classList.add("is-entering");
    try {
      await wait(220, token);
      promptLayer.classList.add("is-submitted");
      await wait(60, token);
    } finally {
      promptLayer.classList.remove("is-entering");
    }
  }

  async function playInstallTextTrack(stateOneAnimation, token) {
    installText.classList.remove("is-complete");
    installText.textContent = "";

    const startAt = Math.max(
      0,
      Number(stateOneAnimation.installTextDelay || 0),
    );
    await wait(startAt, token);
    triggerTextGlitch();

    let previousText = "";

    for (const frame of installFrames) {
      const nextText = frame.startsWith(previousText)
        ? frame.slice(previousText.length)
        : frame;

      if (!frame.startsWith(previousText)) {
        installText.textContent = "";
        previousText = "";
      }

      for (const character of nextText) {
        installText.textContent += character;
        await wait(
          character === "\n"
            ? stateOneAnimation.installFramePause
            : stateOneAnimation.installType,
          token,
        );
      }

      previousText = frame;
      await wait(stateOneAnimation.installFramePause, token);
    }

    installText.classList.add("is-complete");
  }

  async function runStateAnimation(state, token) {
    const stateSettings = getAnimationSettings(state);
    const stateDelayBefore = Math.max(
      0,
      Number(stateSettings.delayBefore || 0),
    );

    updateStateReadout(state);
    animateTimelineState(state, getStateDuration(state));

    if (state !== 1) {
      await wait(stateDelayBefore, token);
    }

    if (state === 1) {
      const stateOneAnimation = stateSettings;
      const logoDelay = Math.max(
        0,
        Number(stateLogoSettings[1]?.revealDelay || 0),
      );
      let installTextError = null;
      const installTextTask = playInstallTextTrack(
        stateOneAnimation,
        token,
      ).catch((error) => {
        installTextError = error;
      });

      await wait(stateDelayBefore, token);
      hideLogoReveal();
      const powerOnTask = playPowerOn(stateOneAnimation, token, {
        hideLogoOnComplete: false,
        resetLogoAtStart: false,
      });
      const logoRevealTask = playLogoReveal(
        { respectPause: true, delay: logoDelay },
        token,
      );
      await Promise.all([powerOnTask, logoRevealTask, installTextTask]);
      if (installTextError) {
        throw installTextError;
      }
    }

    if (state === 2) {
      showLogoReveal(100);
      syncPromptCursorTiming(getAnimationSettings(2));
      promptLayer.classList.add("is-visible");
      promptLayer.classList.remove("is-submitted", "is-entering");
      promptLayer.classList.add("is-typing");
      const promptSettings = getAnimationSettings(2);
      await typeInto(
        promptText,
        promptCopy,
        token,
        promptSettings.promptType,
        promptSettings.promptLinePause,
        {
          mode: promptSettings.promptTypingMode,
          startDelay: getPromptStartDelay(promptSettings),
          human: true,
          contrast: promptSettings.promptTypingContrast,
        },
      );
      if (promptSettings.promptEnterMoment) {
        await wait(70, token);
        promptLayer.classList.remove("is-typing");
        await playPromptEnter(token);
      }
      triggerTextGlitch();
    }

    if (state === 3) {
      resetBuildStatus();
      const stopDots = startBuildDots(token);
      try {
        buildStatus?.classList.add("is-terminal-active");
        buildTitle.classList.add("is-visible");
        pulseTerminalBuildElement(buildTitle, 320);
        await wait(getAnimationSettings(3).buildLineReveal, token);

        for (const [index, row] of statusRows.entries()) {
          row.classList.add("is-visible");
          pulseTerminalBuildElement(row, 260);
          await fillStatusLeaderDots(
            row,
            token,
            getAnimationSettings(3).buildLineReveal * 0.8,
          );
          await wait(
            Math.min(46, getAnimationSettings(3).buildLineReveal * 0.07),
            token,
          );
        }

        for (const index of getBuildCompletionOrder()) {
          await wait(
            Math.min(34, getAnimationSettings(3).buildLineReveal * 0.05),
            token,
          );
          await switchStatusToOk(statusValues[index], token);
        }
      } finally {
        stopDots();
        buildStatus?.classList.remove("is-terminal-active");
        setBuildTitleDots(3);
      }
      triggerTextGlitch();
    }

    if (state === 4) {
      hideAppPreviewShell();
      const stateFourSettings = getAnimationSettings(4);
      let finalRevealTask = null;
      if (stateFourSettings.terminalScroll) {
        const scrollDuration = getTerminalScrollDuration(stateFourSettings);
        installText.textContent = installFrames[installFrames.length - 1];
        installText.classList.add("is-complete");
        promptLayer.classList.add("is-visible", "is-submitted");
        promptLayer.classList.remove("is-typing");
        promptText.textContent = promptCopy;
        resetBuildStatus({ final: true });
        buildLines.forEach((line) => line.classList.add("is-visible"));
        statusRows.forEach(fillStatusLeaderDotsImmediately);
        buildStatus?.classList.add("is-terminal-active");
        finalRevealTask = playFinalReveal(token);
        await playTerminalOutputScroll(token, scrollDuration);
      }

      installText.textContent = "";
      promptLayer.classList.remove(
        "is-visible",
        "is-submitted",
        "is-entering",
        "is-typing",
      );
      promptText.textContent = "";
      buildLines.forEach((line) => line.classList.remove("is-visible"));
      await (finalRevealTask || playFinalReveal(token));
      window.setTimeout(() => {
        stage.classList.remove("terminal-scroll");
        buildStatus?.classList.remove("is-terminal-active");
        resetTerminalScrollStyles();
      }, 180);
      triggerTextGlitch();
    }

    if (state === 5) {
      setPreAppFocus(true, false);
      appPreview.classList.add("is-visible", "is-wire", "is-image-backed");
      appPreview.classList.remove("is-polished", "is-prototype-visible");
      clearPrototypeBuild();
      clearWindowBoot();
      clearAppReveal();
      gateAppPreviewSection();
      const finalStatusScroll = finalStatus.classList.contains("is-visible")
        ? playFinalStatusScrollDown(
            token,
            getTerminalScrollDuration(getAnimationSettings(4)),
          )
        : Promise.resolve();
      triggerWindowBoot({ autoFinish: false });
      const bootFrameReadyDelay = getWindowBootFrameReadyDelay(
        getAnimationSettings(5),
      );
      if (bootFrameReadyDelay > 0) {
        await wait(bootFrameReadyDelay, token);
      }
      finalStatus.classList.remove("is-visible");
      resetFinalStatusScrollStyles();
      await Promise.all([
        playAppRevealOverWindowBoot(token, "prototype", 5),
        finalStatusScroll,
      ]);
    }

    if (state === 6) {
      appPreview.classList.add(
        "is-visible",
        "is-wire",
        "is-image-backed",
        "is-prototype-visible",
      );
      appPreview.classList.remove("is-polished");
      clearWindowBoot();
      clearPrototypeBuild();
      clearAppReveal();
      await playAppReveal(token, "polished", 6);
    }

    await wait(getAnimationSettings(state).delayAfter, token);
  }

  function syncSharedAppPositionCss() {
    setRootVar("--app-left", `${sharedAppSettings.left}px`);
    setRootVar("--app-top", `${sharedAppSettings.top}px`);
    setRootVar("--app-scale", sharedAppSettings.scale);
  }

  function syncStaticAppCss() {
    const cssMap = {
      "--app-window-width": staticAppSettings.windowWidth,
      "--app-window-height": staticAppSettings.windowHeight,
      "--app-sidebar-width": staticAppSettings.sidebarWidth,
      "--app-content-pad-top": staticAppSettings.contentPadTop,
      "--app-content-pad-right": staticAppSettings.contentPadRight,
      "--app-content-pad-bottom": staticAppSettings.contentPadBottom,
      "--app-content-pad-left": staticAppSettings.contentPadLeft,
      "--app-header-margin-bottom": staticAppSettings.headerMarginBottom,
      "--chart-plot-top": staticAppSettings.chartPlotY,
      "--app-title-size": staticAppSettings.appTitleSize,
      "--app-subtitle-size": staticAppSettings.appSubtitleSize,
      "--app-shared-title-size": staticAppSettings.sharedTitleSize,
      "--app-shared-subtitle-size": staticAppSettings.sharedSubtitleSize,
    };

    Object.entries(cssMap).forEach(([name, value]) => {
      setRootVar(name, `${value}px`);
    });

    updateAppChartHover();
  }

  function setPreAppFocus(active, restart = false) {
    stage.classList.toggle("app-focus-active", active);

    if (active && restart && preAppScene) {
      preAppScene.style.animation = "none";
      void preAppScene.offsetWidth;
      preAppScene.style.animation = "";
    }
  }

  const terminalHoverTextSelector = [
    "#installText",
    "#promptText",
    ".build-title-copy",
    ".status-label",
    ".status-value",
    ".final-done",
    ".final-link",
    ".final-prompt",
    ".prototype-build-token",
    ".address-bar",
    ".app-header h2",
    ".app-header p",
    ".metric-icon",
    ".metric-change",
    ".metric-card p",
    ".metric-card strong",
    ".panel-title h3",
    ".panel-title p",
    ".panel-title span",
    ".region-panel h3",
    ".region-panel p",
    ".region-row span",
    ".region-row b",
    ".region-row small",
    ".chart-y-labels span",
    ".chart-months span",
    ".chart-tooltip",
    ".chart-footer span",
  ].join(",");

  function syncPreAppFocusCss() {
    const scaleDelta = 1 - preAppFocusSettings.scale;
    setRootVar("--pre-app-opacity", preAppFocusSettings.opacity);
    setRootVar("--pre-app-scale", preAppFocusSettings.scale);
    setRootVar("--pre-app-scale-step-1", 1 - scaleDelta * 0.35);
    setRootVar("--pre-app-scale-step-2", 1 - scaleDelta * 0.55);
    setRootVar("--pre-app-scale-step-3", 1 - scaleDelta * 0.75);
    setRootVar("--pre-app-scale-step-4", 1 - scaleDelta * 0.92);
    setRootVar("--pre-app-duration", `${preAppFocusSettings.duration}ms`);
    setRootVar("--pre-app-steps", preAppFocusSettings.steps);
    setRootVar(
      "--pre-app-easing",
      `steps(${Math.max(1, Math.round(preAppFocusSettings.steps))}, end)`,
    );
  }

  const APP_REVEAL_NODE_SELECTOR = [
    ".app-reveal-pixel",
    ".app-reveal-mask-tile",
    ".app-reveal-color-bit",
    ".app-reveal-type-token",
    ".app-reveal-image-mask",
    ".app-reveal-chart-overlay",
    ".app-reveal-solid-tile",
    ".app-reveal-scan-tile",
    ".app-reveal-frame-echo",
  ].join(", ");

  function clearAppRevealOverlayNodes() {
    appRevealOverlay
      .querySelectorAll(APP_REVEAL_NODE_SELECTOR)
      .forEach((node) => node.remove());
  }

  function buildAppRevealPixels(settings, { preserveExisting = false } = {}) {
    if (preserveExisting) {
      clearAppRevealOverlayNodes();
    } else {
      appRevealOverlay.replaceChildren();
    }
    const grid = Math.max(4, Math.round(settings.grid));
    const totalPixels = grid * grid;
    const indexes = Array.from({ length: totalPixels }, (_, index) => index);
    const originPoint = getRevealOriginPoint(settings);
    const maxDistance = originPoint.maxDistance || Math.SQRT2;

    indexes.sort((a, b) => {
      const aRow = Math.floor(a / grid);
      const aColumn = a % grid;
      const bRow = Math.floor(b / grid);
      const bColumn = b % grid;
      const aX = (aColumn + 0.5) / grid;
      const aY = (aRow + 0.5) / grid;
      const bX = (bColumn + 0.5) / grid;
      const bY = (bRow + 0.5) / grid;
      const aDistance = Math.hypot(
        aX - originPoint.column,
        aY - originPoint.row,
      );
      const bDistance = Math.hypot(
        bX - originPoint.column,
        bY - originPoint.row,
      );
      return aDistance - bDistance;
    });
    indexes.forEach((pixelIndex, order) => {
      const row = Math.floor(pixelIndex / grid);
      const column = pixelIndex % grid;
      const centerX = (column + 0.5) / grid;
      const centerY = (row + 0.5) / grid;
      const distance = Math.hypot(
        centerX - originPoint.column,
        centerY - originPoint.row,
      );
      const wave = clamp(distance / maxDistance, 0, 1);
      const pixel = document.createElement("span");
      pixel.className = "app-reveal-pixel";
      pixel.style.width = `${100 / grid}%`;
      pixel.style.height = `${100 / grid}%`;
      pixel.style.left = `${column * (100 / grid)}%`;
      pixel.style.top = `${row * (100 / grid)}%`;
      pixel.style.animationDelay = `${(wave * settings.duration * 0.55 + (order % grid) * 2).toFixed(1)}ms`;
      appRevealOverlay.append(pixel);
    });
  }

  function getRevealOriginPoint(settings = {}) {
    const corner = settings.corner || "tl";

    if (corner === "custom") {
      const column = clamp(Number(settings.originX ?? 50) / 100, 0, 1);
      const row = clamp(Number(settings.originY ?? 50) / 100, 0, 1);
      const maxDistance = Math.max(
        Math.hypot(column, row),
        Math.hypot(1 - column, row),
        Math.hypot(column, 1 - row),
        Math.hypot(1 - column, 1 - row),
        0.001,
      );
      return { column, row, maxDistance };
    }

    if (corner === "center") {
      return { column: 0.5, row: 0.5, maxDistance: Math.SQRT1_2 };
    }

    const column = corner === "tr" || corner === "br" ? 1 : 0;
    const row = corner === "bl" || corner === "br" ? 1 : 0;
    return { column, row, maxDistance: Math.SQRT2 };
  }

  function cloneChartOverlayForReveal() {
    if (!appChartOverlay) {
      return null;
    }

    updateAppChartHover();
    const clone = appChartOverlay.cloneNode(true);
    clone.classList.add("app-reveal-chart-overlay");
    clone.setAttribute("aria-hidden", "true");
    [
      "--chart-hover-x",
      "--chart-hover-revenue-y",
      "--chart-hover-target-y",
      "--chart-card-x",
      "--chart-card-y",
    ].forEach((name) => {
      const value = appChartOverlay.style.getPropertyValue(name);
      if (value) {
        clone.style.setProperty(name, value);
      }
    });
    return clone;
  }

  function buildAppColorWave(
    settings,
    { preserveExisting = false, includeChart = false } = {},
  ) {
    if (preserveExisting) {
      clearAppRevealOverlayNodes();
    } else {
      appRevealOverlay.replaceChildren();
    }
    const grid = Math.max(4, Math.round(Number(settings.grid || 14)));
    const intensity = Math.max(0, Number(settings.intensity || 0));
    const duration = Math.max(0, Number(settings.duration || 980));
    const tileDuration = Math.max(
      140,
      Math.min(420, duration * (0.22 + intensity * 0.012)),
    );
    const cornerPoint = getRevealOriginPoint(settings);
    const previewWidth = appPreview.offsetWidth || 768;
    const previewHeight = appPreview.offsetHeight || 514;
    const maxDistance = cornerPoint.maxDistance || Math.SQRT2;
    const revealImage = document.createElement("div");

    revealImage.className = "app-reveal-image-mask";
    revealImage.style.animationDuration = `${Math.max(160, duration).toFixed(1)}ms`;
    if (includeChart) {
      const chartClone = cloneChartOverlayForReveal();
      if (chartClone) {
        revealImage.append(chartClone);
      }
    }
    appRevealOverlay.append(revealImage);

    const appendScanTile = (rect, className, order = 0, durationScale = 1) => {
      const centerX = (rect.x + rect.w / 2) / 100;
      const centerY = (rect.y + rect.h / 2) / 100;
      const distance = Math.sqrt(
        (centerX - cornerPoint.column) ** 2 + (centerY - cornerPoint.row) ** 2,
      );
      const wave = clamp(distance / maxDistance, 0, 1);
      const tile = document.createElement("span");
      const delay = wave * Math.max(0, duration - tileDuration) + order;
      const left = (rect.x / 100) * previewWidth;
      const top = (rect.y / 100) * previewHeight;

      tile.className = `app-reveal-solid-tile app-reveal-scan-tile ${className}`;
      tile.style.width = `${rect.w}%`;
      tile.style.height = `${rect.h}%`;
      tile.style.left = `${rect.x}%`;
      tile.style.top = `${rect.y}%`;
      tile.style.animationDelay = `${delay.toFixed(1)}ms`;
      tile.style.animationDuration = `${(tileDuration * durationScale).toFixed(1)}ms`;
      tile.style.setProperty("--tile-delay", `${delay.toFixed(1)}ms`);
      tile.style.backgroundPosition = `${(-left).toFixed(2)}px ${(-top).toFixed(2)}px`;
      tile.style.backgroundSize = `${previewWidth}px ${previewHeight}px`;
      appRevealOverlay.append(tile);
    };

    const fillGrid = Math.max(4, Math.min(36, grid));
    const fillTiles = [];
    for (let row = 0; row < fillGrid; row += 1) {
      for (let column = 0; column < fillGrid; column += 1) {
        fillTiles.push({ row, column });
      }
    }
    fillTiles
      .sort((a, b) => {
        const aX = (a.column + 0.5) / fillGrid;
        const aY = (a.row + 0.5) / fillGrid;
        const bX = (b.column + 0.5) / fillGrid;
        const bY = (b.row + 0.5) / fillGrid;
        const aDistance = Math.sqrt(
          (aX - cornerPoint.column) ** 2 + (aY - cornerPoint.row) ** 2,
        );
        const bDistance = Math.sqrt(
          (bX - cornerPoint.column) ** 2 + (bY - cornerPoint.row) ** 2,
        );
        return aDistance - bDistance;
      })
      .forEach(({ row, column }, order) => {
        const size = 100 / fillGrid;
        const centerX = (column + 0.5) / fillGrid;
        const centerY = (row + 0.5) / fillGrid;
        const distance = Math.sqrt(
          (centerX - cornerPoint.column) ** 2 +
            (centerY - cornerPoint.row) ** 2,
        );
        const wave = clamp(distance / maxDistance, 0, 1);
        appendScanTile(
          {
            x: column * size,
            y: row * size,
            w: size + 0.04,
            h: size + 0.04,
          },
          "is-fill",
          wave * 24 + (order % fillGrid) * 1.6,
          1.04,
        );
      });

    [
      { x: 10.8, y: 7.6, w: 16.8, h: 30.4 },
      { x: 63.6, y: 7.4, w: 19.2, h: 31.6 },
      { x: 11.4, y: 55.4, w: 8.8, h: 28.2 },
      { x: 27.6, y: 55.0, w: 9.4, h: 29.8 },
      { x: 66.2, y: 55.3, w: 9.6, h: 28.7 },
      { x: 82.2, y: 55.2, w: 8.3, h: 29.4 },
    ].forEach((rect, index) =>
      appendScanTile(
        rect,
        index < 2 ? "is-column is-heavy" : "is-column",
        index * 32,
        1.18,
      ),
    );

    [
      { x: 4, y: 2.4, w: 14, h: 1.4 },
      { x: 24, y: 1.5, w: 30, h: 1.4 },
      { x: 59, y: 2.4, w: 18, h: 1.4 },
      { x: 83, y: 1.8, w: 12, h: 1.4 },
      { x: 2, y: 42.8, w: 18, h: 1.3 },
      { x: 27, y: 43.4, w: 24, h: 1.3 },
      { x: 57, y: 42.6, w: 19, h: 1.3 },
      { x: 80, y: 43.2, w: 17, h: 1.3 },
      { x: 1, y: 87.6, w: 12, h: 1.3 },
      { x: 20, y: 88.5, w: 28, h: 1.3 },
      { x: 58, y: 87.9, w: 20, h: 1.3 },
      { x: 86, y: 88.8, w: 9, h: 1.3 },
    ].forEach((rect, index) =>
      appendScanTile(rect, "is-band", index * 18, 0.78),
    );

    const fragmentCount = Math.max(12, Math.round(grid * intensity * 0.55));
    for (let index = 0; index < fragmentCount; index += 1) {
      const row = index % 9;
      const column = Math.floor(index / 9);
      const x = (column * 17 + row * 7) % 93;
      const y = 8 + ((row * 9 + column * 13) % 82);
      const w = 3.5 + ((index * 5) % 11);
      const h = index % 4 === 0 ? 2.1 : 1.2;

      appendScanTile(
        { x, y, w: Math.min(w, 98 - x), h },
        "is-fragment",
        index * 11,
        0.58,
      );
    }

    const layers = Math.max(1, Math.round(settings.layers ?? 3));
    if (layers > 1) {
      buildAppRevealTypographyTokens(settings, tileDuration);
    }
    if (layers > 2) {
      for (let row = 0; row < 7; row += 1) {
        appendScanTile(
          { x: 7 + row * 12, y: 13 + row * 4.8, w: 8, h: 0.9 },
          "is-noise",
          row * 24,
          0.42,
        );
        appendScanTile(
          { x: 14 + row * 10, y: 77 - row * 3.4, w: 10, h: 0.9 },
          "is-noise",
          row * 20,
          0.42,
        );
      }
    }
  }

  function gateAppPreviewSection() {
    appPreview.classList.add("is-section-reveal-gated");
  }

  function revealGatedAppPreviewSection() {
    appPreview.classList.remove("is-section-reveal-gated");
  }

  const appChartHoverData = [
    { month: "Jan", x: 0, target: 250, revenue: 350 },
    { month: "Feb", x: 39.1694, target: 275, revenue: 330 },
    { month: "Mar", x: 65.0814, target: 300, revenue: 455 },
    { month: "Apr", x: 104.853, target: 325, revenue: 480 },
    { month: "May", x: 135.586, target: 350, revenue: 420 },
    { month: "Jun", x: 165.114, target: 375, revenue: 510 },
    { month: "Jul", x: 230.195, target: 395, revenue: 580 },
    { month: "Aug", x: 266.954, target: 410, revenue: 525 },
    { month: "Sep", x: 318.176, target: 430, revenue: 625 },
    { month: "Oct", x: 370, target: 450, revenue: 715 },
  ];

  const appChartRevenueSegments = [
    [
      [0, 54.675],
      [9.44083, 56.925],
      [30.4919, 60.75],
      [39.1694, 58.05],
    ],
    [
      [39.1694, 58.05],
      [50.0163, 54.675],
      [54.8371, 43.2],
      [65.0814, 39.825],
    ],
    [
      [65.0814, 39.825],
      [75.3257, 36.45],
      [99.43, 35.775],
      [104.853, 35.775],
    ],
    [
      [104.853, 35.775],
      [110.277, 35.775],
      [128.958, 44.55],
      [135.586, 44.55],
    ],
    [
      [135.586, 44.55],
      [142.215, 44.55],
      [158.485, 33.75],
      [165.114, 31.05],
    ],
    [
      [165.114, 31.05],
      [171.743, 28.35],
      [222.362, 20.25],
      [230.195, 20.25],
    ],
    [
      [230.195, 20.25],
      [238.029, 20.25],
      [257.313, 29.025],
      [266.954, 29.025],
    ],
    [
      [266.954, 29.025],
      [276.596, 29.025],
      [301.906, 14.85],
      [318.176, 14.175],
    ],
    [
      [318.176, 14.175],
      [331.192, 13.635],
      [358.149, 4.5],
      [370, 0],
    ],
  ];

  function getCubicPoint(points, t) {
    const [p0, p1, p2, p3] = points;
    const mt = 1 - t;
    const a = mt * mt * mt;
    const b = 3 * mt * mt * t;
    const c = 3 * mt * t * t;
    const d = t * t * t;
    return {
      x: a * p0[0] + b * p1[0] + c * p2[0] + d * p3[0],
      y: a * p0[1] + b * p1[1] + c * p2[1] + d * p3[1],
    };
  }

  function getAppChartRevenuePointFromSegments(svgX) {
    const segment =
      appChartRevenueSegments.find(
        (points) => svgX >= points[0][0] && svgX <= points[3][0],
      ) ||
      (svgX < appChartRevenueSegments[0][0][0]
        ? appChartRevenueSegments[0]
        : appChartRevenueSegments[appChartRevenueSegments.length - 1]);
    let low = 0;
    let high = 1;

    for (let index = 0; index < 18; index += 1) {
      const mid = (low + high) / 2;
      const point = getCubicPoint(segment, mid);
      if (point.x < svgX) {
        low = mid;
      } else {
        high = mid;
      }
    }

    return getCubicPoint(segment, (low + high) / 2);
  }

  function getAppChartRevenuePoint(svgX) {
    const path = appChartOverlay?.querySelector(".app-chart-line-live");
    if (!path?.getTotalLength) {
      return getAppChartRevenuePointFromSegments(svgX);
    }

    const total = path.getTotalLength();
    let low = 0;
    let high = total;

    for (let index = 0; index < 18; index += 1) {
      const mid = (low + high) / 2;
      const point = path.getPointAtLength(mid);
      if (point.x < svgX) {
        low = mid;
      } else {
        high = mid;
      }
    }

    return path.getPointAtLength((low + high) / 2);
  }

  function getNearestAppChartMonth(svgX) {
    return appChartHoverData.reduce((best, item) => {
      if (!best || Math.abs(item.x - svgX) < Math.abs(best.x - svgX)) {
        return item;
      }
      return best;
    }, null);
  }

  function updateAppChartHover(event) {
    if (!appPreview || !appChartOverlay) {
      return;
    }

    const chartStage = appChartOverlay.querySelector(".app-chart-stage");
    const stageRect = chartStage?.getBoundingClientRect();
    const stageWidth = chartStage?.offsetWidth || 408;
    const stageScaleX =
      stageRect && stageWidth ? stageRect.width / Math.max(1, stageWidth) : 1;
    const svgWidth = 370;
    const plotLeft = 36;
    const plotTop =
      parseFloat(
        getComputedStyle(appChartOverlay).getPropertyValue("--chart-plot-top"),
      ) || 22;
    const plotWidth = 370;
    const leftBound = 0;
    const rightBound = 370;
    const fallbackSvgX = 165.114;
    const svgX =
      event && stageRect
        ? clamp(
            (((event.clientX - stageRect.left) / Math.max(0.001, stageScaleX) -
              plotLeft) /
              Math.max(1, plotWidth)) *
              svgWidth,
            leftBound,
            rightBound,
          )
        : fallbackSvgX;
    const revenuePoint = getAppChartRevenuePoint(svgX);
    const targetProgress = (svgX - leftBound) / (rightBound - leftBound);
    const targetY =
      plotTop + 67.5588 + (35.7056 - 67.5588) * clamp(targetProgress, 0, 1);
    const dotRadius = 4;
    const chartX = clamp(
      plotLeft + (svgX / svgWidth) * plotWidth,
      plotLeft + dotRadius,
      plotLeft + plotWidth - dotRadius,
    );
    const revenueY = plotTop + revenuePoint.y;
    const cardWidth = 112;
    const cardX = clamp(
      chartX + 16,
      40,
      Math.max(40, stageWidth - cardWidth - 4),
    );
    const cardY = clamp(Math.min(revenueY, targetY) - 40, 10, 92);
    const nearest = getNearestAppChartMonth(svgX) || appChartHoverData[5];

    appChartOverlay.style.setProperty(
      "--chart-hover-x",
      `${chartX.toFixed(1)}px`,
    );
    appChartOverlay.style.setProperty(
      "--chart-hover-revenue-y",
      `${revenueY.toFixed(1)}px`,
    );
    appChartOverlay.style.setProperty(
      "--chart-hover-target-y",
      `${targetY.toFixed(1)}px`,
    );
    appChartOverlay.style.setProperty(
      "--chart-card-x",
      `${cardX.toFixed(1)}px`,
    );
    appChartOverlay.style.setProperty(
      "--chart-card-y",
      `${cardY.toFixed(1)}px`,
    );
    appChartOverlay
      .querySelector(".app-chart-hover-card b")
      ?.replaceChildren(nearest.month);
    const values = appChartOverlay.querySelectorAll(
      ".app-chart-hover-card strong",
    );
    values[0]?.replaceChildren(`$${nearest.target}K`);
    values[1]?.replaceChildren(`$${nearest.revenue}K`);
  }

  function buildAppRevealTypographyTokens(settings, tileDuration) {
    const textTargets = [
      ".app-header h2",
      ".app-header p",
      ".metric-card p",
      ".metric-card strong",
      ".panel-title h3",
      ".region-panel h3",
      ".region-row span",
    ];
    const previewRect = appPreview.getBoundingClientRect();
    const scaleX = previewRect.width / Math.max(1, appPreview.offsetWidth);
    const scaleY = previewRect.height / Math.max(1, appPreview.offsetHeight);
    const cornerPoint = getRevealOriginPoint(settings);
    const duration = Math.max(0, Number(settings.duration || 980));

    appPreview
      .querySelectorAll(textTargets.join(","))
      .forEach((target, index) => {
        if (index > 28) {
          return;
        }
        const rect = target.getBoundingClientRect();
        const centerX =
          (rect.left + rect.width / 2 - previewRect.left) /
          Math.max(1, previewRect.width);
        const centerY =
          (rect.top + rect.height / 2 - previewRect.top) /
          Math.max(1, previewRect.height);
        const distance =
          Math.sqrt(
            (centerX - cornerPoint.column) ** 2 +
              (centerY - cornerPoint.row) ** 2,
          ) / (cornerPoint.maxDistance || Math.SQRT2);
        const token = document.createElement("span");
        const delay = distance * Math.max(0, duration - tileDuration) + 40;

        token.className = "app-reveal-type-token";
        token.style.left = `${((rect.left - previewRect.left) / scaleX).toFixed(1)}px`;
        token.style.top = `${((rect.top - previewRect.top) / scaleY).toFixed(1)}px`;
        token.style.width = `${Math.max(24, rect.width / scaleX).toFixed(1)}px`;
        token.style.animationDelay = `${delay.toFixed(1)}ms`;
        token.style.animationDuration = `${Math.max(160, tileDuration * 0.72).toFixed(1)}ms`;
        token.textContent =
          index % 3 === 0
            ? "[font:mono->inter]"
            : index % 3 === 1
              ? "[color:lock]"
              : "[type:resolve]";
        appRevealOverlay.append(token);
      });
  }

  function clearAppReveal({ preserveBootFrame = false } = {}) {
    appPreview.classList.remove(
      "app-reveal-running",
      "app-reveal-fading",
      "is-section-reveal-gated",
      "reveal-target-prototype",
      "reveal-target-polished",
      "reveal-colorWave",
      "reveal-pixelTiles",
      "reveal-pixelate",
      "reveal-scanSweep",
      "reveal-crtBloom",
    );
    if (!preserveBootFrame) {
      appPreview.classList.remove("app-boot-clip-enabled");
    }
    if (preserveBootFrame) {
      clearAppRevealOverlayNodes();
    } else {
      appRevealOverlay.replaceChildren();
    }
  }

  function setAppRevealCompleteTarget(revealsPolished) {
    appPreview.classList.remove(
      "app-reveal-running",
      "app-reveal-fading",
      "is-section-reveal-gated",
      "reveal-target-prototype",
      "reveal-target-polished",
      "reveal-colorWave",
      "reveal-pixelTiles",
      "reveal-pixelate",
      "reveal-scanSweep",
      "reveal-crtBloom",
    );
    appPreview.classList.remove("app-boot-clip-enabled");
    appRevealOverlay.replaceChildren();
    appPreview.classList.add("is-visible", "is-image-backed");
    appPreview.classList.toggle("is-wire", !revealsPolished);
    appPreview.classList.toggle("is-polished", revealsPolished);
    appPreview.classList.toggle("is-prototype-visible", !revealsPolished);
    if (revealsPolished) {
      updateAppChartHover();
    }
  }

  function triggerAppReveal(
    mode = "polished",
    state = currentState,
    { preserveBootFrame = false } = {},
  ) {
    const revealSettings = getAppRevealSettings(state);
    const revealsPolished = mode === "polished";
    clearAppReveal({ preserveBootFrame });

    if (revealSettings.preset === "none" || revealSettings.intensity <= 0) {
      setAppRevealCompleteTarget(revealsPolished);
      return 0;
    }

    setRootVar("--app-reveal-duration", `${revealSettings.duration}ms`);
    setRootVar("--app-reveal-strength", revealSettings.intensity);
    setRootVar(
      "--app-reveal-alpha",
      Math.min(0.95, 0.25 + revealSettings.intensity * 0.07).toFixed(2),
    );
    setRootVar(
      "--app-reveal-image",
      revealsPolished
        ? 'url("/img/home/hero/app-done.png")'
        : 'url("/img/home/hero/app-prototype.png")',
    );
    setRootVar(
      "--app-reveal-line-rgb",
      rgbTupleFromColor(revealSettings.lineColor, "#63f1ff"),
    );
    const lineDirection =
      revealSettings.lineDirection === "horizontal" ? "horizontal" : "vertical";
    setRootVar(
      "--app-reveal-line-angle",
      lineDirection === "horizontal" ? "0deg" : "90deg",
    );
    setRootVar(
      "--app-reveal-shadow-line-angle",
      lineDirection === "horizontal" ? "90deg" : "0deg",
    );
    setRootVar(
      "--app-reveal-line-opacity",
      clamp(Number(revealSettings.lineOpacity ?? 0.72), 0, 1).toFixed(2),
    );
    setRootVar(
      "--app-reveal-line-glow",
      `${Math.max(0, Number(revealSettings.lineGlow ?? 18)).toFixed(1)}px`,
    );
    setRootVar(
      "--app-reveal-line-hold",
      `${Math.max(0, Number(revealSettings.lineHold ?? 280)).toFixed(1)}ms`,
    );
    setRootVar(
      "--app-reveal-line-fade",
      `${Math.max(80, Number(revealSettings.lineFade ?? 520)).toFixed(1)}ms`,
    );
    appPreview.classList.add(
      `reveal-${revealSettings.preset}`,
      revealsPolished ? "reveal-target-polished" : "reveal-target-prototype",
    );

    appPreview.classList.add("is-wire", "is-image-backed");
    appPreview.classList.toggle("is-polished", false);
    appPreview.classList.toggle("is-prototype-visible", revealsPolished);

    if (revealSettings.preset === "colorWave") {
      buildAppColorWave(revealSettings, {
        preserveExisting: preserveBootFrame,
        includeChart: revealsPolished,
      });
    } else if (revealSettings.preset === "pixelTiles") {
      buildAppRevealPixels(revealSettings, {
        preserveExisting: preserveBootFrame,
      });
    }

    appPreview.classList.add("app-reveal-running");
    void appPreview.offsetWidth;
    const lineFadeTail =
      Math.max(0, Number(revealSettings.lineHold ?? 280)) +
      Math.max(80, Number(revealSettings.lineFade ?? 520));
    const cleanupDelay = Math.max(
      Number(revealSettings.duration || 0),
      Number(revealSettings.duration || 0) + lineFadeTail * 0.62,
    );
    const finalFadeDuration = 0;
    setRootVar("--app-reveal-final-fade", `${finalFadeDuration.toFixed(1)}ms`);

    window.setTimeout(() => {
      setAppRevealCompleteTarget(revealsPolished);
    }, cleanupDelay + finalFadeDuration);
    return Math.max(0, cleanupDelay + finalFadeDuration);
  }

  function getAppRevealDuration(revealSettings = getAppRevealSettings(5)) {
    if (
      revealSettings.preset === "none" ||
      Number(revealSettings.intensity || 0) <= 0
    ) {
      return 0;
    }

    return Math.max(0, Number(revealSettings.duration || 0));
  }

  function getAppRevealTotalDuration(revealSettings = getAppRevealSettings(5)) {
    const revealDuration = getAppRevealDuration(revealSettings);
    if (revealDuration <= 0) {
      return 0;
    }

    const lineFadeTail =
      Math.max(0, Number(revealSettings.lineHold ?? 280)) +
      Math.max(80, Number(revealSettings.lineFade ?? 520));
    return Math.max(revealDuration, revealDuration + lineFadeTail * 0.62);
  }

  async function playAppReveal(token, mode = "polished", state = currentState) {
    const duration = triggerAppReveal(mode, state);
    if (duration > 0) {
      await wait(duration + 260, token);
    }
    setAppRevealCompleteTarget(mode === "polished");
  }

  appChartOverlay?.addEventListener(
    "pointermove",
    (event) => {
      updateAppChartHover(event);
    },
    { signal: eventController.signal },
  );

  appPreview?.addEventListener(
    "pointermove",
    (event) => {
      if (
        !appPreview.classList.contains("is-image-backed") ||
        !appPreview.classList.contains("is-polished")
      ) {
        return;
      }

      const chartStage = appChartOverlay?.querySelector(".app-chart-stage");
      if (!chartStage) {
        return;
      }

      const rect = chartStage.getBoundingClientRect();
      const overlayRect = appChartOverlay.getBoundingClientRect();
      const insideOverlay =
        event.clientX >= overlayRect.left &&
        event.clientX <= overlayRect.right &&
        event.clientY >= overlayRect.top &&
        event.clientY <= overlayRect.bottom;

      if (insideOverlay) {
        updateAppChartHover(event);
      }
    },
    { signal: eventController.signal },
  );

  function getActiveFxSettingsPresets() {
    normalizeGroupFxSettings();
    return groupGlitchSettings.activePresets
      .filter((preset) => fxPresetOrder.includes(preset))
      .sort((a, b) => {
        const aIndex = fxSettingsPanelOrder.indexOf(a);
        const bIndex = fxSettingsPanelOrder.indexOf(b);
        return (
          (aIndex < 0 ? Number.MAX_SAFE_INTEGER : aIndex) -
          (bIndex < 0 ? Number.MAX_SAFE_INTEGER : bIndex)
        );
      });
  }

  function syncGroupFxLayerOrder() {
    const orderedPresets = getActiveFxSettingsPresets();
    const layerKeys = fxSettingsPanelOrder;
    const scanlinesZ = "330";
    layerKeys.forEach((preset) => {
      setRootVar(
        `--fx-z-${preset}`,
        preset === "vignette"
          ? "300"
          : preset === "scanlines"
            ? scanlinesZ
            : "80",
      );
    });

    orderedPresets.forEach((preset, index) => {
      setRootVar(
        `--fx-z-${preset}`,
        preset === "vignette"
          ? "300"
          : preset === "scanlines"
            ? scanlinesZ
            : String(90 + (orderedPresets.length - index) * 10),
      );
    });
  }

  function shadeMapHash(seed) {
    let value = seed >>> 0;
    value ^= value << 13;
    value ^= value >>> 17;
    value ^= value << 5;
    return ((value >>> 0) % 100000) / 100000;
  }

  function syncShadeMapCells({
    active,
    cellX,
    cellY,
    rectWidth,
    rectHeight,
    count,
    randomness,
    flickerNoise,
    flickerScale,
    speed,
    smoothness,
    opacity,
  }) {
    if (!shadeMapOverlay) {
      return;
    }

    if (!active || count <= 0) {
      shadeMapOverlay.replaceChildren();
      lastShadeMapRenderKey = "off";
      return;
    }

    const columns = Math.max(
      1,
      Math.floor(STAGE_BASE_WIDTH / Math.max(1, cellX)),
    );
    const rows = Math.max(
      1,
      Math.floor(STAGE_BASE_HEIGHT / Math.max(1, cellY)),
    );
    const available = columns * rows;
    const cellCount = Math.max(1, Math.min(Math.round(count), available, 360));
    const renderKey = [
      columns,
      rows,
      cellCount,
      Math.round(rectWidth),
      Math.round(rectHeight),
      randomness.toFixed(2),
      flickerNoise.toFixed(2),
      flickerScale.toFixed(2),
      speed.toFixed(2),
      smoothness.toFixed(2),
      opacity.toFixed(2),
    ].join(":");

    if (
      renderKey === lastShadeMapRenderKey &&
      shadeMapOverlay.childElementCount === cellCount
    ) {
      return;
    }

    const cells = [];
    const used = new Set();
    const randomRatio = randomness / 10;
    const noiseRatio = flickerNoise / 10;
    const speedBase = speed <= 0 ? 999999 : 2.4 / speed;
    const highBase = clamp(opacity, 0, 1);
    const lowBase = highBase * clamp(0.72 - noiseRatio * 0.7, 0, 0.72);
    const midBase = highBase * clamp(0.55 + noiseRatio * 0.22, 0, 0.92);
    const timing =
      smoothness >= 9.75
        ? "linear"
        : `steps(${Math.max(1, Math.round(1 + smoothness * 1.7))}, end)`;

    for (let index = 0; index < cellCount; index += 1) {
      let slot = Math.floor(shadeMapHash(index * 92821 + 17) * available);
      while (used.has(slot)) {
        slot = (slot + 1) % available;
      }
      used.add(slot);

      const column = slot % columns;
      const row = Math.floor(slot / columns);
      const jitterX =
        (shadeMapHash(index * 31847 + 11) - 0.5) * cellX * randomRatio;
      const jitterY =
        (shadeMapHash(index * 51749 + 23) - 0.5) * cellY * randomRatio;
      const sizeJitterX =
        1 + (shadeMapHash(index * 71933 + 31) - 0.5) * randomRatio * 0.42;
      const sizeJitterY =
        1 + (shadeMapHash(index * 61357 + 37) - 0.5) * randomRatio * 0.42;
      const durationJitter =
        0.35 + shadeMapHash(index * 44131 + 43) * (1.3 + noiseRatio * 1.8);
      const phase =
        shadeMapHash(index * 67213 + 59) *
        speedBase *
        (2.5 + noiseRatio * 6 + flickerScale);
      const pulseSeed = shadeMapHash(index * 19937 + 61);
      const low = clamp(lowBase + (pulseSeed - 0.5) * noiseRatio * 0.22, 0, 1);
      const mid = clamp(
        midBase + (shadeMapHash(index * 45989 + 71) - 0.5) * noiseRatio * 0.24,
        low,
        1,
      );
      const high = clamp(
        highBase -
          shadeMapHash(index * 83561 + 83) * noiseRatio * highBase * 0.08,
        mid,
        1,
      );

      const cell = document.createElement("i");
      cell.className = "shade-map-cell";
      cell.style.left = `${column * cellX + jitterX}px`;
      cell.style.top = `${row * cellY + jitterY}px`;
      cell.style.width = `${Math.max(1, rectWidth * sizeJitterX)}px`;
      cell.style.height = `${Math.max(1, rectHeight * sizeJitterY)}px`;
      cell.dataset.centerX = (
        column * cellX +
        jitterX +
        (rectWidth * sizeJitterX) / 2
      ).toFixed(2);
      cell.dataset.centerY = (
        row * cellY +
        jitterY +
        (rectHeight * sizeJitterY) / 2
      ).toFixed(2);
      cell.style.animationDuration = `${speedBase * durationJitter}s`;
      cell.style.animationDelay = `${-phase}s`;
      cell.style.animationTimingFunction = timing;
      cell.style.setProperty("--shade-cell-low", low.toFixed(3));
      cell.style.setProperty("--shade-cell-mid", mid.toFixed(3));
      cell.style.setProperty("--shade-cell-high", high.toFixed(3));
      cells.push(cell);
    }

    shadeMapOverlay.replaceChildren(...cells);
    lastShadeMapRenderKey = renderKey;
    if (shadeMapEraseTarget) {
      scheduleShadeMapCursorErase();
    }
  }

  function clearShadeMapCursorErase() {
    shadeMapErasePointer = null;
    shadeMapEraseTarget = null;
    shadeMapEraseCurrent = null;
    if (shadeMapEraseFrame) {
      window.cancelAnimationFrame(shadeMapEraseFrame);
      shadeMapEraseFrame = 0;
    }
    shadeMapOverlay
      ?.querySelectorAll(".shade-map-cell.is-cursor-erased")
      .forEach((cell) => {
        cell.classList.remove("is-cursor-erased");
      });
  }

  function getShadeMapEraseCenter() {
    return { x: STAGE_BASE_WIDTH / 2, y: STAGE_BASE_HEIGHT / 2 };
  }

  function scheduleShadeMapCursorErase() {
    if (!shadeMapEraseFrame) {
      shadeMapEraseFrame = window.requestAnimationFrame(
        applyShadeMapCursorErase,
      );
    }
  }

  function getShadeMapErasePointFromEvent(event) {
    const rect = stage.getBoundingClientRect();
    return {
      x:
        ((event.clientX - rect.left) / Math.max(1, rect.width)) *
        STAGE_BASE_WIDTH,
      y:
        ((event.clientY - rect.top) / Math.max(1, rect.height)) *
        STAGE_BASE_HEIGHT,
    };
  }

  function returnShadeMapCursorEraseToCenter() {
    const config = getGroupFxConfig("shademap");
    if (!isGroupFxActive("shademap") || config.cursorErase === false) {
      clearShadeMapCursorErase();
      return;
    }

    shadeMapErasePointer = null;
    shadeMapEraseTarget = getShadeMapEraseCenter();
    shadeMapEraseCurrent ||= { ...shadeMapEraseTarget };
    scheduleShadeMapCursorErase();
  }

  function returnShadeMapCursorEraseToCenterWhenOutside(event) {
    const rect = stage.getBoundingClientRect();
    const isInside =
      event.clientX >= rect.left &&
      event.clientX <= rect.right &&
      event.clientY >= rect.top &&
      event.clientY <= rect.bottom;

    if (!isInside) {
      returnShadeMapCursorEraseToCenter();
    }
  }

  function applyShadeMapCursorErase() {
    shadeMapEraseFrame = 0;
    const config = getGroupFxConfig("shademap");
    const active =
      isGroupFxActive("shademap") &&
      config.cursorErase !== false &&
      Number(config.intensity ?? 0) > 0 &&
      Number(config.opacity ?? 0) > 0 &&
      shadeMapEraseTarget;

    if (!shadeMapOverlay || !active) {
      clearShadeMapCursorErase();
      return;
    }

    const radius = Math.max(0, Number(config.cursorEraseRadius ?? 150));
    const fade = Math.max(0, Number(config.cursorEraseFade ?? 200));
    const strength = clamp(Number(config.cursorEraseStrength ?? 1), 0, 1);
    shadeMapEraseCurrent ||= { ...shadeMapEraseTarget };
    const dxTarget = shadeMapEraseTarget.x - shadeMapEraseCurrent.x;
    const dyTarget = shadeMapEraseTarget.y - shadeMapEraseCurrent.y;
    shadeMapEraseCurrent.x += dxTarget * 0.22;
    shadeMapEraseCurrent.y += dyTarget * 0.22;
    const x = shadeMapEraseCurrent.x;
    const y = shadeMapEraseCurrent.y;
    const radiusSq = radius * radius;

    setRootVar("--shade-map-cursor-fade", `${fade}ms`);
    setRootVar("--shade-map-cursor-erased-opacity", (1 - strength).toFixed(2));

    shadeMapOverlay.querySelectorAll(".shade-map-cell").forEach((cell) => {
      const centerX = Number(cell.dataset.centerX);
      const centerY = Number(cell.dataset.centerY);
      if (!Number.isFinite(centerX) || !Number.isFinite(centerY)) {
        return;
      }
      const dx = centerX - x;
      const dy = centerY - y;
      cell.classList.toggle("is-cursor-erased", dx * dx + dy * dy <= radiusSq);
    });

    if (dxTarget * dxTarget + dyTarget * dyTarget > 0.25) {
      scheduleShadeMapCursorErase();
    }
  }

  function updateShadeMapCursorErase(event) {
    const config = getGroupFxConfig("shademap");
    if (!isGroupFxActive("shademap") || config.cursorErase === false) {
      clearShadeMapCursorErase();
      return;
    }
    shadeMapErasePointer = event;
    shadeMapEraseTarget = getShadeMapErasePointFromEvent(event);
    shadeMapEraseCurrent ||= { ...shadeMapEraseTarget };
    scheduleShadeMapCursorErase();
  }

  function syncGlitchControl() {
    syncGroupFxLayerOrder();
    const primaryPreset = getPrimaryVisualFxPreset();
    const primaryConfig = getGroupFxConfig(primaryPreset);
    const intensity =
      primaryPreset === "rgbsplit"
        ? getRgbSplitMaxIntensity(primaryConfig)
        : (primaryConfig.intensity ?? 0);
    const detail = primaryConfig.detail ?? 0;
    const rawSpeed = primaryConfig.speed ?? 1;
    const speed = rawSpeed <= 0 ? 0 : Math.max(0.01, rawSpeed);
    const presetMultiplier =
      {
        off: 0,
        rgbsplit: 0.2,
        shademap: 0,
        rgbgrid: 0,
        scanlines: 0.26,
        parallax: 0,
        vignette: 0,
        perspective: 0,
      }[primaryPreset] ?? 0;

    stage.dataset.textFx = primaryPreset;
    setRootVar(
      "--text-glitch-x",
      `${Math.max(1, intensity) * presetMultiplier * Math.max(0.2, detail / 5)}px`,
    );
    setRootVar(
      "--text-glitch-y",
      `${Math.max(1, intensity) * 0.08 * Math.max(0.2, detail / 5)}px`,
    );
    setRootVar(
      "--text-glitch-rgb",
      `${Math.max(0.25, intensity) * 0.18 * Math.max(0.2, detail / 5)}px`,
    );
    setRootVar("--text-glitch-duration", `${primaryConfig.duration ?? 720}ms`);
    const rgbSplitConfig = getGroupFxConfig("rgbsplit");
    const rgbSplitLogoIntensity = getRgbSplitTargetIntensity(
      rgbSplitConfig,
      "logoIntensity",
    );
    const rgbSplitTextIntensity = getRgbSplitTargetIntensity(
      rgbSplitConfig,
      "textIntensity",
    );
    const rgbSplitWindowIntensity = getRgbSplitTargetIntensity(
      rgbSplitConfig,
      "windowIntensity",
    );
    const rgbSplitIntensity = getRgbSplitMaxIntensity(rgbSplitConfig);
    const rgbSplitSpeed =
      rgbSplitConfig.speed <= 0 ? 0 : Math.max(0.01, rgbSplitConfig.speed ?? 1);
    setRootVar(
      "--rgb-split-x",
      `${Math.max(0, rgbSplitConfig.splitX ?? 2.1)}px`,
    );
    setRootVar(
      "--rgb-split-y",
      `${Math.max(0, rgbSplitConfig.splitY ?? 0.4)}px`,
    );
    setRootVar(
      "--rgb-split-alpha",
      Math.min(0.92, Math.max(0, rgbSplitIntensity / 8)).toFixed(2),
    );
    setRootVar(
      "--rgb-split-logo-alpha",
      Math.min(0.92, Math.max(0, rgbSplitLogoIntensity / 8)).toFixed(2),
    );
    setRootVar(
      "--rgb-split-text-alpha",
      Math.min(0.92, Math.max(0, rgbSplitTextIntensity / 8)).toFixed(2),
    );
    setRootVar(
      "--rgb-split-window-alpha",
      Math.min(0.92, Math.max(0, rgbSplitWindowIntensity / 8)).toFixed(2),
    );
    setRootVar(
      "--rgb-split-color-a",
      rgbTupleFromColor(rgbSplitConfig.colorA, "#ff314a"),
    );
    setRootVar(
      "--rgb-split-color-b",
      rgbTupleFromColor(rgbSplitConfig.colorB, "#00e5ff"),
    );
    setRootVar(
      "--rgb-split-color-c",
      rgbTupleFromColor(rgbSplitConfig.colorC, "#465fff"),
    );
    setRootVar(
      "--rgb-split-duration",
      rgbSplitSpeed === 0
        ? "999999s"
        : `${Math.max(0.22, 1.4 / rgbSplitSpeed)}s`,
    );
    stage.classList.toggle(
      "fx-rgb-split",
      isGroupFxActive("rgbsplit") && rgbSplitIntensity > 0,
    );
    const shadeMapConfig = getGroupFxConfig("shademap");
    const shadeMapIntensity = Math.max(0, shadeMapConfig.intensity ?? 0);
    const shadeMapDetail = Math.max(0, shadeMapConfig.detail ?? 6);
    const shadeMapSpeed =
      shadeMapConfig.speed <= 0 ? 0 : Math.max(0.01, shadeMapConfig.speed ?? 1);
    const shadeMapSmoothness = clamp(
      Number(shadeMapConfig.smoothness ?? 0),
      0,
      10,
    );
    const shadeMapFlickerNoise = clamp(
      Number(shadeMapConfig.flickerNoise ?? 5),
      0,
      10,
    );
    const shadeMapFlickerScale = clamp(
      Number(shadeMapConfig.flickerScale ?? 1),
      0.25,
      5,
    );
    const shadeMapOpacity = Math.max(
      0,
      Math.min(1, shadeMapConfig.opacity ?? 0.58),
    );
    const shadeMapXDensity = clamp(
      Number(shadeMapConfig.xDensity ?? 18),
      2,
      80,
    );
    const shadeMapYDensity = clamp(
      Number(shadeMapConfig.yDensity ?? 18),
      2,
      80,
    );
    const shadeMapRectWidth = clamp(
      Number(shadeMapConfig.blockWidth ?? 210),
      1,
      720,
    );
    const shadeMapRectHeight = clamp(
      Number(shadeMapConfig.blockHeight ?? 34),
      1,
      360,
    );
    const shadeMapQuantity = clamp(
      Number(shadeMapConfig.quantity ?? 72),
      1,
      360,
    );
    const shadeMapRandomness = clamp(
      Number(shadeMapConfig.randomness ?? 3),
      0,
      10,
    );
    const shadeMapRandomnessRatio = shadeMapRandomness / 10;
    const shadeMapCellX = clamp(
      STAGE_BASE_WIDTH / shadeMapXDensity,
      4,
      STAGE_BASE_WIDTH,
    );
    const shadeMapCellY = clamp(
      STAGE_BASE_HEIGHT / shadeMapYDensity,
      4,
      STAGE_BASE_HEIGHT,
    );
    const shadeMapActive =
      isGroupFxActive("shademap") &&
      shadeMapIntensity > 0 &&
      shadeMapOpacity > 0;
    setRootVar("--shade-map-alpha", shadeMapActive ? "1" : "0");
    setRootVar(
      "--shade-map-detail",
      Math.max(0, shadeMapDetail / 10).toFixed(2),
    );
    setRootVar("--shade-map-density-x", shadeMapXDensity.toFixed(2));
    setRootVar("--shade-map-density-y", shadeMapYDensity.toFixed(2));
    setRootVar("--shade-map-quantity", shadeMapQuantity.toFixed(2));
    setRootVar("--shade-map-randomness", shadeMapRandomnessRatio.toFixed(2));
    setRootVar(
      "--shade-map-blur",
      `${Math.max(0, shadeMapConfig.blur ?? 12)}px`,
    );
    setRootVar("--shade-map-cell-x", `${shadeMapCellX}px`);
    setRootVar("--shade-map-cell-y", `${shadeMapCellY}px`);
    setRootVar("--shade-map-square-x", `${shadeMapRectWidth}px`);
    setRootVar("--shade-map-square-y", `${shadeMapRectHeight}px`);
    setRootVar(
      "--shade-map-cell-size",
      `${Math.max(8, Math.min(shadeMapRectWidth, shadeMapRectHeight))}px`,
    );
    setRootVar(
      "--shade-map-offset-a-x",
      `${17 + shadeMapRandomnessRatio * 83}px`,
    );
    setRootVar(
      "--shade-map-offset-a-y",
      `${9 + shadeMapRandomnessRatio * 41}px`,
    );
    setRootVar(
      "--shade-map-offset-b-x",
      `${(0.45 + shadeMapRandomnessRatio * 0.37).toFixed(2)}`,
    );
    setRootVar(
      "--shade-map-offset-b-y",
      `${(0.35 + shadeMapRandomnessRatio * 0.49).toFixed(2)}`,
    );
    setRootVar(
      "--shade-map-offset-c-x",
      `${(1.1 + shadeMapRandomnessRatio * 0.91).toFixed(2)}`,
    );
    setRootVar(
      "--shade-map-offset-c-y",
      `${(0.72 + shadeMapRandomnessRatio * 0.67).toFixed(2)}`,
    );
    setRootVar(
      "--shade-map-scale-b-x",
      `${(1.6 + shadeMapRandomnessRatio * 1.15).toFixed(2)}`,
    );
    setRootVar(
      "--shade-map-scale-b-y",
      `${(1.35 + shadeMapRandomnessRatio * 0.78).toFixed(2)}`,
    );
    setRootVar(
      "--shade-map-scale-c-x",
      `${(2.4 + shadeMapRandomnessRatio * 1.7).toFixed(2)}`,
    );
    setRootVar(
      "--shade-map-scale-c-y",
      `${(1.9 + shadeMapRandomnessRatio * 1.25).toFixed(2)}`,
    );
    setRootVar("--shade-map-flicker-scale", shadeMapFlickerScale.toFixed(2));
    setRootVar(
      "--shade-map-cursor-fade",
      `${Math.max(0, Number(shadeMapConfig.cursorEraseFade ?? 200))}ms`,
    );
    setRootVar(
      "--shade-map-cursor-erased-opacity",
      (
        1 - clamp(Number(shadeMapConfig.cursorEraseStrength ?? 1), 0, 1)
      ).toFixed(2),
    );
    setRootVar(
      "--shade-map-layer-a-alpha",
      shadeMapQuantity >= 1 ? "1.00" : "0.00",
    );
    setRootVar(
      "--shade-map-layer-b-alpha",
      clamp((shadeMapQuantity - 5) / 5, 0, 1).toFixed(2),
    );
    setRootVar(
      "--shade-map-flicker-a",
      shadeMapSpeed === 0
        ? "999999s"
        : `${Math.max(0.08, 1.45 / shadeMapSpeed)}s`,
    );
    setRootVar(
      "--shade-map-flicker-b",
      shadeMapSpeed === 0
        ? "999999s"
        : `${Math.max(0.06, 0.98 / shadeMapSpeed)}s`,
    );
    setRootVar(
      "--shade-map-color",
      rgbTupleFromColor(shadeMapConfig.color, "#ffffff"),
    );
    stage.classList.toggle("fx-shade-map", shadeMapActive);
    syncShadeMapCells({
      active: shadeMapActive,
      cellX: shadeMapCellX,
      cellY: shadeMapCellY,
      rectWidth: shadeMapRectWidth,
      rectHeight: shadeMapRectHeight,
      count: shadeMapQuantity,
      randomness: shadeMapRandomness,
      flickerNoise: shadeMapFlickerNoise,
      flickerScale: shadeMapFlickerScale,
      speed: shadeMapSpeed,
      smoothness: shadeMapSmoothness,
      opacity: shadeMapOpacity,
    });
    if (!shadeMapActive || shadeMapConfig.cursorErase === false) {
      clearShadeMapCursorErase();
    } else if (!shadeMapEraseTarget) {
      returnShadeMapCursorEraseToCenter();
    } else {
      scheduleShadeMapCursorErase();
    }
    const rgbGridConfig = getGroupFxConfig("rgbgrid");
    const rgbGridIntensity = Math.max(0, rgbGridConfig.intensity ?? 0);
    const rgbGridStripe = Math.max(0, rgbGridConfig.stripeHeight ?? 7);
    const rgbGridGap = Math.max(0, rgbGridConfig.stripeGap ?? 0);
    const rgbGridSpeed =
      rgbGridConfig.speed <= 0 ? 0 : Math.max(0.01, rgbGridConfig.speed ?? 0.4);
    const rgbGridOpacity = Math.max(
      0,
      Math.min(1, rgbGridConfig.opacity ?? 0.18),
    );
    const rgbGridActive =
      isGroupFxActive("rgbgrid") &&
      rgbGridIntensity > 0 &&
      rgbGridOpacity > 0 &&
      rgbGridStripe > 0;
    setRootVar("--rgb-grid-stripe", `${rgbGridStripe}px`);
    setRootVar("--rgb-grid-stop-2", `${rgbGridStripe * 2}px`);
    setRootVar("--rgb-grid-stop-3", `${rgbGridStripe * 3}px`);
    setRootVar(
      "--rgb-grid-cycle",
      `${Math.max(1, rgbGridStripe * 3 + rgbGridGap)}px`,
    );
    setRootVar(
      "--rgb-grid-softness",
      `${Math.max(0, rgbGridConfig.softness ?? 0)}px`,
    );
    setRootVar(
      "--rgb-grid-strength",
      Math.min(10, rgbGridIntensity).toFixed(2),
    );
    setRootVar(
      "--rgb-grid-opacity",
      rgbGridActive
        ? Math.min(
            1,
            rgbGridOpacity * Math.max(0.18, rgbGridIntensity / 6),
          ).toFixed(2)
        : "0",
    );
    setRootVar(
      "--rgb-grid-duration",
      rgbGridSpeed === 0 ? "999999s" : `${Math.max(0.5, 9 / rgbGridSpeed)}s`,
    );
    setRootVar(
      "--rgb-grid-red",
      rgbTupleFromColor(rgbGridConfig.redColor, "#2c070c"),
    );
    setRootVar(
      "--rgb-grid-green",
      rgbTupleFromColor(rgbGridConfig.greenColor, "#072614"),
    );
    setRootVar(
      "--rgb-grid-blue",
      rgbTupleFromColor(rgbGridConfig.blueColor, "#080c2d"),
    );
    stage.classList.toggle("fx-rgb-grid", rgbGridActive);
    const scanlineConfig = getGroupFxConfig("scanlines");
    const scanlineRawSpeed = scanlineConfig.speed ?? 1;
    const scanlineSpeed =
      scanlineRawSpeed <= 0 ? 0 : Math.max(0.01, scanlineRawSpeed);
    const overlayIntensity = Math.max(
      intensity,
      isGroupFxActive("scanlines") ? scanlineConfig.intensity : 0,
    );
    setRootVar(
      "--group-fx-alpha",
      Math.min(0.85, Math.max(0, overlayIntensity / 10)).toFixed(2),
    );
    setRootVar("--group-fx-detail", Math.max(0, detail / 10).toFixed(2));
    setRootVar("--group-fx-speed", speed.toFixed(2));
    setRootVar(
      "--group-fx-scan-duration",
      scanlineSpeed === 0
        ? "999999s"
        : `${Math.max(20, 166 / scanlineSpeed)}ms`,
    );
    setRootVar(
      "--group-fx-bar-duration",
      scanlineSpeed === 0 ? "999999s" : `${Math.max(0.3, 2 / scanlineSpeed)}s`,
    );
    const scanlineThickness = scanlineConfig.thickness ?? 2;
    const scanlineSpacing = scanlineConfig.spacing ?? 3;
    const scanlineDetail = scanlineConfig.detail ?? 6;
    setRootVar("--scanline-thickness", `${scanlineThickness}px`);
    setRootVar("--scanline-spacing", `${scanlineSpacing}px`);
    setRootVar(
      "--scanline-step",
      `${Math.max(1, scanlineThickness + scanlineSpacing)}px`,
    );
    setRootVar(
      "--scanline-line-alpha",
      scanlineThickness <= 0
        ? "0"
        : Math.max(0, scanlineDetail / 10).toFixed(2),
    );
    setRootVar("--scanline-bar-height", `${scanlineConfig.barHeight ?? 180}%`);
    setRootVar(
      "--scanline-sweep-opacity",
      Math.max(0, Math.min(1, scanlineConfig.sweepOpacity ?? 0.42)).toFixed(2),
    );
    setRootVar(
      "--scanline-sweep-glow",
      Math.max(0, Math.min(1, scanlineConfig.sweepGlow ?? 0.2)).toFixed(2),
    );
    setRootVar(
      "--scanline-sweep-shadow",
      Math.max(0, Math.min(1, scanlineConfig.sweepShadow ?? 0.28)).toFixed(2),
    );
    setRootVar(
      "--scanline-sweep-softness",
      `${Math.max(0, Math.min(80, scanlineConfig.sweepSoftness ?? 24))}%`,
    );
    setRootVar("--scanline-direction", scanlineConfig.reverse ? -1 : 1);
    stage.classList.toggle(
      "fx-scanlines",
      isGroupFxActive("scanlines") && scanlineConfig.intensity > 0,
    );
    stage.classList.remove("text-glitch-loop");
    const vignetteConfig = getGroupFxConfig("vignette");
    const vignetteHardOpacityRaw = Math.max(
      0,
      Math.min(1, vignetteConfig.hardOpacity ?? 0.38),
    );
    const vignetteActive =
      isGroupFxActive("vignette") &&
      vignetteConfig.intensity > 0 &&
      ((vignetteConfig.opacity ?? 0) > 0 || vignetteHardOpacityRaw > 0);
    const vignetteOpacity = vignetteActive
      ? Math.min(
          1,
          Math.max(0, vignetteConfig.opacity ?? 0.38) *
            Math.max(0.15, Math.min(1, (vignetteConfig.intensity ?? 4) / 6)),
        )
      : 0;
    const vignetteHardOpacity = vignetteActive ? vignetteHardOpacityRaw : 0;
    const fallbackVignetteEdge = Math.max(0, vignetteConfig.edge ?? 180);
    const vignetteTop = Math.max(0, vignetteConfig.top ?? fallbackVignetteEdge);
    const vignetteRight = Math.max(
      0,
      vignetteConfig.right ?? fallbackVignetteEdge,
    );
    const vignetteBottom = Math.max(
      0,
      vignetteConfig.bottom ?? fallbackVignetteEdge,
    );
    const vignetteLeft = Math.max(
      0,
      vignetteConfig.left ?? fallbackVignetteEdge,
    );
    setRootVar("--screen-vignette-opacity", vignetteOpacity.toFixed(2));
    setRootVar(
      "--screen-vignette-hard-opacity",
      vignetteHardOpacity.toFixed(2),
    );
    setRootVar("--screen-vignette-top", `${vignetteTop}px`);
    setRootVar(
      "--screen-vignette-top-mid",
      `${Math.round(vignetteTop * 0.35)}px`,
    );
    setRootVar("--screen-vignette-right", `${vignetteRight}px`);
    setRootVar(
      "--screen-vignette-right-mid",
      `${Math.round(vignetteRight * 0.35)}px`,
    );
    setRootVar("--screen-vignette-bottom", `${vignetteBottom}px`);
    setRootVar(
      "--screen-vignette-bottom-mid",
      `${Math.round(vignetteBottom * 0.35)}px`,
    );
    setRootVar("--screen-vignette-left", `${vignetteLeft}px`);
    setRootVar(
      "--screen-vignette-left-mid",
      `${Math.round(vignetteLeft * 0.35)}px`,
    );
    setRootVar(
      "--screen-vignette-radius",
      `${Math.max(0, vignetteConfig.roundness ?? 28)}px`,
    );
    setRootVar(
      "--screen-vignette-blur",
      `${Math.max(0, vignetteConfig.blur ?? 18)}px`,
    );
    setRootVar(
      "--screen-vignette-color",
      rgbTupleFromColor(vignetteConfig.color, "#000000"),
    );
    const perspectiveConfig = getGroupFxConfig("perspective");
    const perspectiveTiltX = Number(
      perspectiveConfig.tiltX ?? perspectiveConfig.tilt ?? 0,
    );
    const perspectiveTiltY = Number(
      perspectiveConfig.tiltY ?? perspectiveConfig.tilt ?? 0,
    );
    const perspectiveShiftX = Number(
      perspectiveConfig.shiftX ?? perspectiveConfig.shift ?? 0,
    );
    const perspectiveShiftY = Number(
      perspectiveConfig.shiftY ?? perspectiveConfig.shift ?? 0,
    );
    const perspectiveWindowLift = Number(perspectiveConfig.windowLift ?? 0);
    const perspectiveActive =
      isGroupFxActive("perspective") &&
      Number(perspectiveConfig.intensity ?? 0) > 0 &&
      Math.abs(perspectiveTiltX) +
        Math.abs(perspectiveTiltY) +
        Math.abs(perspectiveShiftX) +
        Math.abs(perspectiveShiftY) +
        Math.abs(perspectiveWindowLift) >
        0;
    stage.classList.toggle("fx-perspective", perspectiveActive);
    if (perspectiveActive) {
      schedulePerspectiveFrame();
    } else {
      setPerspectiveVars();
    }
    syncGridLayer();
  }

  function triggerTextGlitch({ force = false } = {}) {
    syncGlitchControl();
    stage.classList.remove("text-glitch-burst");
  }

  function getPlayerRuntimeFlags() {
    const features = window.DATABRICKS_USED_FEATURES || {};
    return {
      parallax: Boolean(features.parallax && isGroupFxActive("parallax")),
      perspective: Boolean(
        features.perspective && isGroupFxActive("perspective"),
      ),
    };
  }

  function scheduleRenderFrame(delay = RENDER_FRAME_MS) {
    if (destroyed || viewportPaused) {
      return;
    }

    if (renderFrameTimer) {
      return;
    }

    const runtimeFlags = getPlayerRuntimeFlags();
    if (!runtimeFlags.parallax && !runtimeFlags.perspective) {
      return;
    }

    renderFrameTimer = window.setTimeout(
      () => {
        renderFrameTimer = 0;
        drawGrid(performance.now());
      },
      Math.max(0, delay),
    );
  }

  function drawGrid(timestamp) {
    if (viewportPaused) {
      return;
    }

    const runtimeFlags = getPlayerRuntimeFlags();
    if (!runtimeFlags.parallax && !runtimeFlags.perspective) {
      if (gridWasVisible && gridCtx) {
        const { width, height } = resizeGridCanvas();
        gridCtx.clearRect(0, 0, width, height);
        gridWasVisible = false;
        lastGridRenderKey = "off";
      }
      return;
    }

    const frameTimestamp = timestamp || performance.now();
    if (
      lastRenderTickAt &&
      frameTimestamp - lastRenderTickAt < RENDER_FRAME_MS - 0.6
    ) {
      scheduleRenderFrame(
        RENDER_FRAME_MS - (frameTimestamp - lastRenderTickAt),
      );
      return;
    }
    lastRenderTickAt = lastRenderTickAt
      ? frameTimestamp - ((frameTimestamp - lastRenderTickAt) % RENDER_FRAME_MS)
      : frameTimestamp;

    updateParallaxFrame();
    updatePerspectiveFrame();
    if (gridWasVisible && gridCtx) {
      const { width, height } = resizeGridCanvas();
      gridCtx.clearRect(0, 0, width, height);
      gridWasVisible = false;
      lastGridRenderKey = "off";
    } else if (gridCanvasDirty) {
      resizeGridCanvas();
    }

    scheduleRenderFrame(RENDER_FRAME_MS);
  }

  function syncLogoSettings() {
    const logoSettings = stateLogoSettings[1];
    const height = Math.round(logoSettings.width * (352 / 2560));
    root.style.setProperty("--logo-left", `${logoSettings.x}px`);
    root.style.setProperty("--logo-top", `${logoSettings.y}px`);
    root.style.setProperty("--logo-width", `${logoSettings.width}px`);
    root.style.setProperty("--logo-height", `${height}px`);
    root.style.setProperty(
      "--logo-base-opacity",
      logoSettings.baseOpacity ?? 0.28,
    );
    root.style.setProperty(
      "--logo-main-color",
      logoSettings.mainColor || "#32323c",
    );
    root.style.setProperty(
      "--logo-reflex-opacity",
      Math.max(0, Math.min(1, Number(logoSettings.reflexOpacity ?? 0))).toFixed(
        2,
      ),
    );
    root.style.setProperty(
      "--logo-reflex-blur",
      `${Math.max(0, Number(logoSettings.reflexBlur ?? 12))}px`,
    );
    root.style.setProperty(
      "--logo-reflex-x",
      `${Number(logoSettings.reflexX ?? 0)}px`,
    );
    root.style.setProperty(
      "--logo-reflex-y",
      `${Number(logoSettings.reflexY ?? 0)}px`,
    );
    root.style.setProperty(
      "--logo-reflex-left",
      `${Number(logoSettings.reflexLeft ?? 0)}px`,
    );
    root.style.setProperty(
      "--logo-reflex-top",
      `${Number(logoSettings.reflexTop ?? 0)}px`,
    );
    root.style.setProperty(
      "--logo-reflex-scale-x",
      Number(logoSettings.reflexScaleX ?? 1).toFixed(3),
    );
    root.style.setProperty(
      "--logo-reflex-scale-y",
      Number(logoSettings.reflexScaleY ?? 1).toFixed(3),
    );
    root.style.setProperty(
      "--logo-reflex-shade-opacity",
      Math.max(
        0,
        Math.min(1, Number(logoSettings.reflexShadeOpacity ?? 0.58)),
      ).toFixed(2),
    );
    root.style.setProperty(
      "--logo-reflex-shade-x",
      `${Number(logoSettings.reflexShadeX ?? 0)}px`,
    );
    root.style.setProperty(
      "--logo-reflex-shade-y",
      `${Number(logoSettings.reflexShadeY ?? -4)}px`,
    );
    root.style.setProperty(
      "--logo-reflex-shade-width",
      `${Math.max(0, Number(logoSettings.reflexShadeWidth ?? 1600))}px`,
    );
    root.style.setProperty(
      "--logo-reflex-shade-height",
      `${Math.max(0, Number(logoSettings.reflexShadeHeight ?? 92))}px`,
    );
    root.style.setProperty(
      "--logo-reflex-shade-blur",
      `${Math.max(0, Number(logoSettings.reflexShadeBlur ?? 0))}px`,
    );
    root.style.setProperty(
      "--logo-reflex-shade-color",
      logoSettings.reflexShadeColor || "#000000",
    );
  }

  function syncLogoClickSettings() {
    sanitizeLogoClickSettings();
    root.style.setProperty("--logo-wave-color", logoClickSettings.waveColor);
    root.style.setProperty(
      "--logo-wave-rgb",
      rgbTupleFromColor(logoClickSettings.waveColor, "#62636a"),
    );
    root.style.setProperty(
      "--logo-wave-opacity",
      Math.max(0, Math.min(1, logoClickSettings.waveOpacity ?? 1)).toFixed(2),
    );
    root.style.setProperty("--logo-tail-color", logoClickSettings.tailColor);
    root.style.setProperty(
      "--logo-tail-rgb",
      rgbTupleFromColor(logoClickSettings.tailColor, "#62636a"),
    );
    root.style.setProperty(
      "--logo-tail-opacity",
      Math.max(0, Math.min(1, logoClickSettings.tailOpacity ?? 1)).toFixed(2),
    );
    root.style.setProperty(
      "--logo-press-x",
      `${Number(logoClickSettings.pressX || 0).toFixed(2)}px`,
    );
    root.style.setProperty(
      "--logo-press-y",
      `${Number(logoClickSettings.pressY || 0).toFixed(2)}px`,
    );
    root.style.setProperty(
      "--logo-wave-glow",
      Math.max(0, Math.min(1, logoClickSettings.glow ?? 0)).toFixed(2),
    );
    root.style.setProperty(
      "--logo-hover-glow-opacity",
      Math.max(0, Math.min(1, logoClickSettings.hoverGlowOpacity ?? 0)).toFixed(
        2,
      ),
    );
    root.style.setProperty(
      "--logo-hover-glow-blur",
      `${Math.max(0, Number(logoClickSettings.hoverGlowBlur ?? 0))}px`,
    );
    root.style.setProperty(
      "--logo-hover-glow-core",
      `${Math.max(0, Number(logoClickSettings.hoverGlowCore ?? 0))}px`,
    );
    root.style.setProperty(
      "--logo-hover-glow-color",
      logoClickSettings.hoverGlowColor || "#62636a",
    );
    root.style.setProperty(
      "--logo-split-amount",
      `${Number(logoClickSettings.splitAmount || 0).toFixed(2)}px`,
    );
    root.style.setProperty(
      "--logo-split-opacity",
      Math.max(0, Math.min(1, logoClickSettings.splitOpacity ?? 0)).toFixed(2),
    );
    root.style.setProperty(
      "--logo-split-red",
      logoClickSettings.splitRed || "#ff281e",
    );
    root.style.setProperty(
      "--logo-split-blue",
      logoClickSettings.splitBlue || "#00dcff",
    );
  }

  function syncRuntimeDragEchoSettings() {
    const settings = dragEchoSettings || {};
    const color = settings.color || "#ffffff";
    root.style.setProperty(
      "--drag-echo-color-rgb",
      rgbTupleFromColor(color, "#ffffff"),
    );
    root.style.setProperty(
      "--drag-echo-opacity",
      Math.max(0, Math.min(1, Number(settings.opacity ?? 0.74))).toFixed(2),
    );
    root.style.setProperty(
      "--drag-echo-lifetime",
      `${Math.max(0, Number(settings.lifetime ?? 760))}ms`,
    );
    root.style.setProperty(
      "--drag-echo-stroke-width",
      `${Math.max(0, Number(settings.width ?? 1))}px`,
    );
  }

  function syncRuntimeVisualSettings() {
    syncPowerOnVariables(getAnimationSettings(1));
    syncPromptCursorTiming(getAnimationSettings(2));
    syncBuildStatusTuning(getAnimationSettings(3));
    syncTextLayers();
    syncLogoSettings();
    syncLogoClickSettings();
    syncSharedAppPositionCss();
    syncStaticAppCss();
    syncPreAppFocusCss();
    syncRuntimeDragEchoSettings();
    const features = window.DATABRICKS_USED_FEATURES || {};
    if (
      features.parallax ||
      features.perspective ||
      features.rgbSplit ||
      features.shadeMap ||
      features.rgbGrid ||
      features.scanlines ||
      features.vignette
    ) {
      syncGlitchControl();
    }
  }

  function updateStateReadout() {}
  function animateTimelineState() {}
  function clearPrototypeBuild() {}

  function disableUnusedRuntimeFeatures() {
    const features = window.DATABRICKS_USED_FEATURES || {};

    if (!features.powerOn) {
      Object.assign(stateAnimationSettings[1], {
        powerOnEnabled: false,
        powerOnDuration: 0,
        powerOnLineDuration: 0,
        powerOnLineExpandDuration: 0,
        powerOnYScaleDuration: 0,
        powerOnBloom: 0,
        powerOnBrightness: 0,
        powerOnStretchBrightness: 0,
      });
    }

    if (!features.logoTubeWarmup) {
      Object.assign(stateAnimationSettings[1], {
        logoTubeWarmupEnabled: false,
        logoTubeWarmupDuration: 0,
      });
    }

    if (!features.terminalScroll) {
      Object.assign(stateAnimationSettings[4], {
        terminalScroll: false,
        terminalScrollDistance: 0,
        terminalScrollDuration: 0,
      });
    }

    if (!features.buildStatus) {
      Object.assign(stateAnimationSettings[3], {
        statusOkSteps: 0,
        statusRowDuration: 0,
        statusRowStagger: 0,
      });
    }

    if (!features.windowBoot) {
      Object.assign(stateAnimationSettings[5], {
        windowBootEnabled: false,
        windowBootDuration: 0,
        windowBootEchoCount: 0,
        windowBootEchoOpacity: 0,
        windowBootAsciiDuration: 0,
      });
    }

    if (!features.appReveal) {
      Object.keys(stateAppRevealSettings).forEach((state) => {
        stateAppRevealSettings[state] = {
          preset: "none",
          intensity: 0,
          duration: 0,
        };
      });
    }

    if (!features.dragEcho) {
      Object.assign(dragEchoSettings, {
        opacity: 0,
        lifetime: 0,
        maxFrames: 0,
      });
    }

    if (!features.logoClick) {
      Object.assign(logoClickSettings, {
        waveOpacity: 0,
        tailOpacity: 0,
        pressX: 0,
        pressY: 0,
        glow: 0,
        hoverGlowOpacity: 0,
        splitOpacity: 0,
      });
    }

    Object.values(stateGridSettings).forEach((settings) => {
      settings.enabled = false;
      settings.opacity = 0;
    });

    if (
      !features.parallax &&
      !features.perspective &&
      !features.rgbSplit &&
      !features.shadeMap &&
      !features.rgbGrid &&
      !features.scanlines &&
      !features.vignette
    ) {
      groupGlitchSettings.activePresets = [];
      groupGlitchSettings.preset = "off";
    }
  }

  async function playTimeline(
    startState = settings.timelineTrimStart,
    endState = settings.timelineTrimEnd,
  ) {
    startState = Math.max(
      1,
      Math.min(TIMELINE_STATE_COUNT, Math.round(startState)),
    );
    endState = Math.max(
      startState,
      Math.min(TIMELINE_STATE_COUNT, Math.round(endState)),
    );
    runId += 1;
    const token = runId;
    timelineRunning = true;
    settings.paused = false;
    prepareStateForAnimation(startState);

    try {
      do {
        prepareStateForAnimation(startState);
        for (let state = startState; state <= endState; state += 1) {
          await runStateAnimation(state, token);
        }
      } while (settings.timelineLoop && token === runId);
    } catch (error) {
      if (error.message !== "Animation cancelled") {
        throw error;
      }
    } finally {
      if (token === runId) {
        timelineRunning = false;
        settings.paused = true;
      }
    }
  }

  function setViewportPaused(paused) {
    if (destroyed) {
      return;
    }

    if (paused === viewportPaused) {
      return;
    }

    viewportPaused = paused;
    root.dataset.viewportPaused = String(viewportPaused);
    stage.classList.toggle("is-render-paused", viewportPaused);
    settings.paused = viewportPaused;

    if (viewportPaused) {
      if (renderFrameTimer) {
        window.clearTimeout(renderFrameTimer);
        renderFrameTimer = 0;
      }

      return;
    }

    drawGrid(performance.now());

    if (!timelineRunning) {
      playTimeline(settings.timelineTrimStart, settings.timelineTrimEnd);
    }
  }

  window.addEventListener(
    "db-hero-viewport-change",
    (event) => {
      intersectionPaused = event.detail?.isInViewport === false;
      setViewportPaused(intersectionPaused || documentVisibilityPaused);
    },
    { signal: eventController.signal },
  );

  document.addEventListener(
    "visibilitychange",
    () => {
      documentVisibilityPaused = document.hidden;
      setViewportPaused(intersectionPaused || documentVisibilityPaused);
    },
    { signal: eventController.signal },
  );

  window.addEventListener(
    "resize",
    () => {
      applyStageFit();
      resizeGridCanvas();
      drawGrid(performance.now());
    },
    { signal: eventController.signal },
  );

  function bootExportPlayer() {
    disableUnusedRuntimeFeatures();
    applyConfig(window.DATABRICKS_EXPORT_CONFIG);
    syncRuntimeVisualSettings();
    loadLogoSvg();
    applyStageFit();
    resizeGridCanvas();

    if (window.DATABRICKS_USED_FEATURES?.dragEcho) {
      bindRuntimeAppWindowDrag();
    }

    if (
      window.DATABRICKS_USED_FEATURES?.parallax ||
      window.DATABRICKS_USED_FEATURES?.perspective
    ) {
      const updateFxPointerFromWindow = (event) => {
        if (stage.contains(event.target)) {
          return;
        }
        const rect = stage.getBoundingClientRect();
        if (
          event.clientX < rect.left ||
          event.clientX > rect.right ||
          event.clientY < rect.top ||
          event.clientY > rect.bottom
        ) {
          return;
        }
        updateParallaxFromPointer(event);
      };
      stage.addEventListener("mousemove", updateParallaxFromPointer, {
        signal: eventController.signal,
      });
      window.addEventListener("pointermove", updateFxPointerFromWindow, {
        passive: true,
        signal: eventController.signal,
      });
      stage.addEventListener(
        "mouseleave",
        () => {
          const config = getGroupFxConfig("parallax");
          if (config.returnToCenter !== false) {
            parallaxTargetX = 0;
            parallaxTargetY = 0;
          }
          const perspectiveConfig = getGroupFxConfig("perspective");
          if (perspectiveConfig.returnToCenter !== false) {
            perspectiveTargetX = 0;
            perspectiveTargetY = 0;
          }
        },
        { signal: eventController.signal },
      );
    }

    if (window.DATABRICKS_USED_FEATURES?.shadeMap) {
      stage.addEventListener("mousemove", updateShadeMapCursorErase, {
        signal: eventController.signal,
      });
      stage.addEventListener("pointermove", updateShadeMapCursorErase, {
        signal: eventController.signal,
      });
      stage.addEventListener("mouseleave", returnShadeMapCursorEraseToCenter, {
        signal: eventController.signal,
      });
      stage.addEventListener(
        "pointerleave",
        returnShadeMapCursorEraseToCenter,
        {
          signal: eventController.signal,
        },
      );
      window.addEventListener(
        "pointermove",
        returnShadeMapCursorEraseToCenterWhenOutside,
        { passive: true, signal: eventController.signal },
      );
    }

    drawGrid(performance.now());
    settings.paused = false;
    playTimeline(settings.timelineTrimStart, settings.timelineTrimEnd);
  }

  function destroyExportPlayer() {
    if (destroyed) {
      return;
    }

    destroyed = true;
    runId += 1;
    logoRunId += 1;
    finalAsciiRunId += 1;
    timelineRunning = false;
    settings.paused = true;
    eventController.abort();
    releaseAppWindowDrag();
    clearWindowBoot();
    clearAppWindowEchoes();

    logoWaveTimers.forEach((timer) => window.clearTimeout(timer));
    logoWaveTimers = [];
    logoHoverSuppressionTimers.forEach((timer) => window.clearTimeout(timer));
    logoHoverSuppressionTimers.clear();

    if (perspectiveFrameRequest) {
      window.cancelAnimationFrame(perspectiveFrameRequest);
      perspectiveFrameRequest = 0;
    }

    if (shadeMapEraseFrame) {
      window.cancelAnimationFrame(shadeMapEraseFrame);
      shadeMapEraseFrame = 0;
    }

    if (renderFrameTimer) {
      window.clearTimeout(renderFrameTimer);
      renderFrameTimer = 0;
    }
  }

  bootExportPlayer();
  return destroyExportPlayer;
}

window.DatabricksHeroPlayer?.destroy?.();
var activePlayerCleanup = null;
window.DatabricksHeroPlayer = {
  mount(root, options = {}) {
    activePlayerCleanup?.();
    const cleanup = createDatabricksHeroExportPlayer(root, options);
    activePlayerCleanup = cleanup;

    return () => {
      if (activePlayerCleanup !== cleanup) {
        return;
      }

      activePlayerCleanup = null;
      cleanup();
    };
  },
  destroy() {
    activePlayerCleanup?.();
    activePlayerCleanup = null;
  },
};
